### `question_737.md`

**שאלה 737.** נתונות שתי מחרוזות `num1` ו-`num2`, המייצגות מספרים שלמים לא שליליים. פתח/י אלגוריתם ב-Python להכפלת מספרים אלו והחזרת התוצאה כמחרוזת. אסור להשתמש בספרייה המובנית `BigInteger` או להמיר את נתוני הקלט למספרים שלמים.

**דוגמאות:**

```
קלט: num1 = "2", num2 = "3"
פלט: "6"

קלט: num1 = "123", num2 = "456"
פלט: "56088"
```

-   א. על מנת לפתור את הבעיה יש להמיר את המחרוזות למספרים שלמים ולהכפיל אותם, ולאחר מכן להמיר את התוצאה בחזרה למחרוזת.
-   ב. על מנת לפתור את הבעיה יש להשתמש באלגוריתם רקורסיבי ולהכפיל את המספרים תו-אחר-תו, החל מהסוף.
-   ג. על מנת לפתור את הבעיה יש ליצור מערך תוצאות עבור מכפלות חלקיות ולחבר אותן, תוך הדמיית כפל ארוך ידני של מספרים.
-   ד. על מנת לפתור את הבעיה די להשתמש רק בלולאת `for` ובאופרטור תנאי.

**תשובה נכונה: ג**

**הסבר:**

לפתרון בעיית הכפלת שני מספרים המיוצגים כמחרוזות, ללא שימוש בפונקציות מובנות להמרה למספרים, האלגוריתם האופטימלי מדמה כפל "ארוך" (כפל בטורים), כפי שנעשה באופן ידני.

*   **אלגוריתם (הדמיית כפל ארוך):**
    1.  **טיפול במקרי קצה:** אם אחת מהמחרוזות שווה ל-`"0"`, מחזירים `"0"`.
    2.  **תוצאה:** יוצרים רשימה `result` עם אפסים. אורכה שווה לסכום אורכי המחרוזות.
    3.   **איטרציה:** מבצעים איטרציה על הספרות של `num1` מהסוף, בלולאה הראשונה, ועל הספרות של `num2` מהסוף בלולאה המקוננת:
           *    מחשבים את מכפלת הספרות `digit1 * digit2`.
          *    מוסיפים מכפלה זו לתא המתאים ברשימה `result` (עם התחשבות בהיסט האינדקסים), מטפלים ביתרת העברה.
    4.  **בניית מחרוזת התוצאה:** בונים מחרוזת מרשימת `result`, תוך הסרת אפסים מובילים.

*   **יתרונות האלגוריתם:**
    *   **דיוק:** מבטיח תוצאה נכונה, כאילו הכפל בוצע ידנית.
    *  **הימנעות מהמרות למספרים:** לא נעשה שימוש בהמרות למספרים, והחישובים מבוצעים רק על ספרות כמחרוזות.

*  **סיבוכיות:** סיבוכיות הזמן של האלגוריתם היא `O(m*n)`, כאשר m ו-n הם אורכי המחרוזות.

**דוגמאות (פסאודו-קוד):**

```
function multiply_strings(num1, num2):
    if num1 == "0" or num2 == "0":
        return "0"

    m = length(num1)
    n = length(num2)
    result = new array with m+n size, initialized by 0.

    for i from m - 1 to 0:
      for j from n - 1 to 0:
          prod = int(num1[i]) * int(num2[j])
          pos1 = i+j
          pos2 = i+j+1;
        sum = prod + result[pos2]
        result[pos2] = sum%10 # save to position
        result[pos1] += sum / 10 # add carryover

    result_string = from result list to string, skipping leading zeroes
    return result_string
```
**דוגמאות מימוש ב-Python:**

```python
def multiply_strings(num1, num2):
    if num1 == "0" or num2 == "0":
        return "0"

    m = len(num1)
    n = len(num2)
    result = [0] * (m + n)

    for i in range(m - 1, -1, -1):
        for j in range(n - 1, -1, -1):
            prod = int(num1[i]) * int(num2[j])
            pos1 = i+j
            pos2 = i + j + 1
            sum_ = prod + result[pos2]
            result[pos2] = sum_ % 10 # save remainder
            result[pos1] += sum_ // 10   # save carryover

    res = "".join(map(str, result)).lstrip('0') # מסיר אפסים מובילים
    return  res if res else "0"


num11 = "2"
num21 = "3"
print(f"קלט: num1 = '{num11}', num2 = '{num21}'")
print(f"פלט: '{multiply_strings(num11, num21)}'") # יפלט: פלט: '6'

num12 = "123"
num22 = "456"
print(f"קלט: num1 = '{num12}', num2 = '{num22}'")
print(f"פלט: '{multiply_strings(num12, num22)}'")  # יפלט: פלט: '56088'

num13 = "0"
num23 = "100"
print(f"קלט: num1 = '{num13}', num2 = '{num23}'")
print(f"פלט: '{multiply_strings(num13, num23)}'") # יפלט: פלט: '0'

num14 = "111"
num24 = "111"
print(f"קלט: num1 = '{num14}', num2 = '{num24}'")
print(f"פלט: '{multiply_strings(num14, num24)}'") # יפלט: פלט: '12321'
```
**ניתוח האפשרויות:**
*   **א. על מנת לפתור את הבעיה יש להמיר את המחרוזות למספרים שלמים ולהכפיל אותם, ולאחר מכן להמיר את התוצאה בחזרה למחרוזת.:** לא נכון. אסור להמיר למספרים שלמים.
*   **ב. על מנת לפתור את הבעיה יש להשתמש באלגוריתם רקורסיבי ולהכפיל את המספרים תו-אחר-תו, החל מהסוף.:** לא נכון. גישה איטרטיבית יעילה יותר מרקורסיה.
*  **ג. על מנת לפתור את הבעיה יש ליצור מערך תוצאות עבור מכפלות חלקיות ולחבר אותן, תוך הדמיית כפל ארוך ידני של מספרים.:** נכון.
*  **ד. על מנת לפתור את הבעיה יתאים רק שימוש בספריית pandas.:** לא נכון. Pandas היא ספרייה לעבודה עם נתונים, ואינה מתאימה למשימה זו.

**לסיכום:**
*  האלגוריתם מחקה כפל "ארוך" ידני.
* משתמש במערך לאחסון תוצאות ביניים.
*  מאפשר הכפלת מספרים גדולים המיוצגים כמחרוזות.

לפיכך, התשובה הנכונה היא **ג. על מנת לפתור את הבעיה יש ליצור מערך תוצאות עבור מכפלות חלקיות ולחבר אותן, תוך הדמיית כפל ארוך ידני של מספרים.**
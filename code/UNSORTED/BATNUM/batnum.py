BATNUM: BATNUM
=================
רמת קושי: 2
-----------------
המשחק "BATNUM" (קרב המספרים) הוא משחק פשוט שבו שני שחקנים מזינים לסירוגין מספרים שלמים בין 1 ל-10. מטרת המשחק היא להגיע לסכום 100. כל שחקן מוסיף את המספר שהזין לסכום הכולל, והשחקן שמביא את הסכום ל-100 או יותר - מפסיד.

כללי המשחק:
1. שני שחקנים מזינים לסירוגין מספרים שלמים בין 1 ל-10.
2. כל מספר שהוזן מתווסף לסכום הכולל.
3. השחקן שלאחר הזנת המספר מטעמו הסכום מגיע ל-100 או יותר – מפסיד.
4. השחקן הראשון מתחיל את המשחק.
-----------------
אלגוריתם:
1. הגדרת סכום התחלתי ל-0.
2. התחלת לולאת המשחק.
3. הצגת הסכום הנוכחי.
4. בקשת קלט מהשחקן הראשון (מספר בין 1 ל-10).
5. אם המספר שהוזן אינו בטווח 1 עד 10, בקש קלט חוזר.
6. הוספת המספר שהוזן לסכום הכולל.
7. בדיקה האם הסכום עלה על 100. אם כן, השחקן הראשון הפסיד, סיים את המשחק.
8. הצגת הסכום הנוכחי.
9. בקשת קלט מהשחקן השני (מספר בין 1 ל-10).
10. אם המספר שהוזן אינו בטווח 1 עד 10, בקש קלט חוזר.
11. הוספת המספר שהוזן לסכום הכולל.
12. בדיקה האם הסכום עלה על 100. אם כן, השחקן השני הפסיד, סיים את המשחק.
13. חזור לשלב 3 אם הסכום לא עלה על 100.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InitializeSum["אתחול סכום: <code><b>totalSum = 0</b></code>"]
    InitializeSum --> GameLoopStart{"התחלת לולאת המשחק"}
    GameLoopStart --> OutputCurrentSum1["הצגת סכום נוכחי"]
    OutputCurrentSum1 --> InputPlayer1["קלט שחקן 1 (1-10): <code><b>player1Input</b></code>"]
    InputPlayer1 --> ValidateInput1{"בדיקה: <code><b>1 <= player1Input <= 10</b></code>?"}
    ValidateInput1 -- לא --> InputPlayer1
    ValidateInput1 -- כן --> AddInput1["<code><b>totalSum = totalSum + player1Input</b></code>"]
    AddInput1 --> CheckSum1{"בדיקה: <code><b>totalSum >= 100</b></code>?"}
    CheckSum1 -- כן --> OutputPlayer1Lose["הצגה: שחקן 1 הפסיד"]
    OutputPlayer1Lose --> End["סיום"]
    CheckSum1 -- לא --> OutputCurrentSum2["הצגת סכום נוכחי"]
    OutputCurrentSum2 --> InputPlayer2["קלט שחקן 2 (1-10): <code><b>player2Input</b></code>"]
    InputPlayer2 --> ValidateInput2{"בדיקה: <code><b>1 <= player2Input <= 10</b></code>?"}
     ValidateInput2 -- לא --> InputPlayer2
    ValidateInput2 -- כן --> AddInput2["<code><b>totalSum = totalSum + player2Input</b></code>"]
    AddInput2 --> CheckSum2{"בדיקה: <code><b>totalSum >= 100</b></code>?"}
    CheckSum2 -- כן --> OutputPlayer2Lose["הצגה: שחקן 2 הפסיד"]
    OutputPlayer2Lose --> End
     CheckSum2 -- לא --> GameLoopStart
```
    
**מקרא**:
    Start - התחלת התוכנית.
    InitializeSum - אתחול המשתנה totalSum (הסכום) ל-0.
    GameLoopStart - התחלת לולאת המשחק.
    OutputCurrentSum1 - הצגת הערך הנוכחי של הסכום עבור שחקן 1.
    InputPlayer1 - בקשת קלט מהשחקן 1 (מספר בין 1 ל-10).
    ValidateInput1 - בדיקה שהמספר שהוזן על ידי שחקן 1 נמצא בטווח 1 עד 10.
    AddInput1 - הוספת המספר שהוזן על ידי שחקן 1 לסכום הכולל.
    CheckSum1 - בדיקה האם הסכום עבר את 100 לאחר מהלך שחקן 1.
    OutputPlayer1Lose - הצגת הודעה על הפסד שחקן 1 אם הסכום שווה ל-100 או יותר.
    End - סיום התוכנית.
     OutputCurrentSum2 - הצגת הערך הנוכחי של הסכום עבור שחקן 2.
    InputPlayer2 - בקשת קלט מהשחקן 2 (מספר בין 1 ל-10).
    ValidateInput2 - בדיקה שהמספר שהוזן על ידי שחקן 2 נמצא בטווח 1 עד 10.
    AddInput2 - הוספת המספר שהוזן על ידי שחקן 2 לסכום הכולל.
    CheckSum2 - בדיקה האם הסכום עבר את 100 לאחר מהלך שחקן 2.
    OutputPlayer2Lose - הצגת הודעה על הפסד שחקן 2 אם הסכום שווה ל-100 או יותר.
```

# אתחול הסכום
totalSum = 0

# לולאת המשחק הראשית
while True:
    # מהלך שחקן ראשון
    print(f"הסכום הנוכחי: {totalSum}")
    while True:
        try:
            player1Input = int(input("שחקן 1, אנא הכנס מספר בין 1 ל-10: "))
            if 1 <= player1Input <= 10:
                break
            else:
                print("אנא הכנס מספר בין 1 ל-10.")
        except ValueError:
             print("אנא הכנס מספר שלם.")
    totalSum += player1Input

    # בדיקה האם שחקן ראשון הפסיד
    if totalSum >= 100:
        print("שחקן 1 הפסיד!")
        break

    # מהלך שחקן שני
    print(f"הסכום הנוכחי: {totalSum}")
    while True:
       try:
            player2Input = int(input("שחקן 2, אנא הכנס מספר בין 1 ל-10: "))
            if 1 <= player2Input <= 10:
                break
            else:
                print("אנא הכנס מספר בין 1 ל-10.")
       except ValueError:
           print("אנא הכנס מספר שלם.")
    totalSum += player2Input

    # בדיקה האם שחקן שני הפסיד
    if totalSum >= 100:
        print("שחקן 2 הפסיד!")
        break
```
```
הסבר הקוד:
1.  **אתחול הסכום**:
    -   `totalSum = 0`: מאתחל את המשתנה `totalSum` לאחסון הסכום הכולל במשחק, החל מ-0.
2.  **לולאת המשחק הראשית `while True:`**:
    -   לולאה אינסופית הנמשכת עד שאחד השחקנים מפסיד.
3.  **מהלך שחקן ראשון**:
    -   `print(f"הסכום הנוכחי: {totalSum}")`: מציג את הסכום הנוכחי על המסך לפני מהלך שחקן 1.
    -   **לולאת קלט עבור שחקן 1**:
        -   `while True:`: לולאה אינסופית להבטחת קלט תקין מהשחקן.
        -  `try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין ערך שאינו מספר שלם, תוצג הודעת שגיאה.
        -   `player1Input = int(input("שחקן 1, אנא הכנס מספר בין 1 ל-10: "))`: מבקש מהשחקן 1 להזין מספר וממיר אותו למספר שלם.
        -   `if 1 <= player1Input <= 10:`: בודק האם המספר שהוזן נמצא בטווח 1 עד 10. אם כן, לולאת הקלט מסתיימת באמצעות `break`.
        -   `else:`: אם המספר מחוץ לטווח, תוצג הודעה עם בקשה להזנה חוזרת.
    -   `totalSum += player1Input`: מוסיף את המספר שהוזן לסכום הכולל.
    -   `if totalSum >= 100:`: בודק האם הסכום עלה על 100.
        -   `print("שחקן 1 הפסיד!")`: מציג הודעה על הפסד שחקן 1.
        -   `break`: מסיים את לולאת המשחק הראשית.
4.  **מהלך שחקן שני**:
    -   `print(f"הסכום הנוכחי: {totalSum}")`: מציג את הסכום הנוכחי על המסך לפני מהלך שחקן 2.
    -    **לולאת קלט עבור שחקן 2**:
        -   `while True:`: לולאה אינסופית להבטחת קלט תקין מהשחקן.
        -  `try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין ערך שאינו מספר שלם, תוצג הודעת שגיאה.
        -   `player2Input = int(input("שחקן 2, אנא הכנס מספר בין 1 ל-10: "))`: מבקש מהשחקן 2 להזין מספר וממיר אותו למספר שלם.
        -   `if 1 <= player2Input <= 10:`: בודק האם המספר שהוזן נמצא בטווח 1 עד 10. אם כן, לולאת הקלט מסתיימת באמצעות `break`.
        -   `else:`: אם המספר מחוץ לטווח, תוצג הודעה עם בקשה להזנה חוזרת.
    -   `totalSum += player2Input`: מוסיף את המספר שהוזן לסכום הכולל.
    -   `if totalSum >= 100:`: בודק האם הסכום עלה על 100.
        -   `print("שחקן 2 הפסיד!")`: מציג הודעה על הפסד שחקן 2.
        -   `break`: מסיים את לולאת המשחק הראשית.
5. סיום התוכנית:
    - לולאת `while True` מסתיימת באמצעות הפקודה `break`
```
<HANG>:
=================
מורכבות: 3
-----------------
משחק "האיש התלוי" הוא משחק מילים שבו שחקן אחד (או המחשב) בוחר מילה, ושחקן אחר מנסה לנחש אותה לפי אותיות.
על כל אות שגויה, השחקן מקבל עונש, בדרך כלל בצורת חלק מציור התלייה. אם הציור הושלם, השחקן מפסיד.

כללי המשחק:
1. המחשב בוחר מילה אקראית מתוך רשימה מוגדרת מראש.
2. השחקן רואה את המילה מיוצגת באמצעות קווים תחתונים (אחד לכל אות).
3. השחקן מנסה לנחש את המילה על ידי הזנת אותיות.
4. אם האות שהוזנה קיימת במילה, היא מוצגת בעמדותיה המתאימות.
5. אם האות שהוזנה אינה קיימת במילה, השחקן מקבל עונש.
6. המשחק נמשך עד שהשחקן מנחש את המילה או ממצה את מגבלת העונשים.
-----------------
אלגוריתם:
1. לאתחל מערך של מילים שהמחשב יכול לבחור.
2. לבחור מילה אקראית מהמערך.
3. ליצור מחרוזת `GUESS$` , המורכבת מקווים תחתונים, באורך המילה שנבחרה.
4. לאתחל את מספר השגיאות, שווה ל-0.
5. להתחיל לולאה "כל עוד המילה לא נוחשה ומספר השגיאות קטן מ-6":
  5.1 לבקש מהשחקן להזין אות.
  5.2 אם האות שהוזנה קיימת במילה שנבחרה:
    5.2.1 לעדכן את המחרוזת `GUESS$` , ולהציג את האות בכל עמדותיה במילה.
    5.2.2 אם כל האותיות נוחשו, לעבור לשלב 6.
  5.3 אחרת:
    5.3.1 להגדיל את מספר השגיאות ב-1.
    5.3.2 להציג את תמונת התלייה, המתאימה למספר השגיאות הנוכחי.
  5.4 אם מספר השגיאות שווה ל-6, לעבור לשלב 7.
6. להציג את ההודעה "YOU GOT IT!", לאחר מכן את המילה שנבחרה, ולעבור לשלב 8.
7. להציג את ההודעה "SORRY, YOU DIDN'T GET IT.", לאחר מכן את המילה שנבחרה, ולעבור לשלב 8.
8. סוף המשחק.
-----------------

"""
import random

# רשימת מילים למשחק
WORDS = ["python", "java", "kotlin", "javascript", "swift", "ruby", "csharp"]

def draw_hangman(errors):
    """
    פונקציה להצגת מצב האיש התלוי בהתאם למספר השגיאות.
    
    Args:
      errors: מספר השגיאות שביצע השחקן.
    """
    hangman_stages = [
        """
          ________
         |        |
         |
         |
         |
         |
        ---
        """,
        """
          ________
         |        |
         |        O
         |
         |
         |
        ---
        """,
        """
          ________
         |        |
         |        O
         |        |
         |
         |
        ---
        """,
        """
          ________
         |        |
         |        O
         |       /|
         |
         |
        ---
        """,
        """
          ________
         |        |
         |        O
         |       /|\\
         |
         |
        ---
        """,
        """
          ________
         |        |
         |        O
         |       /|\\
         |       /
         |
        ---
        """,
        """
          ________
         |        |
         |        O
         |       /|\\
         |       / \\
         |
        ---
        """
    ]
    print(hangman_stages[errors])

def play_hangman():
    """פונקציה ראשית למשחק האיש התלוי"""
    # בוחרים מילה אקראית מהרשימה
    target_word = random.choice(WORDS).upper()
    # יוצרים מחרוזת מקווים תחתונים, המתאימה לאורך המילה
    guess_string = "_" * len(target_word)
    # מאתחלים את מונה השגיאות
    number_of_errors = 0
    
    # לולאת המשחק הראשית
    while number_of_errors < 6 and "_" in guess_string:
        print("מילה:", guess_string)
        # מבקשים מהמשתמש להזין אות
        user_letter = input("הזן אות: ").upper()
        
        # בודקים אם האות קיימת במילה
        if user_letter in target_word:
            # מעדכנים את guess_string, מציגים את האות במקומותיה
            new_guess_string = ""
            for i in range(len(target_word)):
                if target_word[i] == user_letter:
                    new_guess_string += user_letter
                else:
                    new_guess_string += guess_string[i]
            guess_string = new_guess_string
            
            # בודקים אם המילה נוחשה
            if guess_string == target_word:
                print("מזל טוב! ניחשת את המילה:", target_word)
                return  # מסיימים את המשחק
            
        else:
            # מגדילים את מספר השגיאות ומציירים את האיש התלוי
            number_of_errors += 1
            draw_hangman(number_of_errors)
    
    # בדיקה למקרה הפסד
    if number_of_errors == 6:
        print("מצטער, לא ניחשת את המילה. המילה שנבחרה:", target_word)
    
if __name__ == "__main__":
    play_hangman()
"""
ניתוח קוד:

1.  **ייבוא מודול `random`:**
    -   `import random`: מייבא את מודול random לבחירה אקראית של מילה.

2.  **רשימת מילים `WORDS`:**
    -   `WORDS = ["python", "java", "kotlin", "javascript", "swift", "ruby", "csharp"]`: רשימה המכילה מילים, שמתוכן המחשב בוחר מילה למשחק.
    
3. **פונקציה `draw_hangman(errors)`:**
    -   מציגה את מצב האיש התלוי בהתאם למספר השגיאות, באמצעות ASCII-art.
    -  `hangman_stages` - מערך של מחרוזות, המייצגות את שלבי האיש התלוי.
    -  `print(hangman_stages[errors])` - מציגה על המסך את המחרוזת המתאימה.

4.  **פונקציה `play_hangman()`:**
    -   **בחירת מילה:**
        -  `target_word = random.choice(WORDS).upper()`: בוחרת מילה אקראית מהרשימה `WORDS` וממירה אותה לאותיות גדולות.
    -   **יצירת מחרוזת לניחוש:**
        -   `guess_string = "_" * len(target_word)`: יוצרת מחרוזת, המורכבת מקווים תחתונים, שאורכה תואם לאורך המילה שנבחרה.
    -   **אתחול מונה שגיאות:**
        -   `number_of_errors = 0`: מגדירה את מספר השגיאות ההתחלתי ל-0.
    -   **לולאת המשחק הראשית `while number_of_errors < 6 and "_" in guess_string:`**:
        -   הלולאה נמשכת, כל עוד מספר השגיאות קטן מ-6 ובמחרוזת `guess_string` יש קווים תחתונים (כלומר, כל עוד המילה לא נוחשה ומגבלת השגיאות לא מוצתה).
        -   `print("מילה:", guess_string)`: מציגה את המצב הנוכחי של המילה עם אותיות שניחשו וקווים תחתונים.
        -   `user_letter = input("הזן אות: ").upper()`: מבקשת מהמשתמש להזין אות וממירה אותה לאות גדולה.
        -   **בדיקת קיום האות במילה:**
            -   `if user_letter in target_word:`: בודקת, האם האות שהוזנה קיימת במילה שנבחרה.
            -   אם האות קיימת:
                -  `new_guess_string = ""`: יוצרת מחרוזת ריקה `new_guess_string` להרכבת הגרסה החדשה של המילה המנוחשת.
                -  לולאת `for i in range(len(target_word))` עוברת על כל הסימנים במילה שנבחרה.
                -  אם האות הנוכחית במילה שנבחרה תואמת את האות שהוזנה `user_letter`, אז מוסיפה אותה ל-`new_guess_string`, אחרת, מוסיפה את הסימן מהעמדה הנוכחית מ-`guess_string`.
                - `guess_string = new_guess_string`: מעדכנת את `guess_string` עם הגרסה החדשה עם אותיות שניחשו.
                -  `if guess_string == target_word:`: בודקת, האם המילה נוחשה.
                -  `print("מזל טוב! ניחשת את המילה:", target_word)`: מציגה ברכה ואת המילה שנבחרה.
                -  `return`: מסיימת את הפונקציה (המשחק).
            -   **אם האות אינה קיימת במילה:**
                -   `number_of_errors += 1`: מגדילה את מונה השגיאות ב-1.
                -   `draw_hangman(number_of_errors)`: קוראת לפונקציה `draw_hangman` כדי להציג את מצב האיש התלוי.
    -   **בדיקה למקרה הפסד:**
        -   `if number_of_errors == 6:`: בודקת האם מספר השגיאות שווה ל-6.
        -   `print("מצטער, לא ניחשת את המילה. המילה שנבחרה:", target_word)`: מציגה הודעה על הפסד ואת המילה שנבחרה.

5. **הפעלת המשחק:**
    -  `if __name__ == "__main__":`: בלוק זה מבטיח שהפונקציה `play_hangman()` תופעל, רק אם הקובץ מורץ ישירות, ולא מיובא כמודול.
    -  `play_hangman()`: קוראת לפונקציה כדי להתחיל את המשחק.
"""
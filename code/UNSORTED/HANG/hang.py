<תליין>:
=================
רמת קושי: 3
-----------------
משחק "תליין" הוא משחק מילים, שבו שחקן אחד (או המחשב) בוחר מילה, ושחקן אחר מנסה לנחש אותה לפי אותיות. על כל אות שגויה השחקן מקבל עונש, לרוב בצורת חלק מציור התליין. אם הציור מושלם, השחקן מפסיד.

כללי המשחק:
1. המחשב בוחר מילה אקראית מרשימה מוגדרת מראש.
2. השחקן רואה את המילה, מוצגת באמצעות קווים תחתונים (אחד לכל אות).
3. השחקן מנסה לנחש את המילה באמצעות הזנת אותיות.
4. אם האות שהוזנה קיימת במילה, היא מוצגת במקומותיה המתאימים.
5. אם האות שהוזנה אינה קיימת במילה, השחקן מקבל עונש.
6. המשחק נמשך עד שהשחקן מנחש את המילה או מגיע למגבלת העונשים.
-----------------
אלגוריתם:
1. אתחול מערך מילים שהמחשב יכול לבחור.
2. בחירת מילה אקראית מהמערך.
3. יצירת מחרוזת `GUESS$`, המורכבת מקווים תחתונים, באורך המילה שנבחרה.
4. אתחול מספר השגיאות ל-0.
5. התחלת לולאה "כל עוד המילה לא נוחשה ומספר השגיאות קטן מ-6":
  5.1 בקשת הזנת אות מהשחקן.
  5.2 אם האות שהוזנה קיימת במילה שנבחרה:
    5.2.1 עדכון המחרוזת `GUESS$`, הצגת האות בכל המיקומים שלה במילה.
    5.2.2 אם כל האותיות נוחשו, עבור לשלב 6.
  5.3 אחרת:
    5.3.1 הגדלת מספר השגיאות ב-1.
    5.3.2 הצגת ציור התליין המתאים למספר השגיאות הנוכחי.
  5.4 אם מספר השגיאות שווה ל-6, עבור לשלב 7.
6. הדפסת ההודעה "YOU GOT IT!", ולאחר מכן המילה שנבחרה, ומעבר לשלב 8.
7. הדפסת ההודעה "SORRY, YOU DIDN'T GET IT.", ולאחר מכן המילה שנבחרה, ומעבר לשלב 8.
8. סיום המשחק.
-----------------

"""
import random

# רשימת מילים למשחק
WORDS = ["python", "java", "kotlin", "javascript", "swift", "ruby", "csharp"]

def draw_hangman(errors):
    """
    פונקציה להצגת ציור התליין בהתאם למספר השגיאות.

    Args:
      errors: מספר השגיאות שבוצעו על ידי השחקן.
    """
    hangman_stages = [
        """
          ________
         |        |
         |
         |
         |
         |
        ---
        """,
        """
          ________
         |        |
         |        O
         |
         |
         |
        ---
        """,
        """
          ________
         |        |
         |        O
         |        |
         |
         |
        ---
        """,
        """
          ________
         |        |
         |        O
         |       /|
         |
         |
        ---
        """,
        """
          ________
         |        |
         |        O
         |       /|\\
         |
         |
        ---
        """,
        """
          ________
         |        |
         |        O
         |       /|\\
         |       /
         |
        ---
        """,
        """
          ________
         |        |
         |        O
         |       /|\\
         |       / \\
         |
        ---
        """
    ]
    print(hangman_stages[errors])

def play_hangman():
    """פונקציה ראשית למשחק התליין"""
    # בחירת מילה אקראית מהרשימה
    target_word = random.choice(WORDS).upper()
    # יצירת מחרוזת מקווים תחתונים, באורך המתאים למילה
    guess_string = "_" * len(target_word)
    # אתחול מונה השגיאות
    number_of_errors = 0

    # לולאת המשחק הראשית
    while number_of_errors < 6 and "_" in guess_string:
        print("מילה:", guess_string)
        # בקשת הזנת אות מהמשתמש
        user_letter = input("הכנס אות: ").upper()

        # בדיקה האם האות קיימת במילה
        if user_letter in target_word:
            # עדכון guess_string, הצגת האות במיקומיה
            new_guess_string = ""
            for i in range(len(target_word)):
                if target_word[i] == user_letter:
                    new_guess_string += user_letter
                else:
                    new_guess_string += guess_string[i]
            guess_string = new_guess_string

            # בדיקה האם המילה נוחשה
            if guess_string == target_word:
                print("מזל טוב! ניחשת את המילה:", target_word)
                return  # סיום המשחק

        else:
            # הגדלת מספר השגיאות וציור התליין
            number_of_errors += 1
            draw_hangman(number_of_errors)

    # בדיקה למקרה הפסד
    if number_of_errors == 6:
        print("צר לי, לא ניחשת את המילה. המילה שנבחרה:", target_word)

if __name__ == "__main__":
    play_hangman()
"""
ניתוח הקוד:

1.  **ייבוא מודול `random`:**
    -   `import random`: מייבא את מודול random לבחירת מילה אקראית.

2.  **רשימת המילים `WORDS`:**
    -   `WORDS = ["python", "java", "kotlin", "javascript", "swift", "ruby", "csharp"]`: רשימה המכילה את המילים, שמתוכן המחשב בוחר מילה למשחק.

3. **פונקציה `draw_hangman(errors)`:**
    -   מציגה את מצב ציור התליין בהתאם למספר השגיאות, תוך שימוש ב-ASCII-art.
    -  `hangman_stages` - מערך של מחרוזות, המייצגות את שלבי התליין.
    -  `print(hangman_stages[errors])` - מדפיסה למסך את המחרוזת המתאימה.

4.  **פונקציה `play_hangman()`:**
    -   **בחירת מילה:**
        -  `target_word = random.choice(WORDS).upper()`: בוחרת באופן אקראי מילה מהרשימה `WORDS` וממירה אותה לאותיות רישיות.
    -   **יצירת מחרוזת לניחוש:**
        -   `guess_string = "_" * len(target_word)`: יוצרת מחרוזת, המורכבת מקווים תחתונים, שאורכה תואם את אורך המילה שנבחרה.
    -   **אתחול מונה השגיאות:**
        -   `number_of_errors = 0`: מגדירה את מספר השגיאות ההתחלתי ל-0.
    -   **לולאת המשחק הראשית `while number_of_errors < 6 and "_" in guess_string:`:**
        -   הלולאה נמשכת כל עוד מספר השגיאות קטן מ-6 וקיימים קווים תחתונים במחרוזת `guess_string` (כלומר, כל עוד המילה לא נוחשה ומגבלת השגיאות לא מוצתה).
        -   `print("מילה:", guess_string)`: מדפיסה את המצב הנוכחי של המילה עם האותיות שנוחשו והקווים התחתונים.
        -   `user_letter = input("הכנס אות: ").upper()`: מבקשת מהמשתמש להזין אות וממירה אותה לאות רישית.
        -   **בדיקת קיום האות במילה:**
            -   `if user_letter in target_word:`: בודקת האם האות שהוזנה קיימת במילה שנבחרה.
            -   אם האות קיימת:
                -  `new_guess_string = ""`: יוצרת מחרוזת ריקה `new_guess_string` להרכבת הגרסה החדשה של המילה המנוחשת.
                -  הלולאה `for i in range(len(target_word))` עוברת על כל התווים במילה שנבחרה.
                -  אם האות הנוכחית במילה שנבחרה זהה לאות שהוזנה `user_letter`, אז מוסיפה אותה ל-`new_guess_string`, אחרת, מוסיפה את התו מהמיקום הנוכחי מ-`guess_string`.
                - `guess_string = new_guess_string`: מעדכנת את `guess_string` בגרסה החדשה עם האותיות שנוחשו.
                -  `if guess_string == target_word:`: בודקת האם המילה נוחשה.
                -  `print("מזל טוב! ניחשת את המילה:", target_word)`: מדפיסה הודעת ברכה ואת המילה שנבחרה.
                -  `return`: מסיימת את הפונקציה (את המשחק).
            -   **אם האות אינה קיימת במילה:**
                -   `number_of_errors += 1`: מגדילה את מונה השגיאות ב-1.
                -   `draw_hangman(number_of_errors)`: קוראת לפונקציה `draw_hangman` להצגת ציור התליין.
    -   **בדיקה למקרה הפסד:**
        -   `if number_of_errors == 6:`: בודקת האם מספר השגיאות שווה ל-6.
        -   `print("צר לי, לא ניחשת את המילה. המילה שנבחרה:", target_word)`: מדפיסה הודעה על הפסד ואת המילה שנבחרה.

5. **הפעלת המשחק:**
    -  `if __name__ == "__main__":`: בלוק זה מבטיח שהפונקציה `play_hangman()` תופעל רק אם הקובץ מורץ ישירות, ולא אם הוא מיובא כמודול.
    -  `play_hangman()`: קוראת לפונקציה כדי להתחיל את המשחק.
"""
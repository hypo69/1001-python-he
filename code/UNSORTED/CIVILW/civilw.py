CIVILW:
=================
רמת קושי: 7
-----------------
המשחק "מלחמת אזרחים" מהווה סימולציה של קרב בין שני צבאות: הקונפדרציה והאיחוד. השחקן שולט בצבא הקונפדרציה ומקבל החלטות לגבי סדר הכוחות שלו וסוגי ההתקפות (מתקפה חזיתית או תמרון איגוף). מטרת המשחק היא להביס את צבא האיחוד תוך מזעור אבדות הצד של השחקן. המשחק לוקח בחשבון גורמים אקראיים המשפיעים על תוצאת הקרב, מה שהופך כל עימות לייחודי.

חוקי המשחק:
1. השחקן שולט בצבא הקונפדרציה ועליו להביס את צבא האיחוד.
2. השחקן מזין את מספר החיילים למתקפה.
3. השחקן בוחר את סוג המתקפה: חזיתית (1) או איגוף (2).
4. בהתאם לבחירת השחקן וגורמים אקראיים, מחושבות האבדות לשני הצדדים.
5. לאחר כל קרב, המשחק מציג את סדר הכוחות הנוכחי של שני הצבאות.
6. המשחק מסתיים בניצחון של אחד הצדדים כאשר סדר הכוחות של היריב הופך להיות שווה ל-0 או פחות מכך.
-----------------
אלגוריתם:
1. קביעת סדר הכוחות ההתחלתי של צבא האיחוד (UnionForce) ל-1000 ושל צבא הקונפדרציה (ConfederateForce) ל-800.
2. התחלת לולאה "כל עוד לשני הצבאות סדר כוחות גדול מ-0":
    2.1. בקשת מספר החיילים שהשחקן מעוניין לשלוח למתקפה (AttackForce) מהשחקן.
        2.1.1. אם AttackForce גדול יותר מהכוחות הזמינים של הקונפדרציה (ConfederateForce), הצגת ההודעה "אין מספיק כוחות" וחזרה לתחילת שלב 2.1.
    2.2. בקשת סוג המתקפה: חזיתית (1) או איגוף (2) (AttackType) מהשחקן.
    2.3. חישוב אבדות הקונפדרציה (ConfederateLosses) באופן אקראי, על ידי הכפלת AttackForce במספר אקראי בין 0 ל-0.4 (עבור מתקפה חזיתית) או במספר אקראי בין 0 ל-0.2 (עבור תמרון איגוף).
        2.3.1. אם ConfederateLosses גדול יותר מ-AttackForce, הגדרת ConfederateLosses להיות שווה ל-AttackForce.
    2.4. חישוב אבדות האיחוד (UnionLosses) באופן אקראי, על ידי הכפלת AttackForce במספר אקראי בין 0 ל-0.3.
        2.4.1. אם AttackType שווה ל-2, הגדלת UnionLosses במספר אקראי בין 0 ל-100.
    2.5. עדכון סדר כוחות הצבאות:
        ConfederateForce = ConfederateForce - ConfederateLosses
        UnionForce = UnionForce - UnionLosses
    2.6. הצגת סדר הכוחות הנוכחי של שני הצבאות.
    2.7. בדיקת תנאי ניצחון:
        2.7.1. אם UnionForce קטן או שווה ל-0, הצגת ההודעה "הקונפדרציה ניצחה!" וסיום המשחק.
        2.7.2. אם ConfederateForce קטן או שווה ל-0, הצגת ההודעה "האיחוד ניצח!" וסיום המשחק.
3. סיום המשחק.
-----------------

"""
import random

# סדר כוחות התחלתי של הצבאות
unionForce = 1000  # צבא האיחוד
confederateForce = 800  # צבא הקונפדרציה

# לולאת המשחק הראשית
while unionForce > 0 and confederateForce > 0:
    # בקשת מספר חיילים למתקפה
    while True:
         try:
            attackForce = int(input("הכנס מספר חיילים למתקפה (קונפדרציה): "))
            if attackForce > confederateForce:
                print("אין מספיק כוחות! נסה שוב.")
            else:
                break
         except ValueError:
                print("אנא הכנס מספר שלם.")

    # בקשת סוג המתקפה
    while True:
        try:
             attackType = int(input("בחר סוג מתקפה (1 - חזיתית, 2 - איגוף): "))
             if attackType in [1, 2]:
                break
             else:
                 print("סוג מתקפה שגוי, נסה שוב")
        except ValueError:
            print("אנא הכנס מספר שלם 1 או 2.")

    # חישוב אבדות הקונפדרציה
    if attackType == 1:  # מתקפה חזיתית
        confederateLosses = int(attackForce * random.random() * 0.4)
    else:  # תמרון איגוף
        confederateLosses = int(attackForce * random.random() * 0.2)

    if confederateLosses > attackForce:
        confederateLosses = attackForce

    # חישוב אבדות האיחוד
    unionLosses = int(attackForce * random.random() * 0.3)
    if attackType == 2:
        unionLosses += random.randint(0, 100)

    # עדכון סדר כוחות הצבאות
    confederateForce -= confederateLosses
    unionForce -= unionLosses

    # הצגת סדר הכוחות הנוכחי של הצבאות
    print(f"קונפדרציה: {confederateForce} חיילים")
    print(f"איחוד: {unionForce} חיילים")

    # בדיקת תנאי ניצחון
    if unionForce <= 0:
        print("הקונפדרציה ניצחה!")
    elif confederateForce <= 0:
        print("האיחוד ניצח!")

"""
הסבר קוד:
1.  **ייבוא מודול `random`**:
    -   `import random`: מייבא את מודול `random`, המשמש ליצירת מספרים אקראיים בעת חישוב האבדות.
2.  **אתחול סדר כוחות הצבאות**:
    -   `unionForce = 1000`: מגדיר את סדר הכוחות ההתחלתי של צבא האיחוד ל-1000.
    -   `confederateForce = 800`: מגדיר את סדר הכוחות ההתחלתי של צבא הקונפדרציה ל-800.
3.  **לולאת המשחק הראשית `while unionForce > 0 and confederateForce > 0:`**:
    -   הלולאה ממשיכה כל עוד לשני הצבאות סדר כוחות גדול מ-0, כלומר עד שאחד הצדדים ינצח.
    - **קלט נתונים**:
        -   לולאת `while True: try: ... except ValueError:` מבטיחה קלט נתונים תקין. אם המשתמש מזין ערך שאינו מספר, התוכנית תציג הודעת שגיאה ותבקש קלט חוזר.
        - `attackForce = int(input("הכנס מספר חיילים למתקפה (קונפדרציה): "))`: מבקש מהשחקן את מספר החיילים למתקפה וממיר את הקלט למספר שלם.
        - בדיקת מספר החיילים שהוזן
        - `if attackForce > confederateForce: print("אין מספיק כוחות! נסה שוב.")`: בודק שמספר החיילים התוקפים אינו עולה על סדר הכוחות של צבא הקונפדרציה. אם כן, מוצגת שגיאה והלולאה ממשיכה.
        - `attackType = int(input("בחר סוג מתקפה (1 - חזיתית, 2 - איגוף): "))`: מבקש מהשחקן את סוג המתקפה וממיר את הקלט למספר שלם.
         - בדיקת סוג המתקפה שהוזן
        - `if attackType in [1, 2]: break`: בודק שסוג המתקפה שווה ל-1 או 2. אם כן, יוצא מהלולאה. אחרת, מציג שגיאה ומבקש קלט חוזר.
    -   **חישוב אבדות הקונפדרציה**:
        -   `if attackType == 1: ... else:`: בהתאם לסוג המתקפה, מחושב גודל האבדות.
        -   `confederateLosses = int(attackForce * random.random() * 0.4)`: עבור מתקפה חזיתית, אבדות הקונפדרציה הן מספר אקראי בין 0 ל-40% מכוח התקיפה.
        -  `confederateLosses = int(attackForce * random.random() * 0.2)`: עבור תמרון איגוף, אבדות הקונפדרציה הן מספר אקראי בין 0 ל-20% מכוח התקיפה.
        -  `if confederateLosses > attackForce: confederateLosses = attackForce`: מבטיח שהאבדות לא יעלו על מספר התוקפים
    -   **חישוב אבדות האיחוד**:
        -   `unionLosses = int(attackForce * random.random() * 0.3)`: אבדות האיחוד הן מספר אקראי בין 0 ל-30% מכוח התקיפה.
        -   `if attackType == 2: unionLosses += random.randint(0, 100)`: בעת תמרון איגוף, מתווסף לאבדות האיחוד מספר אקראי בין 0 ל-100.
    -   **עדכון סדר כוחות הצבאות**:
        -   `confederateForce -= confederateLosses`: מפחית את סדר הכוחות של צבא הקונפדרציה לפי האבדות.
        -   `unionForce -= unionLosses`: מפחית את סדר הכוחות של צבא האיחוד לפי האבדות.
    -   **הצגת סדר הכוחות הנוכחי של הצבאות**:
        -   `print(f"קונפדרציה: {confederateForce} חיילים")`: מציג את סדר הכוחות הנוכחי של צבא הקונפדרציה.
        -   `print(f"איחוד: {unionForce} חיילים")`: מציג את סדר הכוחות הנוכחי של צבא האיחוד.
    -   **בדיקת תנאי ניצחון**:
        -   `if unionForce <= 0: print("הקונפדרציה ניצחה!")`: אם סדר הכוחות של צבא האיחוד הפך קטן או שווה ל-0, הקונפדרציה מוכרזת כמנצחת.
        -   `elif confederateForce <= 0: print("האיחוד ניצח!")`: אם סדר הכוחות של צבא הקונפדרציה הפך קטן או שווה ל-0, האיחוד מוכרז כמנצח.
"""
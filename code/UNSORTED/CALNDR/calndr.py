CALNDR:
=================
רמת מורכבות: 4
-----------------
המשחק "לוח שנה" מציג על המסך לוח שנה לחודש, בהתבסס על מספר היום בשבוע שממנו החודש מתחיל, ועל מספר הימים בחודש. המשתמש מזין את שני הפרמטרים הללו, והתוכנית בונה את לוח השנה.

כללי המשחק:
1. המשתמש מזין את מספר היום בשבוע שממנו החודש מתחיל (מ-1 עד 7, כאשר 1 - יום ראשון, 7 - שבת).
2. המשתמש מזין את המספר הכולל של ימים בחודש (מ-28 עד 31).
3. התוכנית מציגה על המסך את לוח השנה בצורת טבלה, שבה ימי החודש מחולקים לפי שבועות.
-----------------
אלגוריתם:
1. בקש מהמשתמש להזין את מספר היום בשבוע שממנו החודש מתחיל (1-7).
2. בקש מהמשתמש להזין את המספר הכולל של ימים בחודש (28-31).
3. הצג שורה עם שמות ימי השבוע: "SUN MON TUE WED THU FRI SAT".
4. חשב את מספר הרווחים לפני המספר הראשון, בהתבסס על מספר היום בשבוע שהוזן.
5. הצג את המספר הדרוש של רווחים לפני המספר הראשון של החודש.
6. התחל לולאה מ-1 עד מספר הימים בחודש:
    6.1 הצג את היום הנוכחי בחודש.
    6.2 אם היום הנוכחי נופל בסוף השבוע (שבת), הצג מעבר לשורה חדשה.
    6.3 אם היום אינו היום האחרון בחודש, הוסף רווח אחרי המספר.
7. סיום פלט לוח השנה.
-----------------
בלוק-סכמה:
```mermaid
flowchart TD
    Start["התחלה"] --> InputStartDay["קלט: <code><b>startDay</b></code> (יום בשבוע של תחילת החודש)"]
    InputStartDay --> InputDaysInMonth["קלט: <code><b>daysInMonth</b></code> (מספר ימים בחודש)"]
    InputDaysInMonth --> OutputWeekdays["פלט: <b>SUN MON TUE WED THU FRI SAT</b>"]
    OutputWeekdays --> CalculateSpaces["<code><b>spaces = (startDay - 1) * 4</b></code> (חישוב מספר רווחים)"]
    CalculateSpaces --> OutputSpaces["פלט: <code><b>spaces</b></code> רווחים"]
    OutputSpaces --> LoopStart{"תחילת לולאה: <code><b>day = 1 to daysInMonth</b></code>"}
    LoopStart -- true --> OutputDay["פלט: <code><b>day</b></code>"]
    OutputDay --> CheckEndOfLine{"בדיקה: <code><b>(startDay + day - 2) mod 7 == 6</b></code> (סוף שבוע?)"}
    CheckEndOfLine -- true --> OutputNewline["פלט: שורה חדשה"]
    OutputNewline --> CheckLastDay{"בדיקה: <code><b>day != daysInMonth</b></code> (יום אחרון?)"}
    CheckEndOfLine -- false --> CheckLastDay
    CheckLastDay -- true --> OutputSpace["פלט: רווח"]
    OutputSpace --> LoopStart
    CheckLastDay -- false --> LoopStart
    LoopStart -- false --> End["סיום"]

```

מקרא:
    Start - התחלת התוכנית.
    InputStartDay - קלט מספר יום בשבוע, שממנו מתחיל החודש (startDay).
    InputDaysInMonth - קלט המספר הכולל של ימים בחודש (daysInMonth).
    OutputWeekdays - פלט הכותרת עם ימי השבוע.
    CalculateSpaces - חישוב מספר הרווחים לפני המספר הראשון של החודש.
    OutputSpaces - פלט מספר הרווחים המחושב.
    LoopStart - תחילת הלולאה לפלט כל יום בחודש.
    OutputDay - פלט היום הנוכחי של החודש.
    CheckEndOfLine - בדיקה האם היום הנוכחי הוא סוף שבוע (שבת).
    OutputNewline - פלט שורה חדשה, אם היום הנוכחי הוא שבת.
    CheckLastDay - בדיקה האם היום הנוכחי הוא היום האחרון בחודש.
    OutputSpace - פלט רווח אחרי המספר, אם היום אינו היום האחרון בחודש.
    End - סיום התוכנית.
"""


# מבקשים מהמשתמש את מספר היום בשבוע שממנו החודש מתחיל (1 - יום ראשון, 7 - שבת)
while True:
    try:
      startDay = int(input("הזן את יום השבוע, שממנו החודש מתחיל (1-ראשון, 7-שבת): "))
      if 1 <= startDay <= 7:
         break
      else:
        print("בבקשה, הזן מספר בין 1 ל-7")
    except ValueError:
        print("בבקשה, הזן מספר שלם.")

# מבקשים מהמשתמש את מספר הימים בחודש (28-31)
while True:
    try:
        daysInMonth = int(input("הזן את מספר הימים בחודש (28-31): "))
        if 28 <= daysInMonth <= 31:
            break
        else:
            print("בבקשה, הזן מספר בין 28 ל-31")
    except ValueError:
          print("בבקשה, הזן מספר שלם.")

# מציגים את הכותרת עם ימי השבוע
print(" SUN MON TUE WED THU FRI SAT")

# מחשבים את מספר הרווחים לפני המספר הראשון של החודש
spaces = (startDay - 1) * 4

# מציגים את הרווחים
print(" " * spaces, end="")

# מציגים את לוח השנה
for day in range(1, daysInMonth + 1):
    # מציגים את היום הנוכחי בחודש
    print(f"{day:3}", end=" ")

    # בודקים, אם היום הנוכחי הוא שבת, עוברים לשורה חדשה
    if (startDay + day - 2) % 7 == 6:
        print()
    elif day != daysInMonth: #אם זה לא היום האחרון מוסיפים רווח
      print(" ",end = "")
"""
הסבר על הקוד:
1.  **קלט נתונים**:
    -   `while True:` - לולאה אינסופית, המאפשרת לחזור על קלט הנתונים עד שיודפסו ערכים תקינים.
    -   `try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין ערך שאינו מספר שלם, תוצג הודעת שגיאה.
    -   `startDay = int(input(...))`: מבקש מהמשתמש את מספר היום בשבוע שממנו החודש מתחיל, וממיר אותו למספר שלם.
    -   `daysInMonth = int(input(...))`: מבקש מהמשתמש את מספר הימים בחודש וממיר אותו למספר שלם.
    - `if 1 <= startDay <= 7:`: בדיקת תקינות קלט הערך `startDay` (1-ראשון, 7-שבת).
    - `if 28 <= daysInMonth <= 31`: בדיקת תקינות קלט הערך `daysInMonth` (מספר הימים בחודש).
2.  **פלט כותרת**:
    -   `print(" SUN MON TUE WED THU FRI SAT")`: מציג את השורה עם שמות ימי השבוע.
3.  **חישוב הזחות (רווחים)**:
    -   `spaces = (startDay - 1) * 4`: מחשב את מספר הרווחים שיש להציג לפני היום הראשון של החודש. כל יום בשבוע תופס 4 תווים (3 ספרות + רווח).
4.  **פלט רווחים**:
    -   `print(" " * spaces, end="")`: מציג את מספר הרווחים הדרוש לפני תחילת לוח השנה, `end=""` מבטל את המעבר האוטומטי לשורה חדשה אחרי `print`.
5.  **לולאת פלט לוח השנה**:
    -   `for day in range(1, daysInMonth + 1):`: הלולאה עוברת על כל ימי החודש.
    -   `print(f"{day:3}", end=" ")`: מציג את היום הנוכחי בפורמט (3 תווים) ורווח אחריו.
    -   `if (startDay + day - 2) % 7 == 6:`: בדיקה האם היום הנוכחי הוא שבת. אם השארית מחלוקה ב-7 היא 6, אז זה שבת.
    -   `print()`: אם היום הנוכחי הוא שבת, עובר לשורה חדשה.
    -    `elif day != daysInMonth:`: אם היום אינו היום האחרון בחודש מוסיפים רווח.
"""
"""
DICE:
=================
רמת קושי: 2
-----------------
משחק "קוביות" הוא משחק פשוט שבו השחקן מטיל שתי קוביות משחק, והמחשב מציג את סכום הערכים שהתקבלו.

חוקי המשחק:
1. המחשב מדמה הטלה של שתי קוביות משחק בעלות שש פאות.
2. המחשב מציג על המסך את ערכי כל קוביה ואת סכומם.
-----------------
אלגוריתם:
1. ליצור מספר אקראי בין 1 ל-6 עבור הקוביה הראשונה.
2. ליצור מספר אקראי בין 1 ל-6 עבור הקוביה השנייה.
3. לחשב את סכום הערכים של שתי הקוביות.
4. להציג על המסך את ערך הקוביה הראשונה, את ערך הקוביה השנייה ואת סכומם.
-----------------

"""
import random

# ליצור מספר אקראי בין 1 ל-6 עבור הקוביה הראשונה
dice1 = random.randint(1, 6)
# ליצור מספר אקראי בין 1 ל-6 עבור הקוביה השנייה
dice2 = random.randint(1, 6)

# לחשב את סכום הערכים של שתי הקוביות
total = dice1 + dice2

# להציג את התוצאות
print(f"קוביה 1: {dice1}")
print(f"קוביה 2: {dice2}")
print(f"סכום: {total}")

"""
הסבר הקוד:
1. **ייבוא המודול `random`**:
   - `import random`: מייבא את המודול `random`, המשמש ליצירת מספרים אקראיים.
2. **יצירת (הדמיית) הטלות הקוביות**:
   - `dice1 = random.randint(1, 6)`: יוצר מספר שלם אקראי בין 1 ל-6 (הדמיית הטלת קוביה בעלת שש פאות) ושומר אותו במשתנה `dice1`.
   - `dice2 = random.randint(1, 6)`: יוצר מספר שלם אקראי בין 1 ל-6 ושומר אותו במשתנה `dice2`.
3. **חישוב הסכום**:
   - `total = dice1 + dice2`: מסכם את הערכים של `dice1` ו-`dice2`, ושומר את התוצאה במשתנה `total`.
4. **הצגת התוצאות**:
   - `print(f"קוביה 1: {dice1}")`: מציג על המסך את ערך הקוביה הראשונה.
   - `print(f"קוביה 2: {dice2}")`: מציג על המסך את ערך הקוביה השנייה.
   - `print(f"סכום: {total}")`: מציג על המסך את סכום הערכים של שתי הקוביות.
"""
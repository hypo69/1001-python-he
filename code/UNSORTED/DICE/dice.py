"""
קוביות:
=================
מורכבות: 2
-----------------
המשחק "קוביות" הוא משחק פשוט שבו השחקן מטיל שתי קוביות משחק, והמחשב מציג את סכום הערכים שהתקבלו.

כללי המשחק:
1.  המחשב מדמה הטלה של שתי קוביות משחק בעלות שש פאות.
2.  המחשב מציג על המסך את הערכים של כל קובייה ואת סכומם.
-----------------
אלגוריתם:
1.  יש ליצור מספר אקראי בטווח 1 עד 6 עבור הקובייה הראשונה.
2.  יש ליצור מספר אקראי בטווח 1 עד 6 עבור הקובייה השנייה.
3.  יש לחשב את סכום הערכים של שתי הקוביות.
4.  יש להציג על המסך את הערך של הקובייה הראשונה, את הערך של הקובייה השנייה ואת סכומם.
-----------------

"""
import random

# יוצר מספר אקראי בטווח 1 עד 6 עבור הקובייה הראשונה
dice1 = random.randint(1, 6)
# יוצר מספר אקראי בטווח 1 עד 6 עבור הקובייה השנייה
dice2 = random.randint(1, 6)

# מחשב את סכום הערכים של שתי הקוביות
total = dice1 + dice2

# מציג את התוצאות
print(f"Кость 1: {dice1}")
print(f"Кость 2: {dice2}")
print(f"Сумма: {total}")

"""
הסבר הקוד:
1.  **ייבוא מודול `random`**:
   - `import random`: מייבא את מודול `random`, המשמש ליצירת מספרים אקראיים.
2.  **יצירת הטלות הקוביות**:
   -   `dice1 = random.randint(1, 6)`: יוצר מספר שלם אקראי בטווח 1 עד 6 (הדמיה של הטלת קובייה בעלת שש פאות) ושומר אותו במשתנה `dice1`.
   -   `dice2 = random.randint(1, 6)`: יוצר מספר שלם אקראי בטווח 1 עד 6 ושומר אותו במשתנה `dice2`.
3.  **חישוב הסכום**:
   -   `total = dice1 + dice2`: מחבר את הערכים של `dice1` ו-`dice2`, ושומר את התוצאה במשתנה `total`.
4.  **הצגת התוצאות**:
   -   `print(f"Кость 1: {dice1}")`: מציג על המסך את הערך של הקובייה הראשונה.
   -   `print(f"Кость 2: {dice2}")`: מציג על המסך את הערך של הקובייה השנייה.
   -   `print(f"Сумма: {total}")`: מציג על המסך את סכום הערכים של שתי הקוביות.
"""
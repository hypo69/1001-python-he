CUBE
=================

רמת קושי: 5
-----------------

המשחק "קובייה" הוא משחק פאזל שבו השחקן צריך להרכיב קובייה על ידי הזזת הפאות שלה. הקובייה מיוצגת כרשת 3x3, כאשר כל תא מייצג פאה של הקובייה. השחקן יכול להזיז את פאות הקובייה למעלה, למטה, שמאלה וימינה. מטרת המשחק היא להרכיב את הקובייה, כלומר לסדר את הפאות בסדר הנכון.
כללי המשחק:
1.  הקובייה מיוצגת כרשת 3x3.
2.  השחקן יכול להזיז את פאות הקובייה על ידי קלט פקודות: U (למעלה), D (למטה), L (שמאלה), R (ימינה).
3.  מטרת המשחק היא להרכיב את הקובייה, כלומר לסדר את הפאות בסדר הנכון.
4.  המיקום ההתחלתי של הקובייה נוצר באקראי.
5.  המשחק מסתיים כאשר הקובייה מורכבת, כלומר כאשר כל הפאות מסודרות בסדר הנכון.
-----------------
אלגוריתם:
1.  לאתחל את הקובייה בערכים אקראיים מ-1 עד 9 בצורת רשת 3x3.
2.  להציג את הקובייה על המסך.
3.  להתחיל לולאת משחק:
    3.1. לבקש מהשחקן קלט פקודה להזזת פאת הקובייה (U, D, L, R).
    3.2. לבצע את הזזת הפאה בהתאם לפקודה:
        -   אם הפקודה היא "U", להזיז את כל השורות למעלה.
        -   אם הפקודה היא "D", להזיז את כל השורות למטה.
        -   אם הפקודה היא "L", להזיז את כל העמודות שמאלה.
        -   אם הפקודה היא "R", להזיז את כל העמודות ימינה.
    3.3. להציג את הקובייה על המסך.
    3.4. לבדוק האם הקובייה הורכבה.
    3.5. אם הקובייה הורכבה, להציג הודעת ניצחון ולסיים את המשחק.
    3.6. אם הקובייה לא הורכבה, לחזור לשלב 3.1.
-----------------

"""
import random

def generate_cube():
    """מייצר קובייה אקראית 3x3."""
    numbers = list(range(1, 10))
    random.shuffle(numbers)
    cube = [numbers[i:i+3] for i in range(0, 9, 3)]
    return cube

def display_cube(cube):
    """מציג את הקובייה על המסך."""
    for row in cube:
        print('  '.join(map(str, row)))

def move_cube(cube, move):
    """מזיז את פאות הקובייה."""
    if move == 'U':
        # מזיזים את כל השורות למעלה
        temp = cube[0]
        for i in range(2):
            cube[i] = cube[i+1]
        cube[2] = temp
    elif move == 'D':
        # מזיזים את כל השורות למטה
        temp = cube[2]
        for i in range(2, 0, -1):
            cube[i] = cube[i-1]
        cube[0] = temp
    elif move == 'L':
        # מזיזים את כל העמודות שמאלה
        temp = [row[0] for row in cube]
        for i in range(3):
            for j in range(2):
                cube[i][j] = cube[i][j+1]
            cube[i][2] = temp[i]
    elif move == 'R':
         # מזיזים את כל העמודות ימינה
        temp = [row[2] for row in cube]
        for i in range(3):
            for j in range(2, 0, -1):
                cube[i][j] = cube[i][j-1]
            cube[i][0] = temp[i]
    return cube

def is_solved(cube):
  """בודק האם הקובייה הורכבה."""
  # בדיקה שכל המספרים מסודרים לפי הסדר מ-1 עד 9
  expected = [1, 2, 3, 4, 5, 6, 7, 8, 9]
  actual = []
  for row in cube:
    actual.extend(row)
  return actual == expected

# אתחול הקובייה
cube = generate_cube()
# הצגת הקובייה על המסך
display_cube(cube)

# לולאת המשחק הראשית
while True:
    # מבקשים קלט מהלך מהמשתמש
    move = input("Введите ход (U, D, L, R): ").upper()
    # בודקים תקינות המהלך שהוזן
    if move not in ['U', 'D', 'L', 'R']:
        print("Неверный ход. Попробуйте еще раз.")
        continue
    # מזיזים את פאות הקובייה
    cube = move_cube(cube, move)
    # הצגת הקובייה על המסך
    display_cube(cube)
    # בודקים האם הקובייה הורכבה
    if is_solved(cube):
        print("Поздравляю! Вы собрали куб!")
        break # מסיימים את הלולאה אם הקובייה הורכבה

"""
הסבר הקוד:
1.  **ייבוא המודול `random`**:
   -  `import random`: מייבא את המודול `random`, המשמש לערבוב רכיבי הקובייה.
2.  **פונקציה `generate_cube()`**:
    -   `numbers = list(range(1, 10))`: יוצר רשימת מספרים מ-1 עד 9, שתשמש למילוי הקובייה.
    -   `random.shuffle(numbers)`: מערבב את רשימת המספרים בסדר אקראי.
    -   `cube = [numbers[i:i+3] for i in range(0, 9, 3)]`: יוצר רשת 3x3 (קובייה) מהמספרים המעורבבים.
    -   מחזיר את הקובייה שנוצרה.
3.  **פונקציה `display_cube(cube)`**:
    -   מציג את מצב הקובייה הנוכחי על המסך, תוך עיצובו כרשת.
4.  **פונקציה `move_cube(cube, move)`**:
    -   מקבלת את הקובייה ואת המהלך (`move`) כארגומנטים.
    -   מבצעת את הזזת פאות הקובייה בהתאם לפקודה שהוזנה.
        -   **`if move == 'U':`**: מזיזה את כל שורות הקובייה למעלה.
        -   **`elif move == 'D':`**: מזיזה את כל שורות הקובייה למטה.
        -   **`elif move == 'L':`**: מזיזה את כל עמודות הקובייה שמאלה.
        -   **`elif move == 'R':`**: מזיזה את כל עמודות הקובייה ימינה.
    -   מחזירה את הקובייה לאחר ההזזה.
5.  **פונקציה `is_solved(cube)`**:
    -   בודקת האם הקובייה הורכבה, על ידי השוואת רצף המספרים הנוכחי לרצף הצפוי (1-9).
    -   `expected = [1, 2, 3, 4, 5, 6, 7, 8, 9]`: יוצר רשימה עם רצף המספרים הצפוי.
    -   `actual = []`: מאתחל רשימה ריקה לשמירת רצף המספרים הנוכחי מהקובייה.
    -   `for row in cube: actual.extend(row)`: מוסיף לרשימה `actual` את כל המספרים מהקובייה.
    -   `return actual == expected`: מחזיר `True` אם הקובייה הורכבה, ו-`False` אחרת.
6.  **חלק התוכנית הראשי**:
    -   `cube = generate_cube()`: יוצר את הקובייה ההתחלתית.
    -   `display_cube(cube)`: מציג את הקובייה ההתחלתית על המסך.
    -   **לולאת המשחק `while True:`**:
        -   `move = input("Введите ход (U, D, L, R): ").upper()`: מבקש מהמשתמש קלט מהלך וממיר אותו לאותיות גדולות.
        -   **בדיקת תקינות המהלך**:
            - `if move not in ['U', 'D', 'L', 'R']:`: בודק האם המהלך קביל (U, D, L או R).
            - `print("Неверный ход. Попробуйте еще раз.")`: אם המהלך אינו קביל, מציג הודעת שגיאה ועובר לאיטרציה הבאה של הלולאה.
            - `continue` - עובר לאיטרציה הבאה של הלולאה.
        -   `cube = move_cube(cube, move)`: מזיז את פאות הקובייה בהתאם למהלך שהוזן.
        -   `display_cube(cube)`: מציג את הקובייה המעודכנת על המסך.
        -   **בדיקה האם הקובייה הורכבה**:
            -   `if is_solved(cube):`: בודק האם הקובייה הורכבה.
            -   `print("Поздравляю! Вы собрали куб!")`: מציג הודעת ניצחון.
            -   `break`: מסיים את לולאת המשחק.
"""
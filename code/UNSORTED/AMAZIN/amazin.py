# **Amazin** (Maze Generator)

#### תיאור
המשחק הינו מחולל מבוכים, היוצר בכל פעם מבוך ייחודי עם מסלול נכון יחיד. השחקן מגדיר את מימדי המבוך, והתוכנית בונה אותו בהתאם לפרמטרים שצוינו.

- **מאפיינים:**
  - השחקן מזין את רוחב וגובה המבוך.
  - התוכנית מבטיחה קיום של מסלול יחיד דרך המבוך.
  - המבוך מוצג על המסך.

---

### הנחיה צעד-אחר-צעד ליישום:

#### 1. **אתחול המשחק**
   - הצע לשחקן להזין את מימדי המבוך (רוחב וגובה).
   - ודא שהמימדים שהוזנו תקינים (לדוגמה, גדולים מאחד).

#### 2. **לוגיקת הליבה של יצירת המבוך**
   - צור מטריצה בגודל הנתון, המייצגת רשת של תאים.
   - השתמש באלגוריתם ליצירת מבוכים, לדוגמה, *אלגוריתם חיפוש לעומק רקורסיבי (DFS)*:
     - התחל מתא התחלתי אקראי.
     - התקדם לתאים סמוכים, תוך הסרת הקירות בין התא הנוכחי לתא הבא.
     - אם כל השכנים כבר בוקרו, חזור לתא הקודם והמשך.
     - סיים את התהליך כאשר כל התאים בוקרו.
   - הבטח קיום של מסלול נכון יחיד דרך המבוך.

#### 3. **הצגת המבוך**
   - השתמש בתווים להצגה:
     - `+`, `-`, `|` עבור קירות.
     - רווחים עבור מעברים.
   - הצג את המבוך המוגמר בצורת טקסט.

#### 4. **פונקציות נוספות**
   - אפשרות להגדיר גודל מוגדר מראש (לדוגמה, 10x10), אם המשתמש הזין נתונים לא תקינים.
   - אזהרה לגבי גדלי מבוך גדולים מדי למניעת עומס יתר על הזיכרון.

---

### דוגמה להפעלת התוכנית

1. **תחילת ההפעלה**:
   ```
   הזן את רוחב וגובה המבוך:
   > 10 8
   ```

2. **פלט המבוך**:
   ```
   +--+--+--+--+--+--+--+--+--+--+
   |        |        |           |
   +  +--+  +  +--+  +  +--+--+  +
   |     |     |     |        |  |
   +--+  +  +--+  +  +  +--+  +  +
   |     |        |     |     |  |
   +--+--+--+--+--+--+--+--+--+--+
   ```

3. **יציאה מהתוכנית**:
   ```
   האם ליצור מבוך חדש? (כן/לא):
   > לא
   להתראות!
   ```

---

"""
import random

def create_maze(width, height):
    """יוצר מבוך בגדלים נתונים."""
    # אתחול מטריצת המבוך
    maze = [['+' for _ in range(2 * width + 1)] for _ in range(2 * height + 1)]

    # פונקציה לבדיקת תקינות קואורדינטות של תא
    def is_valid(x, y):
        return 0 <= x < height and 0 <= y < width

    # פונקציה למעבר רקורסיבי על המבוך
    def carve_path(x, y):
        # רשימת כיוונים אפשריים למעבר
        directions = [(0, 1), (0, -1), (1, 0), (-1, 0)]
        random.shuffle(directions) # ערבוב אקראי של כיוונים

        for dx, dy in directions:
            nx, ny = x + dx, y + dy # מחשבים את קואורדינטות התא הבא

            # בדיקה האם התא השכן בטווח המבוך ועדיין לא בוקר
            if is_valid(nx, ny) and maze[2 * nx + 1][2 * ny + 1] == '+':
                # מסירים את הקיר בין התא הנוכחי לתא הבא
                maze[2 * x + 1 + dx][2 * y + 1 + dy] = ' '
                maze[2 * nx + 1][2 * ny + 1] = ' ' # מסמנים את התא הבא ככזה שבוקר
                carve_path(nx, ny) # קריאה רקורסיבית להמשך המעבר

    # מתחילים את יצירת המבוך מתא אקראי
    start_x = random.randint(0, height - 1)
    start_y = random.randint(0, width - 1)
    maze[2 * start_x + 1][2 * start_y + 1] = ' '
    carve_path(start_x, start_y)
    return maze


def print_maze(maze):
    """מדפיס את המבוך למסך."""
    for row in maze:
        print(''.join(row))

def main():
    """פונקציית המשחק הראשית."""
    while True:
        try:
            width = int(input("הזן את רוחב המבוך: "))
            height = int(input("הזן את גובה המבוך: "))

            # בדיקת תקינות גדלים
            if width <= 0 or height <= 0:
                print("גדלי המבוך חייבים להיות גדולים מ-0.")
                continue
            if width > 50 or height > 50:
                print("גדלי מבוך גדולים מדי. מומלץ להשתמש בערכים קטנים מ-50.")
                continue

            maze = create_maze(width, height) # יצירת המבוך
            print_maze(maze) # הדפסת המבוך למסך

        except ValueError:
            print("פורמט קלט שגוי. אנא הזן מספרים שלמים.")
            continue

        # הצעה ליצור מבוך נוסף
        new_maze = input("האם ליצור מבוך חדש? (כן/לא): ").lower()
        if new_maze != 'כן':
            print("להתראות!")
            break

if __name__ == "__main__":
    main()
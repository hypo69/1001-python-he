**תותחן (GUNNER):**
=================
מורכבות: 7
-----------------
המשחק "תותחן" הוא משחק בו השחקן מנסה לפגוע במטרה הנמצאת במרחק מסוים, באמצעות ירי מתותח בזווית נתונה ועם מהירות התחלתית נתונה. המשחק לוקח בחשבון את כוח הכבידה.
מטרת המשחק היא לבחור את הזווית והמהירות הנכונות לירי, כך שהפגז יגיע למטרה.
חוקי המשחק:
1. המחשב מגריל מרחק אקראי למטרה.
2. השחקן מזין את זווית הירי (במעלות) ואת המהירות ההתחלתית של הפגז.
3. המחשב מחשב את מסלול הפגז, תוך לקיחת כוח הכבידה בחשבון.
4. אם הפגז פוגע במטרה (המרחק למטרה נמצא בטווח הסטייה המותרת), השחקן מנצח.
5. אם הפגז אינו פוגע במטרה, השחקן יכול לנסות שוב, עד שינצל את כל הניסיונות או יפגע במטרה.
-----------------
אלגוריתם:
1. קבע את המספר המרבי של ניסיונות (לדוגמה, 10).
2. הגרל מרחק אקראי למטרה בטווח שבין 100 ל-1000.
3. התחל לולאה "כל עוד לא נגמרו הניסיונות":
    3.1 בקש מהשחקן להזין את זווית הירי (במעלות) ואת המהירות ההתחלתית.
    3.2 המר את הזווית ממעלות לרדיאנים.
    3.3 חשב את מרחק מעוף הפגז לפי הנוסחה:
        מרחק = (מהירות_התחלתית^2 * sin(2 * זווית)) / כוח_כבידה
       כאשר כוח_כבידה = 32.2
    3.4 אם מרחק מעוף הפגז נמצא בטווח של +/- 10% ממרחק המטרה, הצג הודעה על פגיעה וסיים את המשחק.
    3.5 אם מרחק מעוף הפגז קטן ממרחק המטרה, הצג הודעה על כך שהמרחק אינו מספיק.
    3.6 אם מרחק מעוף הפגז גדול ממרחק המטרה, הצג הודעה על ירי ארוך מדי.
4. אם כל הניסיונות מוצו, הצג הודעה על הפסד.
-----------------
דיאגרמת זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InitializeVariables["<p align='left'>אתחול משתנים:
    <code><b>
    maxAttempts = 10
    gravity = 32.2
    targetDistance = random(100, 1000)
    attempt = 0
    </b></code></p>"]
    InitializeVariables --> LoopStart{"תחילת לולאה: כל עוד לא נגמרו הניסיונות"}
    LoopStart -- כן --> IncreaseAttempts["<code><b>attempt = attempt + 1</b></code>"]
    IncreaseAttempts --> InputAngleSpeed["<p align='left'>קלט זווית ירי (במעלות)
    <code><b>angle</b></code> ומהירות התחלתית
    <code><b>initialSpeed</b></code></p>"]
    InputAngleSpeed --> ConvertAngle["<code><b>angle = angle * pi / 180</b></code>"]
    ConvertAngle --> CalculateDistance["<p align='left'>חישוב מרחק מעוף הפגז
    <code><b>distance = (initialSpeed^2 * sin(2 * angle)) / gravity</b></code></p>"]
    CalculateDistance --> CheckHit{"<p align='left'>בדיקה:
    <code><b>targetDistance - (targetDistance * 0.10)  &lt;= distance &lt;=  targetDistance + (targetDistance * 0.10) </b></code></p>"}
    CheckHit -- כן --> OutputWin["פלט הודעה: <b>YOU HIT THE TARGET!</b>"]
    OutputWin --> End["סוף"]
    CheckHit -- לא --> CheckShort{"בדיקה: <code><b>distance &lt; targetDistance</b></code>?"}
    CheckShort -- כן --> OutputShort["פלט הודעה: <b>TOO SHORT</b>"]
    OutputShort --> LoopStart
    CheckShort -- לא --> OutputLong["פלט הודעה: <b>TOO LONG</b>"]
    OutputLong --> LoopStart
    LoopStart -- לא --> OutputLose["פלט הודעה: <b>YOU LOSE!</b>"]
    OutputLose --> End
```
מקרא:
    Start - התחלת התוכנית.
    InitializeVariables - אתחול משתנים: maxAttempts (המספר המרבי של ניסיונות) מוגדר ל-10, gravity (כוח הכבידה) מוגדר ל-32.2, targetDistance (מרחק למטרה) מוגרל באופן אקראי בין 100 ל-1000, attempt (הניסיון הנוכחי) מוגדר ל-0.
    LoopStart - תחילת הלולאה, הנמשכת כל עוד לא נגמרו הניסיונות.
    IncreaseAttempts - הגדלת מונה הניסיונות ב-1.
    InputAngleSpeed - בקשת קלט מהמשתמש עבור זווית הירי (במעלות) והמהירות ההתחלתית.
    ConvertAngle - המרת הזווית ממעלות לרדיאנים.
    CalculateDistance - חישוב מרחק מעוף הפגז.
    CheckHit - בדיקה האם הפגז פגע במטרה (המרחק נמצא בטווח +/- 10% ממרחק המטרה).
    OutputWin - פלט הודעה על ניצחון אם הפגז פגע במטרה.
    End - סוף התוכנית.
    CheckShort - בדיקה האם מרחק מעוף הפגז קטן ממרחק המטרה.
    OutputShort - פלט הודעה "TOO SHORT" אם המרחק קטן מהמטרה.
    OutputLong - פלט הודעה "TOO LONG" אם המרחק גדול מהמטרה.
    OutputLose - פלט הודעה "YOU LOSE!" אם כל הניסיונות מוצו.
"""
import random
import math

# אתחול פרמטרי המשחק
maxAttempts = 10  # המספר המרבי של ניסיונות
gravity = 32.2  # כוח הכבידה
targetDistance = random.randint(100, 1000)  # מרחק אקראי למטרה
attempt = 0  # מונה ניסיונות
accuracy = 0.1  # סטייה מותרת של 10%

print(f"Расстояние до цели: {targetDistance}")

# לולאת המשחק הראשית
while attempt < maxAttempts:
    attempt += 1
    print(f"Попытка {attempt} из {maxAttempts}")

    # בקשת קלט נתונים מהמשתמש
    while True: # לולאה אינסופית לבדיקת קלט תקין.
        try: # מטפל בשגיאות אפשריות של קלט שאינו מספרי.
            angle = float(input("Введите угол выстрела (в градусах): ")) # מבקש מהמשתמש את זווית הירי.
            initialSpeed = float(input("Введите начальную скорость: ")) # מבקש מהמשתמש את מהירות הירי ההתחלתית.
            break # יציאה מלולאת הקלט אם הנתונים תקינים.
        except ValueError:
             print("Пожалуйста, введите числовое значение")


    # המרת הזווית לרדיאנים
    angle = math.radians(angle)

    # חישוב מרחק מעוף הפגז
    distance = (initialSpeed ** 2 * math.sin(2 * angle)) / gravity

    # בדיקה האם הפגז פגע במטרה
    if targetDistance * (1- accuracy) <= distance <= targetDistance * (1 + accuracy): # בודק האם מרחק מעוף הפגז נמצא בטווח +/- 10% ממרחק המטרה.
        print("ВЫ ПОПАЛИ В ЦЕЛЬ!") # פלט הודעה על ניצחון אם הפגז פגע במטרה.
        break # סיום הלולאה אם הפגז פגע במטרה.
    elif distance < targetDistance: # בדיקה אם הפגז לא הגיע למטרה.
        print("Слишком коротко!") # פלט הודעה על כך שהירי היה קצר מדי.
    else: # אם הפגז עבר את המטרה.
        print("Слишком далеко!") # פלט הודעה על כך שהירי היה ארוך מדי.

# פלט הודעה על הפסד אם כל הניסיונות מוצו.
if attempt == maxAttempts: # בודק אם כל הניסיונות מוצו.
    print("Вы проиграли!") # מציג הודעה על הפסד.
"""
הסבר הקוד:
1. **ייבוא מודולים:**
   - `import random`: מייבא את המודול `random` לצורך הגרלת מספרים אקראיים.
   - `import math`: מייבא את המודול `math` לצורך פעולות מתמטיות, כגון `sin` ו-`radians`.

2. **אתחול פרמטרי המשחק:**
   - `maxAttempts = 10`: מגדיר את המספר המרבי של ניסיונות ל-10.
   - `gravity = 32.2`: מגדיר את ערך כוח הכבידה.
   - `targetDistance = random.randint(100, 1000)`: מגריל מרחק אקראי למטרה בטווח שבין 100 ל-1000.
   - `attempt = 0`: מאתחל את מונה הניסיונות.
   - `accuracy = 0.1`: מגדיר סטייה מותרת של 10% לפגיעה במטרה.

3. **לולאת המשחק הראשית `while attempt < maxAttempts:`:**
   - הלולאה נמשכת כל עוד מספר הניסיונות `attempt` קטן מהמספר המרבי של ניסיונות `maxAttempts`.
   - `attempt += 1`: מגדיל את מונה הניסיונות ב-1.
   - `print(f"Попытка {attempt} из {maxAttempts}")`: מציג את מספר הניסיון הנוכחי.
   - **קלט נתונים מהמשתמש:**
     - `while True:`: לולאה אינסופית לבדיקת קלט תקין.
     - `try...except ValueError`: מטפל בשגיאות אפשריות של קלט שאינו מספרי.
     - `angle = float(input("Введите угол выстрела (в градусах): "))`: מבקש מהמשתמש את זווית הירי.
     - `initialSpeed = float(input("Введите начальную скорость: "))`: מבקש מהמשתמש את מהירות הירי ההתחלתית.
     - `break`: יציאה מלולאת הקלט אם הנתונים תקינים.
   - **המרת זווית:**
     - `angle = math.radians(angle)`: ממיר את הזווית ממעלות לרדיאנים.
   - **חישוב מרחק מעוף הפגז:**
     - `distance = (initialSpeed ** 2 * math.sin(2 * angle)) / gravity`: מחשב את מרחק מעוף הפגז, תוך שימוש בנוסחה מפיזיקה.
   - **בדיקת פגיעה:**
     - `if targetDistance * (1 - accuracy) <= distance <= targetDistance * (1 + accuracy):`: בודק האם מרחק מעוף הפגז נמצא בטווח +/- 10% ממרחק המטרה.
     - `print("ВЫ ПОПАЛИ В ЦЕЛЬ!")`: מציג הודעה על ניצחון אם הפגז פגע במטרה.
     - `break`: מסיים את הלולאה אם הפגז פגע במטרה.
     - `elif distance < targetDistance:`: בודק אם הפגז לא הגיע למטרה.
     - `print("Слишком коротко!")`: מציג הודעה על כך שהירי היה קצר מדי.
     - `else:`: אם הפגז עבר את המטרה.
     - `print("Слишком далеко!")`: מציג הודעה על כך שהירי היה ארוך מדי.

4. **בדיקת הפסד:**
   - `if attempt == maxAttempts:`: בודק אם כל הניסיונות מוצו.
   - `print("Вы проиграли!")`: מציג הודעה על הפסד.
"""
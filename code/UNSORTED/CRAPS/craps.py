קְרֶפְּס:
=================
רמת קושי: 7
-----------------
המשחק "קְרֶפְּס" הוא משחק קוביות מזל, שבו השחקן מהמר על התוצאה של הטלת שתי קוביות משחק. בסיבוב הראשון, השחקן זוכה אם סכום הנקודות על הקוביות שווה ל-7 או ל-11, ומפסיד אם הסכום שווה ל-2, 3 או 12. אם סכום הנקודות שווה ל-4, 5, 6, 8, 9 או 10, אז זה הופך להיות "מספר היעד" של השחקן, והוא ממשיך להטיל את הקוביות עד שיצא מספר זה שוב (במקרה זה השחקן זוכה) או עד שיצא 7 (במקרה זה השחקן מפסיד).

כללי המשחק:
1. בתחילת המשחק, השחקן מטיל שתי קוביות.
2. אם סכום הנקודות על הקוביות שווה ל-7 או ל-11, השחקן זוכה.
3. אם סכום הנקודות שווה ל-2, 3 או 12, השחקן מפסיד.
4. אם סכום הנקודות שווה ל-4, 5, 6, 8, 9 או 10, אז מספר זה הופך להיות "מספר היעד" (point).
5. לאחר קביעת "מספר היעד", השחקן ממשיך להטיל קוביות עד אשר:
   -  ייצא "מספר היעד", ואז השחקן זוכה.
   -  ייצא 7, ואז השחקן מפסיד.
-----------------
אלגוריתם:
1. לייצר ערכים אקראיים לשתי קוביות (מ-1 עד 6).
2. לחשב את סכום הערכים שיצאו.
3. אם הסכום שווה ל-7 או 11, להציג הודעת זכייה ולעבור לשלב 7.
4. אם הסכום שווה ל-2, 3 או 12, להציג הודעת הפסד ולעבור לשלב 7.
5. לשמור את הסכום כ"מספר יעד" (point).
6.  להתחיל לולאה:
    6.1 לייצר ערכים אקראיים לשתי קוביות.
    6.2 לחשב את סכום הערכים שיצאו.
    6.3 אם הסכום שווה ל"מספר היעד", להציג הודעת זכייה ולעבור לשלב 7.
    6.4 אם הסכום שווה ל-7, להציג הודעת הפסד ולעבור לשלב 7.
    6.5 אחרת, לחזור על הלולאה החל משלב 6.
7. סוף המשחק.
-----------------

```python
import random

def roll_dice():
    """מטילה שתי קוביות משחק ומחזירה את ערכיהן."""
    dice1 = random.randint(1, 6)
    dice2 = random.randint(1, 6)
    return dice1, dice2

def calculate_sum(dice1, dice2):
  """מחשבת את סכום הערכים של שתי הקוביות."""
  return dice1 + dice2

def play_craps():
    """מפעילה את המשחק קְרֶפְּס."""
    # הטלת קוביות ראשונה
    dice1, dice2 = roll_dice()
    sum_of_dice = calculate_sum(dice1,dice2)
    print(f"Выпало: {dice1} + {dice2} = {sum_of_dice}") # NOTE: Keeping the Russian prompt part in print as requested by rule 4.

    # בדיקת זכייה או הפסד לאחר ההטלה הראשונה
    if sum_of_dice == 7 or sum_of_dice == 11:
        print("YOU WIN!")
        return
    elif sum_of_dice == 2 or sum_of_dice == 3 or sum_of_dice == 12:
        print("YOU LOSE!")
        return

    # קביעת מספר היעד
    point = sum_of_dice
    print(f"Ваше целевое число: {point}") # NOTE: Keeping the Russian prompt part in print as requested by rule 4.

    # ממשיכים להטיל קוביות עד זכייה או הפסד
    while True:
      dice1, dice2 = roll_dice()
      sum_of_dice = calculate_sum(dice1,dice2)
      print(f"Выпало: {dice1} + {dice2} = {sum_of_dice}") # NOTE: Keeping the Russian prompt part in print as requested by rule 4.
      if sum_of_dice == point:
          print("YOU WIN!")
          return
      elif sum_of_dice == 7:
          print("YOU LOSE!")
          return

if __name__ == "__main__":
    play_craps()
```
```
הסבר הקוד:
1.  **ייבוא המודול `random`**:
   -  `import random`: מייבא את המודול `random`, המשמש לייצור מספרים אקראיים (ערכים עבור קוביות המשחק).
2. **הפונקציה `roll_dice()`**:
   -  `def roll_dice():`: הפונקציה מדמה הטלה של שתי קוביות משחק.
   -  `dice1 = random.randint(1, 6)`: מייצרת מספר שלם אקראי מ-1 עד 6 (הערך של הקובייה הראשונה).
   -  `dice2 = random.randint(1, 6)`: מייצרת מספר שלם אקראי מ-1 עד 6 (הערך של הקובייה השנייה).
   - `return dice1, dice2`: מחזירה את ערכי שתי הקוביות.
3. **הפונקציה `calculate_sum(dice1, dice2)`**:
   -  `def calculate_sum(dice1, dice2):`: הפונקציה מחשבת את סכום הערכים של שתי הקוביות.
   -  `return dice1 + dice2`: מחזירה את הסכום.
4.  **הפונקציה `play_craps()`**:
    -   `def play_craps():`:  הפונקציה שמכילה את כל לוגיקת המשחק "קְרֶפְּס".
    -   `dice1, dice2 = roll_dice()`: קוראת לפונקציה `roll_dice()` עבור הטלת הקוביות הראשונה.
    -   `sum_of_dice = calculate_sum(dice1,dice2)`: קוראת לפונקציה `calculate_sum()` לקבלת סכום הערכים של ההטלה הראשונה.
    -   `print(f"Выпало: {dice1} + {dice2} = {sum_of_dice}")`: מציגה על המסך את ערכי הקוביות ואת סכומן.
    -   **בדיקת הזכייה/ההפסד הראשונה**:
        -   `if sum_of_dice == 7 or sum_of_dice == 11:`: בודקת אם השחקן זכה בהטלה הראשונה (הסכום שווה ל-7 או ל-11).
        -   `print("YOU WIN!")`: מציגה הודעה על זכייה.
        -   `return`: מסיימת את המשחק.
        -   `elif sum_of_dice == 2 or sum_of_dice == 3 or sum_of_dice == 12:`: בודקת אם השחקן הפסיד בהטלה הראשונה (הסכום שווה ל-2, 3 או 12).
        -   `print("YOU LOSE!")`: מציגה הודעה על הפסד.
        -   `return`: מסיימת את המשחק.
    -  **קביעת מספר היעד (point)**:
        -   `point = sum_of_dice`: אם לא אירעה זכייה ולא אירע הפסד, הסכום הנוכחי הופך להיות מספר היעד (point).
        -   `print(f"Ваше целевое число: {point}")`: מציגה הודעה על מספר היעד.
    -  **לולאת המשחק הראשית**:
       -  `while True:`:  לולאה אינסופית שממשיכה עד שהשחקן זוכה או מפסיד.
       - `dice1, dice2 = roll_dice()`:  קוראת לפונקציה `roll_dice()` עבור הטלת קוביות חדשה.
       - `sum_of_dice = calculate_sum(dice1,dice2)`: קוראת לפונקציה `calculate_sum()` לקבלת סכום הערכים של ההטלה החדשה.
       - `print(f"Выпало: {dice1} + {dice2} = {sum_of_dice}")`: מציגה על המסך את ערכי הקוביות ואת סכומן.
       - **בדיקת הזכייה/ההפסד בלולאה**:
          - `if sum_of_dice == point:`: בודקת אם השחקן זכה (הסכום שווה למספר היעד).
          -   `print("YOU WIN!")`: מציגה הודעה על זכייה.
          -   `return`: מסיימת את המשחק.
          -   `elif sum_of_dice == 7:`: בודקת אם השחקן הפסיד (הסכום שווה ל-7).
          -   `print("YOU LOSE!")`: מציגה הודעה על הפסד.
          -   `return`: מסיימת את המשחק.
5.  **הפעלת המשחק**:
    -  `if __name__ == "__main__":`: בלוק זה מבטיח שהפונקציה `play_craps()` תופעל רק אם הקובץ מורץ ישירות, ולא מיובא כמודול.
    -  `play_craps()`: קוראת לפונקציה כדי להתחיל את המשחק.
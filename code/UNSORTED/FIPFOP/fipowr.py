FIPFOP:
=================
מורכבות: 5
-----------------
המשחק FIPFOP הינו משחק חידה, שבו על השחקן להזין מספרים, בזה אחר זה. מטרת המשחק היא להשיג מצב שבו סכום כל המספרים שהוזנו על ידי המשתמש יגיע ל-100. המשחק מסתיים כאשר סכום כל המספרים שהוזנו מגיע ל-100.

כללי המשחק:
1. השחקן מזין מספרים שלמים, אחד בכל פעם.
2. כל מספר שהוזן מתווסף לסכום הכולל.
3. המשחק מסתיים כאשר הסכום הכולל מגיע ל-100.
4. לאחר סיום המשחק, מוצג המספר הכולל של המספרים שהוזנו (מהלכים).
-----------------
אלגוריתם:
1. הגדרת הסכום ל-0.
2. הגדרת מונה המהלכים ל-0.
3. התחלת לולאה "כל עוד הסכום קטן מ-100":
    3.1 בקשת קלט מספר מהשחקן.
    3.2 הגדלת מונה המהלכים ב-1.
    3.3 הוספת המספר שהוזן לסכום הכולל.
4. הצגת הודעה: "השתמשת ב-{מספר המהלכים} מהלכים".
5. סיום המשחק.
-----------------

"""


# אתחול הסכום ומונה המהלכים
totalSum = 0 # סכום המספרים שהוזנו
numberOfTurns = 0 # מספר המהלכים

# לולאת המשחק הראשית
while totalSum < 100:
    # מבקשים קלט מספר מהמשתמש
    try:
        userNumber = int(input("Введите число: "))
    except ValueError:
         print("Пожалуйста, введите целое число.")
         continue

    # מגדילים את מונה המהלכים
    numberOfTurns += 1
    # מוסיפים את המספר שהוזן לסכום הכולל
    totalSum += userNumber

# מציגים הודעה על מספר המהלכים
print(f"ТЫ ИСПОЛЬЗОВАЛ {numberOfTurns} ХОДОВ")


"""
הסבר קוד:
1. **אתחול משתנים:**
   - `totalSum = 0`: מאתחל את המשתנה `totalSum` לשמירת סכום המספרים שהוזנו, מתחיל מ-0.
   - `numberOfTurns = 0`: מאתחל את המשתנה `numberOfTurns` לספירת מספר המהלכים, גם כן מתחיל מ-0.
2. **לולאת המשחק הראשית `while totalSum < 100`:**
   -  הלולאה נמשכת כל עוד סכום המספרים שהוזנו (`totalSum`) קטן מ-100.
3. **קלט נתונים מהמשתמש:**
    - `try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין מספר שאינו שלם, תוצג הודעת שגיאה.
   - `userNumber = int(input("Введите число: "))`: מבקש מהמשתמש להזין מספר וממיר אותו למספר שלם, שומר את התוצאה במשתנה `userNumber`.
4. **הגדלת מונה המהלכים:**
   - `numberOfTurns += 1`: מגדיל את מונה המהלכים ב-1 בכל איטרציה של הלולאה.
5.  **הוספת מספר לסכום:**
   -  `totalSum += userNumber`: מוסיף את המספר שהוזן (`userNumber`) לסכום הכולל (`totalSum`).
6.  **הצגת תוצאה:**
    - `print(f"ТЫ ИСПОЛЬЗОВАЛ {numberOfTurns} ХОДОВ")`: מציג על המסך הודעה המציינת כמה מהלכים בוצעו, כאשר סכום המספרים הגיע ל-100 או יותר.
"""
DIAMND:
=================
רמת קושי: 7
-----------------
המשחק DIAMND הוא משחק טקסטואלי שבו השחקן מנסה לצייר מעוין באמצעות כוכביות, על ידי קביעת גובהו.
המשחק מבקש תחילה מהמשתמש גובה אי-זוגי עבור המעוין. לאחר מכן, אם המספר שהוזן הוא אי-זוגי, המשחק בונה מעוין מכוכביות, תוך שימוש ברווחים ליצירת גיאומטריה נכונה של הצורה. אם המספר שהוזן הוא זוגי או קטן מ-1, מוצגת הודעת שגיאה.
כללי המשחק:
1.  השחקן מזין מספר אי-זוגי, הקובע את גובה המעוין.
2.  אם הוזן מספר זוגי או מספר קטן מ-1, המשחק מציג הודעת שגיאה.
3.  אם הוזן מספר תקין (אי-זוגי וגדול מ-0), המשחק מציג על המסך מעוין מכוכביות בגובה שנקבע.
-----------------
אלגוריתם:
1.  בקש מהמשתמש את קלט גובה המעוין (H).
2.  בדוק האם H הוא זוגי או קטן מ-1. אם כן, הצג הודעת שגיאה ועבור לשלב 1.
3.  אם H הוא אי-זוגי וגדול מ-0, אז:
    3.1. הגדר את המשתנה `space` ל-H // 2 (חילוק שלם).
    3.2. הגדר את המשתנה `stars` ל-1.
    3.3. התחל לולאה מ-1 עד H כולל:
        3.3.1. הדפס `space` רווחים.
        3.3.2. הדפס `stars` כוכביות.
        3.3.3. הדפס מעבר שורה.
        3.3.4. אם מספר השורה הנוכחי קטן מ-H // 2 + 1:
            3.3.4.1. הקטן את `space` ב-1.
            3.3.4.2. הגדל את `stars` ב-2.
        3.3.5. אחרת:
            3.3.5.1. הגדל את `space` ב-1.
            3.3.5.2. הקטן את `stars` ב-2.
4.  סיום המשחק.
-----------------

"""


# בקשת גובה המעוין מהמשתמש
while True:
    try:
        height = int(input("הזן גובה מעוין אי-זוגי (גדול מ-0): "))
        if height > 0 and height % 2 != 0:
            break  # יציאה מהלולאה אם הגובה תקין
        else:
            print("אנא הזן מספר אי-זוגי גדול מ-0.")
    except ValueError:
        print("אנא הזן מספר שלם.")


# אתחול משתנים
spaces = height // 2  # כמות התחלתית של רווחים לפני הכוכביות
stars = 1           # כמות התחלתית של כוכביות בשורה הראשונה

# לולאה להצגת המעוין
for row in range(1, height + 1):
    # הדפסת רווחים
    print(" " * spaces, end="")
    # הדפסת כוכביות
    print("*" * stars)

    # הגדלה או הקטנה של כמות הרווחים והכוכביות בהתאם לשורה
    if row <= height // 2:
        spaces -= 1
        stars += 2
    else:
        spaces += 1
        stars -= 2

"""
הסבר הקוד:
1. **בקשת גובה המעוין:**
   - לולאת `while True:`: לולאה אינסופית הנמשכת עד שהמשתמש יזין גובה מעוין תקין.
   - `try...except ValueError:`: בלוק לטיפול בשגיאות קלט. אם המשתמש מזין ערך שאינו מספר, מוצגת הודעת שגיאה.
   - `height = int(input(...))`: מבקש את קלט גובה המעוין מהמשתמש וממיר את הערך שהוזן למספר שלם.
   - `if height > 0 and height % 2 != 0:`: בודק האם המספר שהוזן הוא אי-זוגי וגדול מאפס. אם כן, הפקודה `break` יוצאת מהלולאה.
   - אם המספר שהוזן אינו עומד בתנאי, מוצגת הודעת שגיאה והלולאה נמשכת.

2. **אתחול משתנים:**
   - `spaces = height // 2`: מחשב את כמות הרווחים ההתחלתית לפני הכוכביות, תוך שימוש בחילוק שלם (`//`).
   - `stars = 1`: מאתחל את כמות הכוכביות בשורה הראשונה ל-1.

3. **לולאה להצגת המעוין:**
   - `for row in range(1, height + 1):`: הלולאה עוברת על השורות מ-1 עד `height` כולל.
   - `print(" " * spaces, end="")`: מדפיס את הכמות הנדרשת של רווחים לפני הכוכביות בשורה הנוכחית. הפרמטר `end=""` מונע מעבר שורה אוטומטי.
   - `print("*" * stars)`: מדפיס את הכמות הנדרשת של כוכביות. לאחר מכן מתרחש מעבר שורה אוטומטי.
   - `if row <= height // 2:`: בודק האם השורה הנוכחית נמצאת בחלק העליון של המעוין (כולל האמצע).
     - `spaces -= 1`: מקטין את כמות הרווחים ב-1 כדי שהמעוין יתרחב.
     - `stars += 2`: מגדיל את כמות הכוכביות ב-2 כדי שהמעוין יתרחב.
   - `else:`: אם השורה הנוכחית נמצאת בחלק התחתון של המעוין.
     - `spaces += 1`: מגדיל את כמות הרווחים ב-1 כדי שהמעוין יצר.
     - `stars -= 2`: מקטין את כמות הכוכביות ב-2 כדי שהמעוין יצר.
"""
GUESS:
=================
רמת קושי: 3
-----------------
המשחק "נחש את המספר" הוא משחק קלאסי שבו המחשב בוחר מספר אקראי בטווח שבין 1 ל-100, והשחקן צריך לנחש את המספר הזה. לאחר כל ניסיון, השחקן מקבל רמזים: "נמוך מדי" או "גבוה מדי".
המשחק נמשך עד שהשחקן מנחש את המספר.

כללי המשחק:
1. המחשב בוחר מספר שלם אקראי בין 1 ל-100.
2. השחקן מזין את הניחושים שלו לגבי המספר הנבחר.
3. לאחר כל ניסיון, המחשב מציין האם המספר שהוזן היה נמוך מדי, גבוה מדי, או שנחש.
4. המשחק נמשך עד שהשחקן מנחש את המספר הנבחר.
-----------------
אלגוריתם:
1. לאפס את מונה הניסיונות ל-0.
2. ליצור מספר אקראי בטווח שבין 1 ל-100.
3. להתחיל לולאת "כל עוד המספר לא נחש":
    3.1 להגדיל את מונה הניסיונות ב-1.
    3.2 לבקש מהשחקן להזין מספר.
    3.3 אם המספר שהוזן שווה למספר הנבחר, לעבור לשלב 4.
    3.4 אם המספר שהוזן קטן מהמספר הנבחר, להציג את ההודעה "TOO LOW".
    3.5 אם המספר שהוזן גדול מהמספר הנבחר, להציג את ההודעה "TOO HIGH".
4. להציג את ההודעה "YOU GOT IT IN {numberOfGuesses} GUESSES!"
5. סוף המשחק.
-----------------

"""
import random

# אתחול מונה הניסיונות
numberOfGuesses = 0
# יצירת מספר אקראי בין 1 ל-100
targetNumber = random.randint(1, 100)

# לולאת המשחק הראשית
while True:
    # הגדלת מספר הניסיונות
    numberOfGuesses += 1
    # בקשת קלט מספר מהמשתמש
    try:
        userGuess = int(input("נחש מספר בין 1 ל-100: "))
    except ValueError:
        print("אנא הזן מספר שלם.")
        continue

    # בדיקה האם המספר נחש
    if userGuess == targetNumber:
        print(f"מזל טוב! ניחשת את המספר ב-{numberOfGuesses} ניסיונות!")
        break  # סיום הלולאה אם המספר נחש
    elif userGuess < targetNumber:
        print("נמוך מדי")  # ציון שהמספר הנבחר גדול יותר
    else:
        print("גבוה מדי")  # ציון שהמספר הנבחר קטן יותר

"""
הסבר הקוד:
1.  **ייבוא המודול `random`**:
   -  `import random`: מייבא את המודול `random`, המשמש ליצירת מספר אקראי.
2.  **אתחול משתנים**:
    -   `numberOfGuesses = 0`: מאתחל את המשתנה `numberOfGuesses` לספירת ניסיונות השחקן, ומגדיר את ערכו ההתחלתי ל-0.
    -   `targetNumber = random.randint(1, 100)`: יוצר מספר שלם אקראי בטווח של 1 עד 100 (כולל) ושומר אותו במשתנה `targetNumber`. זהו המספר שהשחקן צריך לנחש.
3.  **הלולאה הראשית `while True:`**:
    -   `while True:`: מתחילה לולאה אינסופית, שתמשיך לפעול עד שהשחקן ינחש את המספר ויופעל האופרטור `break`.
    -   `numberOfGuesses += 1`: מגדילה את מונה הניסיונות ב-1 בכל איטרציה חדשה של הלולאה.
    -   **קלט נתונים עם בדיקת שגיאות**:
        -   `try...except ValueError`: בלוק זה מיועד לטיפול בשגיאות. אם המשתמש יזין קלט שאינו מספר שלם, התוכנית לא תקרוס עם שגיאה, אלא תציג את ההודעה "אנא הזן מספר שלם." ותמשיך לאיטרציה הבאה של הלולאה.
        -   `userGuess = int(input("נחש מספר בין 1 ל-100: "))`: מציגה הודעה עם בקשה להזנת מספר וממירה את הערך שהוזן למספר שלם, שומרת את התוצאה במשתנה `userGuess`.
    -   **תנאי ניצחון**:
        -   `if userGuess == targetNumber:`: בודקת האם המספר שהוזן על ידי השחקן שווה למספר הנבחר.
        -   `print(f"מזל טוב! ניחשת את המספר ב-{numberOfGuesses} ניסיונות!")`: מציגה הודעת ניצחון, הכוללת את מספר הניסיונות שנדרשו לשחקן.
        -   `break`: מסיימת את הלולאה הנוכחית `while True`, מכיוון שהמשחק הסתיים.
    -  **רמזים**:
        - `elif userGuess < targetNumber:`: אם המספר שהוזן קטן מהמספר הנבחר, בלוק זה מתבצע.
        - `print("נמוך מדי")`: מציגה רמז לפיו יש להזין מספר גדול יותר.
        - `else:`: אם המספר אינו שווה ואינו קטן מהנבחר (משמע הוא גדול יותר), בלוק זה מתבצע.
        - `print("גבוה מדי")`: מציגה רמז לפיו יש להזין מספר קטן יותר.
"""
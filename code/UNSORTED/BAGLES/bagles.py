BAGLES:
=================
רמת קושי: 6
-----------------
המשחק "בייגלס" הוא משחק חידה לוגי שבו השחקן מנסה לנחש מספר תלת-ספרתי המורכב מספרות שאינן חוזרות על עצמן. לאחר כל ניסיון, השחקן מקבל רמזים: "PICO" מציין שאחת הספרות נוחשה ונמצאת במיקום הנכון, "FERMI" מציין שאחת הספרות נוחשה אך אינה במיקום הנכון, "BAGELS" מציין שאף ספרה לא נוחשה.

כללי המשחק:
1. המחשב מגריל מספר תלת-ספרתי אקראי המורכב מספרות שאינן חוזרות על עצמן.
2. השחקן מזין את ניחושיו בצורת מספר תלת-ספרתי.
3. המחשב מספק רמזים:
    - "PICO" - ספרה אחת נוחשה ונמצאת במיקום הנכון.
    - "FERMI" - ספרה אחת נוחשה, אך אינה במיקום הנכון.
    - "BAGELS" - אף ספרה לא נוחשה.
4. הרמזים ניתנים לפי סדר המיקום של הספרות במספר שהוגרל, למשל אם המספר שהוגרל הוא `123` והשחקן הזין `142`, הרמזים יהיו `PICO FERMI`.
5. המשחק נמשך עד שהשחקן ינחש את המספר.
6. אם לאחר 10 ניסיונות השחקן לא מנחש את המספר, המשחק מסתיים והמספר שהוגרל מוצג.
-----------------
אלגוריתם:
1. ליצור מספר תלת-ספרתי אקראי המורכב מספרות שאינן חוזרות על עצמן (לדוגמה, 123).
2. להגדיר את מספר הניסיונות שווה ל-0.
3. לולאה "כל עוד המספר לא נוחש או מספר הניסיונות קטן מ-10":
    3.1. להגדיל את מספר הניסיונות ב-1.
    3.2. לבקש מהשחקן מספר תלת-ספרתי.
    3.3. להשוות את המספר שהוזן עם המספר שהוגרל וליצור את הרמזים "PICO", "FERMI" ו-"BAGELS".
    3.4. אם המספר נוחש, להדפיס הודעת ניצחון ומספר ניסיונות.
    3.5. אם המספר לא נוחש, להדפיס את הרמזים שנוצרו.
4. אם לאחר 10 ניסיונות המספר לא נוחש, להדפיס את המספר שהוגרל והודעת הפסד.
5. סוף המשחק.
-----------------

"""
import random

def generate_secret_number():
    """מגרילה מספר תלת-ספרתי אקראי עם ספרות שאינן חוזרות על עצמן."""
    digits = list(range(10))  # יוצרת רשימת ספרות מ-0 עד 9
    random.shuffle(digits)   # מערבבת את הספרות באופן אקראי
    secret_number = str(digits[0]) + str(digits[1]) + str(digits[2]) # יוצרת מספר תלת-ספרתי וממירה למחרוזת
    return secret_number

def get_clues(secret_number, user_guess):
    """מגרילה את הרמזים PICO, FERMI ו-BAGELS."""
    clues = []
    for i in range(3): # עוברת על תווי המספרים לפי אינדקס
        if user_guess[i] == secret_number[i]: # אם הספרה נוחשה ונמצאת במקומה
            clues.append("PICO")
        elif user_guess[i] in secret_number:  # אם הספרה נוחשה, אך אינה במקומה
            clues.append("FERMI")
    if not clues:   # אם לא נמצא אף רמז
        return ["BAGELS"]
    return clues

def play_bagels():
    """לוגיקת המשחק המרכזית של בייגלס."""
    secret_number = generate_secret_number()  # מגרילה מספר סודי
    number_of_guesses = 0 # מאתחלת את מונה הניסיונות
    
    print("הגרלתי מספר תלת-ספרתי, המורכב מספרות שאינן חוזרות על עצמן.")
    print("נסה לנחש אותו. \nרמזים:")
    print("PICO - ספרה במיקום הנכון, FERMI - ספרה קיימת, אך לא במקומה, BAGELS - שום דבר לא נוחש")
    
    while number_of_guesses < 10:  # לולאת המשחק המרכזית
        number_of_guesses += 1 # מגדילה את מונה הניסיונות
        
        while True:
            user_guess = input(f"ניסיון מס' {number_of_guesses}. אנא הזן מספר תלת-ספרתי: ") # קולטת קלט מהמשתמש
            if len(user_guess) == 3 and user_guess.isdigit(): # בודקת את תקינות הנתונים שהוזנו
               break # מסיימת את הלולאה אם הקלט תקין
            else:
               print("אנא הזן מספר תלת-ספרתי!") # חוזרת על הבקשה אם הקלט אינו תקין
        
        if user_guess == secret_number: # בודקת האם המשתמש ניחש את המספר
            print(f"כל הכבוד! ניחשת את המספר ב-{number_of_guesses} ניסיונות!")
            return # מסיימת את המשחק אם נוחש
        else:
            clues = get_clues(secret_number, user_guess) # מקבלת את הרמזים
            print(" ".join(clues)) # מדפיסה את הרמזים למשתמש
    
    print(f"הפסדת. המספר שהוגרל היה: {secret_number}")  # מדפיסה את המספר שהוגרל, אם לא נוחש ב-10 ניסיונות

if __name__ == "__main__":
    play_bagels() # הפעלת המשחק
```
```
הסבר הקוד:
1. **ייבוא מודול `random`**:
   - `import random`: מייבאת את מודול `random`, המשמש ליצירת מספרים אקראיים.

2. **פונקציה `generate_secret_number()`**:
   - פונקציה זו מגרילה מספר סודי תלת-ספרתי, תוך שימוש בספרות שאינן חוזרות על עצמן.
   - `digits = list(range(10))`: יוצרת רשימת ספרות מ-0 עד 9.
   - `random.shuffle(digits)`: מערבבת את הספרות באופן אקראי.
   - `secret_number = str(digits[0]) + str(digits[1]) + str(digits[2])`: מרכיבה את שלוש הספרות הראשונות מהרשימה המעורבבת וממירה אותן למחרוזת.
   - `return secret_number`: מחזירה את המספר הסודי שהתקבל כמחרוזת.

3. **פונקציה `get_clues(secret_number, user_guess)`**:
   - פונקציה זו משווה את הניחוש של המשתמש עם המספר הסודי ומגרילה את הרמזים.
   - `clues = []`: מאתחלת רשימה לאחסון הרמזים.
   - לולאה `for i in range(3)`: עוברת על כל ספרה במספר התלת-ספרתי.
      - `if user_guess[i] == secret_number[i]`: בודקת האם הספרה נוחשה ונמצאת במיקום הנכון, מוסיפה "PICO" לרשימת הרמזים.
      - `elif user_guess[i] in secret_number`: אם הספרה קיימת במספר הסודי, אך לא במקומה, מוסיפה "FERMI" לרשימת הרמזים.
   - `if not clues`: אם אין רמזים (הספרות לא תואמות), מחזירה רשימה עם הרמז היחיד "BAGELS".
   - `return clues`: מחזירה את רשימת הרמזים.

4.  **פונקציה `play_bagels()`**:
    -  `secret_number = generate_secret_number()`: מגרילה מספר סודי באמצעות הפונקציה `generate_secret_number()`.
    - `number_of_guesses = 0`: מאתחלת את מונה הניסיונות.
    - מוצגת הודעת ברכה ותיאור הרמזים.
    - לולאה `while number_of_guesses < 10`: לולאת המשחק המרכזית, המתבצעת כל עוד מספר הניסיונות קטן מ-10.
       - `number_of_guesses += 1`: מגדילה את מספר הניסיונות.
       - לולאה פנימית `while True` לקבלת ולידציה של הקלט מהמשתמש.
         - `user_guess = input(f"ניסיון מס' {number_of_guesses}. אנא הזן מספר תלת-ספרתי: ")`: מבקשת קלט מהמשתמש.
         - `if len(user_guess) == 3 and user_guess.isdigit()`: בודקת האם הקלט הוא מספר תלת-ספרתי. אם כן, יוצאים מהלולאה.
         - `else`: אם הקלט שגוי, מוצגת הודעת שגיאה והלולאה חוזרת על עצמה.
        - `if user_guess == secret_number`: בודקת האם המספר נוחש.
         - מדפיסה הודעת ניצחון ומסיימת את המשחק.
        -  `else`: אם המספר לא נוחש:
          - `clues = get_clues(secret_number, user_guess)`: מקבלת את הרמזים באמצעות הפונקציה `get_clues()`.
          - `print(" ".join(clues))`: מדפיסה את הרמזים שהתקבלו.
    - אם הלולאה מסתיימת (השחקן לא מנחש את המספר ב-10 ניסיונות), מוצגת הודעת הפסד והמספר שהוגרל.

5. **הפעלת המשחק**:
   - `if __name__ == "__main__":`: בודקת האם הסקריפט הופעל ישירות.
   - `play_bagels()`: קוראת לפונקציה `play_bagels()` כדי להתחיל את המשחק.
BAGLES:
=================
רמת קושי: 6
-----------------
משחק "בייגלס" הוא משחק היגיון וחידה, שבו השחקן מנסה לנחש מספר תלת-ספרתי המורכב מספרות שאינן חוזרות על עצמן. לאחר כל ניסיון, השחקן מקבל רמזים: "PICO" פירושו שאחת הספרות נוחשה ונמצאת במיקום הנכון, "FERMI" פירושו שאחת הספרות נוחשה אך אינה במיקום הנכון, "BAGELS" פירושו שאף אחת מהספרות לא נוחשה.

כללי המשחק:
1. המחשב מגריל מספר תלת-ספרתי אקראי המורכב מספרות שאינן חוזרות על עצמן.
2. השחקן מזין את ניחושיו בצורת מספר תלת-ספרתי.
3. המחשב מספק רמזים:
    - "PICO" - ספרה אחת נוחשה ונמצאת במיקום הנכון.
    - "FERMI" - ספרה אחת נוחשה, אך אינה במיקום הנכון.
    - "BAGELS" - אף אחת מהספרות לא נוחשה.
4. הרמזים ניתנים לפי סדר הספרות במספר הסודי, לדוגמה, אם המספר הסודי הוא `123` והשחקן הזין `142`, הרמזים יהיו `PICO FERMI`.
5. המשחק נמשך עד שהשחקן מנחש את המספר.
6. אם לאחר 10 ניסיונות השחקן אינו מנחש את המספר, המשחק מסתיים והמספר הסודי מוצג.
-----------------
אלגוריתם:
1. להגריל מספר תלת-ספרתי אקראי, המורכב מספרות שאינן חוזרות על עצמן (לדוגמה, 123).
2. לקבוע את מספר הניסיונות כשווה ל-0.
3. לולאה 'כל עוד המספר לא נוחש או מספר הניסיונות קטן מ-10':
    3.1. להגדיל את מספר הניסיונות ב-1.
    3.2. לבקש מהשחקן מספר תלת-ספרתי.
    3.3. להשוות את המספר שהוזן למספר הסודי ולהגריל את הרמזים "PICO", "FERMI" ו-"BAGELS".
    3.4. אם המספר נוחש, להציג הודעת ניצחון ומספר הניסיונות.
    3.5. אם המספר לא נוחש, להציג את הרמזים שהוגרלו.
4. אם לאחר 10 ניסיונות המספר לא נוחש, להציג את המספר הסודי והודעת הפסד.
5. סוף המשחק.
-----------------

"""
import random

def generate_secret_number():
    """מגרילה מספר תלת-ספרתי אקראי עם ספרות שאינן חוזרות על עצמן."""
    digits = list(range(10))  # יוצרת רשימת ספרות מ-0 עד 9
    random.shuffle(digits)   # מערבבת את הספרות באופן אקראי
    secret_number = str(digits[0]) + str(digits[1]) + str(digits[2]) # יוצרת מספר תלת-ספרתי וממירה אותו למחרוזת
    return secret_number

def get_clues(secret_number, user_guess):
    """מגרילה את הרמזים PICO, FERMI ו-BAGELS."""
    clues = []
    for i in range(3): # עוברת לפי אינדקסים על תווי המספרים
        if user_guess[i] == secret_number[i]: # אם הספרה נוחשה ונמצאת במיקומה הנכון
            clues.append("PICO")
        elif user_guess[i] in secret_number:  # אם הספרה נוחשה, אך אינה במיקומה הנכון
            clues.append("FERMI")
    if not clues:   # אם לא נמצא אף רמז
        return ["BAGELS"]
    return clues

def play_bagels():
    """לוגיקת המשחק הראשית של "בייגלס"."""
    secret_number = generate_secret_number()  # מגרילה מספר סודי
    number_of_guesses = 0 # מאתחלת את מספר הניסיונות

    print("אני הגרלתי מספר תלת-ספרתי, המורכב מספרות שאינן חוזרות על עצמן.")
    print("נסה לנחש אותו. \nרמזים:")
    print("PICO - ספרה במיקום הנכון, FERMI - ספרה קיימת, אך לא במיקום הנכון, BAGELS - כלום לא נוחש")

    while number_of_guesses < 10:  # לולאת המשחק הראשית
        number_of_guesses += 1 # מגדילה את מונה הניסיונות

        while True:
            user_guess = input(f"ניסיון מס' {number_of_guesses}. הזן מספר תלת-ספרתי: ") # מקבלת קלט משתמש
            if len(user_guess) == 3 and user_guess.isdigit(): # בודקת תקינות נתונים שהוזנו
               break # מסיימת את הלולאה אם הקלט תקין
            else:
               print("בבקשה הזן מספר תלת-ספרתי!") # חוזרת על הבקשה אם הקלט אינו תקין

        if user_guess == secret_number: # בודקת האם המשתמש ניחש את המספר
            print(f"מזל טוב! ניחשת את המספר ב-{number_of_guesses} ניסיונות!")
            return # מסיימת את המשחק אם נוחש
        else:
            clues = get_clues(secret_number, user_guess) # מקבלת רמזים
            print(" ".join(clues)) # מציגה רמזים למשתמש

    print(f"הפסדת. המספר הסודי היה: {secret_number}")  # מציגה את המספר הסודי אם לא נוחש ב-10 ניסיונות

if __name__ == "__main__":
    play_bagels() # הפעלת המשחק
```
```
הסבר הקוד:
1. **ייבוא מודול `random`**:
   - `import random`: מייבא את מודול `random`, המשמש להגרלת מספרים אקראיים.

2. **פונקציה `generate_secret_number()`**:
   - פונקציה זו מגרילה את המספר הסודי התלת-ספרתי, המורכב מספרות שאינן חוזרות על עצמן.
   - `digits = list(range(10))`: יוצרת רשימת ספרות מ-0 עד 9.
   - `random.shuffle(digits)`: מערבבת את הספרות באופן אקראי.
   - `secret_number = str(digits[0]) + str(digits[1]) + str(digits[2])`: אוספת את שלוש הספרות הראשונות מהרשימה המעורבבת וממירה אותן למחרוזת.
   - `return secret_number`: מחזירה את המספר הסודי שהתקבל כמחרוזת.

3. **פונקציה `get_clues(secret_number, user_guess)`**:
   - פונקציה זו משווה את ניחוש המשתמש למספר הסודי ומגרילה את הרמזים.
   - `clues = []`: מאתחלת רשימה לאחסון הרמזים.
   - לולאה `for i in range(3)`: עוברת על כל ספרה במספר התלת-ספרתי.
      - `if user_guess[i] == secret_number[i]`: בודקת אם הספרה נוחשה ונמצאת במיקום הנכון, ומוסיפה את "PICO" לרשימת הרמזים.
      - `elif user_guess[i] in secret_number`: אם הספרה קיימת במספר הסודי, אך לא במיקומה, מוסיפה את "FERMI" לרשימת הרמזים.
   - `if not clues`: אם אין רמזים (הספרות אינן תואמות), מחזירה רשימה עם הרמז היחיד "BAGELS".
   - `return clues`: מחזירה את רשימת הרמזים.

4. **פונקציה `play_bagels()`**:
   - `secret_number = generate_secret_number()`: מגרילה מספר סודי באמצעות הפונקציה `generate_secret_number()`.
   - `number_of_guesses = 0`: מאתחלת את מונה הניסיונות.
   - מוצג מסר פתיחה ותיאור הרמזים.
   - לולאה `while number_of_guesses < 10`: לולאת המשחק הראשית, הפועלת כל עוד מספר הניסיונות קטן מ-10.
      - `number_of_guesses += 1`: מגדילה את מספר הניסיונות.
      - לולאה פנימית `while True` לקבלת ואימות הקלט מהמשתמש.
        - `user_guess = input(f"ניסיון מס' {number_of_guesses}. הזן מספר תלת-ספרתי: ")`: מבקשת קלט מהמשתמש.
        - `if len(user_guess) == 3 and user_guess.isdigit()`: בודקת האם הקלט הוא מספר תלת-ספרתי. אם כן, יוצאת מהלולאה.
        - `else`: אם הקלט אינו חוקי, מוצגת הודעת שגיאה והלולאה חוזרת.
      - `if user_guess == secret_number`: בודקת האם המספר נוחש.
        - מציגה הודעת ניצחון ומסיימת את המשחק.
      - `else`: אם המספר לא נוחש:
        - `clues = get_clues(secret_number, user_guess)`: מקבלת רמזים באמצעות הפונקציה `get_clues()`.
        - `print(" ".join(clues))`: מציגה את הרמזים שהתקבלו.
   - אם הלולאה מסתיימת (השחקן אינו מנחש את המספר ב-10 ניסיונות), מציגה הודעת הפסד והמספר הסודי.

5. **הפעלת המשחק**:
   - `if __name__ == "__main__":`: בודקת האם הסקריפט הופעל ישירות.
   - `play_bagels()`: קוראת לפונקציה `play_bagels()` להפעלת המשחק.
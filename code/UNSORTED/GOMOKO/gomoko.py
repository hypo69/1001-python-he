<GOMOKO>:
=================
קושי: 5
-----------------
המשחק "גומוקו" (ידוע גם כ"חמש בשורה") הוא משחק לוח אסטרטגי לשני שחקנים. שחקנים מציבים בתורם את האסימונים שלהם (בדרך כלל איקסים ועיגולים) על לוח המשחק, ושואפים לבנות קו רציף של חמישה אסימונים משלהם אנכית, אופקית או באלכסון. המשחק מסתיים כאשר אחד מהשחקנים משיג תנאי זה, או כאשר כל הלוח מלא, ובמקרה זה מוכרז תיקו.
בגרסה זו של המשחק, הלוח מיוצג על ידי מטריצה בגודל 15x15, ומהלכי השחקן והמחשב מתבצעים לסירוגין. המחשב משתמש באלגוריתם פשוט לבחירת מהלכו. שחקנים מזינים את מהלכיהם בצורת קואורדינטות x ו-y.
כללי המשחק:
1. המשחק מתקיים על לוח בגודל 15x15.
2. שחקנים מבצעים מהלכים לסירוגין, מציבים את האסימונים שלהם (איקס 'X' לשחקן הראשון, עיגול 'O' לשחקן השני).
3. מטרת המשחק - להיות הראשון שבונה קו רציף של חמישה אסימונים משלו אנכית, אופקית או באלכסון.
4. המשחק מסתיים בניצחון של אחד מהשחקנים או בתיקו, אם הלוח מלא לחלוטין ואף אחד לא בנה קו של חמישה.
5. שחקנים מזינים את מהלכיהם בצורת קואורדינטות x ו-y.
-----------------
אלגוריתם:
1.  אתחול:
    1.1.  יצירת לוח ריק בגודל 15x15 (מערך A).
    1.2.  קביעת מאפיין תחילת המשחק `G = 1`.
    1.3.  קביעת דגל השחקן הנוכחי ל-1 (`P=1`).
2.  התחלת לולאת המשחק, כל עוד G שווה 1
    2.1.  הצגת מצב הלוח הנוכחי על המסך.
    2.2. אם השחקן הנוכחי הוא 1:
        2.2.1 בקשת קואורדינטות המהלך X, Y מהמשתמש.
        2.2.2 בדיקת תקינות המהלך: אם התא A[X,Y] אינו ריק, בקשת קלט המהלך שוב.
        2.2.3 הצבת סימן המהלך של השחקן הנוכחי (במקרה של P=1 זהו "X") בתא A[X,Y] שבלוח.
        2.2.4 שינוי דגל השחקן ל-P=2.
     2.3 אם השחקן הנוכחי הוא 2:
        2.3.1 המחשב בוחר מהלך אקראי, עד שימצא תא ריק.
        2.3.2 הצבת סימן המהלך של השחקן הנוכחי (במקרה של P=2 זהו "O") בתא A[X,Y] שבלוח.
        2.3.3 שינוי דגל השחקן ל-P=1.
     2.4 בדיקה האם יש מנצח או שהלוח מלא:
        2.4.1 אם יש קו של חמישה, יש להציג הודעה על המנצח ולהקצות `G = 0` (סיום המשחק).
        2.4.2 אם הלוח מלא ואין מנצח, יש להציג הודעה על תיקו ולהקצות `G = 0` (סיום המשחק).
3. סיום המשחק.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["Начало"] --> InitializeBoard["Инициализация: <br>Создание доски 15x15; <br>G = 1 (флаг игры); <br>P = 1 (игрок 1)"]
    InitializeBoard --> GameLoopStart{"Начало цикла игры:<br>while G == 1"}
    GameLoopStart --> DisplayBoard["Вывод текущего состояния доски"]
    DisplayBoard --> CheckPlayer{"Проверка: <br>P == 1 (ход игрока)?"]
    CheckPlayer -- Да --> PlayerInput["Ввод хода игрока (X, Y)"]
     PlayerInput --> ValidatePlayerMove{"Проверка допустимости хода:<br>Ячейка A[X,Y] пуста?"}
     ValidatePlayerMove -- Нет --> PlayerInput
      ValidatePlayerMove -- Да --> UpdateBoardPlayer["Обновление доски:<br>A[X, Y] = 'X'"]
    UpdateBoardPlayer --> ChangePlayerP1["Переключение игрока: P = 2"]
     ChangePlayerP1 --> CheckWinOrFull["Проверка победы или заполненности доски"]
      CheckPlayer -- Нет --> ComputerMove["Ход компьютера: выбор пустой ячейки (X, Y)"]
    ComputerMove --> UpdateBoardComputer["Обновление доски:<br>A[X, Y] = 'O'"]
    UpdateBoardComputer --> ChangePlayerP2["Переключение игрока: P = 1"]
     ChangePlayerP2 --> CheckWinOrFull
    CheckWinOrFull -- Победитель --> OutputWinner["Вывод сообщения о победе; <br> G = 0"]
    OutputWinner --> End["Конец"]
    CheckWinOrFull -- Доска заполнена --> OutputDraw["Вывод сообщения о ничьей; <br> G = 0"]
    OutputDraw --> End
     CheckWinOrFull -- Нет --> GameLoopStart
    GameLoopStart -- Нет --> End

```
מקרא:
    Start - תחילת המשחק.
    InitializeBoard - אתחול שדה המשחק (הלוח) בגודל 15x15, קביעת דגל המשחק G ל-1 וקביעת השחקן הנוכחי P ל-1.
    GameLoopStart - התחלת לולאת המשחק הראשית, הנמשכת כל עוד דגל המשחק G שווה ל-1.
    DisplayBoard - הצגת מצב שדה המשחק הנוכחי על המסך.
    CheckPlayer - בדיקה תור מי: אם P שווה ל-1, זהו מהלך השחקן, אחרת זהו מהלך המחשב.
    PlayerInput - קלט קואורדינטות X ו-Y עבור מהלך השחקן.
    ValidatePlayerMove - בדיקה האם התא שנבחר על ידי השחקן ריק (מהלך תקין). אם התא אינו ריק, מבקשת קלט מהלך שוב.
    UpdateBoardPlayer - עדכון הלוח: הצבת הסימן 'X' בתא שנבחר על ידי השחקן.
    ChangePlayerP1 - מעבר לשחקן השני: P הופך ל-2 (מהלך המחשב).
     ComputerMove - מהלך המחשב: בחירת תא ריק (X, Y).
    UpdateBoardComputer - עדכון הלוח: הצבת הסימן 'O' בתא שנבחר על ידי המחשב.
    ChangePlayerP2 - מעבר לשחקן הראשון: P הופך ל-1 (מהלך השחקן).
    CheckWinOrFull - בדיקה האם יש מנצח או ששדה המשחק מלא.
    OutputWinner - הצגת הודעה על ניצחון וקביעת דגל המשחק G ל-0 לסיום הלולאה.
    OutputDraw - הצגת הודעה על תיקו וקביעת דגל המשחק G ל-0 לסיום הלולאה.
    End - סיום המשחק.
"""


import random

# 1. אתחול:
# 1.1. יצירת לוח ריק בגודל 15x15
board = [[' ' for _ in range(15)] for _ in range(15)]
# 1.2. קביעת דגל תחילת המשחק G = 1
game_on = True
# 1.3. קביעת דגל השחקן, P = 1 (שחקן 1 מבצע מהלך ראשון)
current_player = 1

# פונקציה להצגת הלוח בקונסולה
def print_board(board):
    print("   ", end="")
    for i in range(15):
         print(f"{i:2}", end=" ")
    print()
    print("  " + "----" * 15)
    for i, row in enumerate(board):
        print(f"{i:2}|", end=" ")
        for cell in row:
            print(f"{cell:2}", end=" ")
        print()
    print()


# פונקציה לבדיקת קיום קו ניצחון
def check_winner(board, player):
    symbol = 'X' if player == 1 else 'O'
    # בדיקת קווים אופקיים
    for row in board:
        for i in range(len(row) - 4):
            if row[i] == row[i + 1] == row[i + 2] == row[i + 3] == row[i + 4] == symbol:
                return True
    # בדיקת קווים אנכיים
    for col in range(15):
        for i in range(15 - 4):
            if board[i][col] == board[i + 1][col] == board[i + 2][col] == board[i + 3][col] == board[i + 4][col] == symbol:
                 return True
    # בדיקת אלכסונים (מלמעלה-משמאל למטה-ימין)
    for row in range(15 - 4):
        for col in range(15 - 4):
            if board[row][col] == board[row+1][col+1] == board[row+2][col+2] == board[row+3][col+3] == board[row+4][col+4] == symbol:
                 return True
    # בדיקת אלכסונים (מלמעלה-מימין למטה-שמאל)
    for row in range(15 - 4):
        for col in range(4, 15):
            if board[row][col] == board[row+1][col-1] == board[row+2][col-2] == board[row+3][col-3] == board[row+4][col-4] == symbol:
                 return True
    return False


# פונקציה לבדיקת תיקו
def check_full_board(board):
    for row in board:
        for cell in row:
            if cell == ' ':
                return False # אם קיים לפחות תא אחד פנוי, המשחק לא הסתיים
    return True  # אם אין תאים ריקים - תיקו

# 2. לולאת המשחק הראשית
while game_on:
    # 2.1. הצגת מצב הלוח הנוכחי
    print_board(board)

    # 2.2. מהלך השחקן (P = 1)
    if current_player == 1:
        while True:
             try:
                # 2.2.1. בקשת קואורדינטות המהלך X, Y
                x = int(input("שחקן 1 (X) הכנס x (0-14): "))
                y = int(input("שחקן 1 (X) הכנס y (0-14): "))

                # 2.2.2. בדיקת תקינות המהלך:
                if 0 <= x < 15 and 0 <= y < 15 and board[x][y] == ' ':
                    # 2.2.3. הצבת סימן המהלך של השחקן הנוכחי (X) על הלוח
                    board[x][y] = 'X'
                    break
                else:
                    print("מהלך לא תקין. נסה שוב.")
             except ValueError:
                  print("קלט לא חוקי. אנא הכנס מספרים שלמים.")
        # 2.2.4. החלפת דגל השחקן
        current_player = 2
    # 2.3. מהלך המחשב (P = 2)
    else:
        print("מהלך המחשב:")
        while True:
            # 2.3.1. המחשב בוחר מהלך אקראי
            x = random.randint(0, 14)
            y = random.randint(0, 14)
            if board[x][y] == ' ':
                 # 2.3.2. הצבת סימן המהלך של השחקן הנוכחי (O) על הלוח
                board[x][y] = 'O'
                break

        # 2.3.3. החלפת דגל השחקן
        current_player = 1

    # 2.4. בדיקה האם קיים מנצח או תיקו
    if check_winner(board, 1 if current_player == 2 else 2 ):
        print_board(board)
        print(f"ניצח שחקן {'1 (X)' if current_player == 2 else '2 (O)'}!")
        game_on = False  # סיום המשחק
    elif check_full_board(board):
         print_board(board)
         print("תיקו!")
         game_on = False  # סיום המשחק

"""
הסבר על הקוד:
1.  **אתחול**:
    -   `board = [[' ' for _ in range(15)] for _ in range(15)]`: יוצר לוח משחק בגודל 15x15, המלא ברווחים (תאים ריקים).
    -   `game_on = True`: קובע דגל המאותת שהמשחק נמשך.
    -   `current_player = 1`: קובע את השחקן הנוכחי כשחקן הראשון (שחקן 1).

2.  **פונקציה `print_board(board)`**:
    -   מציגה את מצב לוח המשחק הנוכחי בקונסולה. כולל מספור שורות ועמודות לנוחות.

3.  **פונקציה `check_winner(board, player)`**:
    -   בודקת האם קיים מנצח.
    -   הסימן של השחקן הנוכחי ('X' עבור שחקן 1, 'O' עבור שחקן 2).
    -   בודקת קווים אופקיים, אנכיים ושני סוגי האלכסונים עבור רצף של חמישה סימנים.
    -   מחזירה `True` אם נמצא מנצח, אחרת `False`.

4. **פונקציה `check_full_board(board)`**:
    - בודקת האם לוח המשחק מלא.
    - מחזירה `True` אם אין תאים ריקים, אחרת `False`.

5.  **לולאת המשחק הראשית `while game_on:`**:
    -   נמשכת כל עוד `game_on` שווה ל-`True`.
    -   מציגה את הלוח הנוכחי.
    -   **מהלך השחקן**:
        -   אם `current_player` שווה ל-1, זהו מהלך השחקן הראשון.
        -   מבקשת קלט של קואורדינטות x ו-y.
         -   בודקת שהקואורדינטות בטווח המותר ושהתא ריק. אם לא, חוזרת על הבקשה.
        -   מציבה את הסימן 'X' בתא הנבחר.
        -   מחליפה את השחקן הנוכחי לשחקן השני (`current_player = 2`).
    -   **מהלך המחשב**:
        -   אם `current_player` שווה ל-2, זהו מהלך המחשב.
        -   המחשב בוחר קואורדינטות x ו-y אקראיות.
        -   בודקת שהתא אינו תפוס.
        -   מציבה את הסימן 'O' בתא הנבחר.
        -   מחליפה את השחקן הנוכחי לשחקן הראשון (`current_player = 1`).
    -   **בדיקה עבור ניצחון או תיקו**:
        -   קוראת לפונקציה `check_winner()` לבדיקת קיום מנצח, ואם קיים מנצח, מציגה בקונסולה הודעה על הניצחון, וקובעת את `game_on` ל-`False`.
        -   קוראת לפונקציה `check_full_board()` לבדיקת האם הלוח מלא. אם הלוח מלא, מציגה הודעה על תיקו ומסיימת את המשחק.

"""
```
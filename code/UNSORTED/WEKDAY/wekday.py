WEKDAY:
=================
מורכבות: 4
-----------------
המשחק "איזה יום בשבוע?" הוא משחק בו המחשב מבקש מהמשתמש להזין מספר יום בשנה (בין 1 ל-366), ולאחר מכן מודיע לו איזה יום בשבוע זה. המשחק מתחשב בשנים מעוברות.

כללי המשחק:
1. המשתמש מתבקש להזין מספר יום בשנה (בין 1 ל-366).
2. המחשב קובע האם השנה היא מעוברת.
3. המחשב מחשב את יום השבוע התואם למספר היום שהוזן.
4. המחשב מציג את יום השבוע.
-----------------
אלגוריתם:
1. הצגת בקשה למשתמש להזין את יום השנה.
2. קבלת קלט המשתמש והמרתו למספר שלם.
3. הצגת בקשה למשתמש להזין את השנה.
4. קבלת קלט המשתמש והמרתו למספר שלם.
5. חישוב האם השנה היא מעוברת.
6. חישוב יום השבוע באמצעות שארית החלוקה של מספר הימים ב-7. (1 - ראשון, 2 - שני, ..., 7 - שבת)
7. הצגת יום השבוע.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["Начало"] --> InputDay["Ввод номера дня в году: <code><b>dayOfYear</b></code>"]
    InputDay --> InputYear["Ввод года: <code><b>year</b></code>"]
    InputYear --> IsLeapYear{"Проверка: <code><b>год високосный?</b></code>"}
    IsLeapYear -- Да --> CalculateDayOfWeekLeap["Вычисление дня недели для високосного года"]
    IsLeapYear -- Нет --> CalculateDayOfWeekNonLeap["Вычисление дня недели для обычного года"]
    CalculateDayOfWeekLeap --> OutputDayOfWeek["Вывод дня недели"]
    CalculateDayOfWeekNonLeap --> OutputDayOfWeek
    OutputDayOfWeek --> End["Конец"]

```
**מקרא**:
    Start - התחלת התוכנית.
    InputDay - בקשה מהמשתמש להזין את מספר היום בשנה ושמירתו במשתנה dayOfYear.
    InputYear - בקשה מהמשתמש להזין את השנה ושמירתו במשתנה year.
    IsLeapYear - בדיקה האם השנה שהוזנה היא מעוברת.
    CalculateDayOfWeekLeap - חישוב יום השבוע עבור שנה מעוברת.
    CalculateDayOfWeekNonLeap - חישוב יום השבוע עבור שנה רגילה.
    OutputDayOfWeek - הצגת יום השבוע על המסך.
    End - סיום התוכנית.
"""


# מבקש מהמשתמש את מספר היום בשנה
try:
    dayOfYear = int(input("הזן מספר יום בשנה (1-366): "))
    if not 1 <= dayOfYear <= 366:
         print("אנא הזן מספר בין 1 ל-366.")
         exit()
except ValueError:
    print("אנא הזן מספר שלם.")
    exit()


# מבקש מהמשתמש את השנה
try:
    year = int(input("הזן שנה: "))
except ValueError:
    print("אנא הזן מספר שלם.")
    exit()


# קובע האם השנה היא מעוברת
isLeapYear = (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)

# מחשב את יום השבוע
if isLeapYear:
    dayOfWeek = (dayOfYear + 2) % 7 # ה-1 בינואר של שנה מעוברת הוא יום שני (היום השני בשבוע), לכן מוסיפים 2
else:
    dayOfWeek = (dayOfYear + 1) % 7 # ה-1 בינואר של שנה רגילה הוא יום ראשון (היום הראשון בשבוע), לכן מוסיפים 1

# מתאים את מספר יום השבוע לייצוג מחרוזת
days = ["ראשון", "שני", "שלישי", "רביעי", "חמישי", "שישי", "שבת"]
dayOfWeekStr = days[dayOfWeek] if dayOfWeek > 0 else days[6]  # אם 0, זה שבת


# מציג את התוצאה
print(f"זהו יום ה-{dayOfWeekStr}.")

"""
הסבר הקוד:
1.  **קלט נתונים**:
    -   `try...except ValueError`: בלוקי `try...except` משמשים לטיפול בשגיאות אפשריות בעת קלט נתונים על ידי המשתמש. אם המשתמש מזין קלט שלא ניתן להמיר למספר שלם, התוכנית תציג הודעת שגיאה ותסתיים.
    -   `dayOfYear = int(input("הזן מספר יום בשנה (1-366): "))`: מבקש מהמשתמש להזין מספר יום בשנה וממיר את הקלט למספר שלם.
    -    `if not 1 <= dayOfYear <= 366`: בודק האם המספר שהוזן הוא יום חוקי בשנה (1-366). אם לא, מציג הודעת שגיאה ומסיים את התוכנית.
    -   `year = int(input("הזן שנה: "))`: מבקש מהמשתמש להזין את השנה וממיר את הקלט למספר שלם.
2.  **קביעת שנה מעוברת**:
    -   `isLeapYear = (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)`: קובע האם השנה שהוזנה היא מעוברת. שנה היא מעוברת אם היא מתחלקת ב-4, אך לא מתחלקת ב-100, או אם היא מתחלקת ב-400.
3.  **חישוב יום השבוע**:
    -   `if isLeapYear:`: בודק האם השנה היא מעוברת.
        - `dayOfWeek = (dayOfYear + 2) % 7`: אם השנה מעוברת, מוסיפים 2 למספר היום, מכיוון שה-1 בינואר של שנה מעוברת הוא יום שני. מחשבים את שארית החלוקה ב-7, מה שנותן לנו את יום השבוע (0 - ראשון, 1 - שני, ..., 6 - שבת).
    -   `else:`: אם השנה אינה מעוברת.
        -   `dayOfWeek = (dayOfYear + 1) % 7`:  אם השנה אינה מעוברת, מוסיפים 1 למספר היום, מכיוון שה-1 בינואר של שנה רגילה הוא יום ראשון. כמו כן, מחשבים את שארית החלוקה ב-7.
4.  **המרה של מספר יום השבוע למחרוזת**:
     -   `days = ["ראשון", "שני", "שלישי", "רביעי", "חמישי", "שישי", "שבת"]`: רשימת שמות ימי השבוע.
     - `dayOfWeekStr = days[dayOfWeek] if dayOfWeek > 0 else days[6]`: ממירים את מספר יום השבוע למחרוזת, באמצעות הרשימה `days`.  אם `dayOfWeek` שווה ל-0, מגדירים את יום השבוע כ"שבת".
5.  **הצגת התוצאה**:
    -   `print(f"זהו יום ה-{dayOfWeekStr}.")`: מציג הודעה עם יום השבוע על המסך.
"""
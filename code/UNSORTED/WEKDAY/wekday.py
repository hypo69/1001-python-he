WEKDAY:
=================
רמת מורכבות: 4
-----------------
המשחק "איזה יום בשבוע?" הוא משחק שבו המחשב מבקש מהמשתמש להזין את מספר היום בשנה (מ-1 עד 366), ולאחר מכן מודיע איזה יום בשבוע זה. המשחק לוקח בחשבון שנים מעוברות.

כללי המשחק:
1. המשתמש מתבקש להזין מספר יום בשנה (מ-1 עד 366).
2. המחשב קובע אם השנה היא שנה מעוברת.
3. המחשב מחשב את יום השבוע המתאים למספר היום שהוזן.
4. המחשב מציג את יום השבוע.
-----------------
אלגוריתם:
1. הצג הנחיה למשתמש להזין את יום השנה.
2. קבל את קלט המשתמש והמר אותו למספר שלם.
3. הצג הנחיה למשתמש להזין את השנה.
4. קבל את קלט המשתמש והמר אותו למספר שלם.
5. חשב האם השנה היא שנה מעוברת.
6. חשב את יום השבוע באמצעות שארית החלוקה של מספר הימים ב-7. (1 - ראשון, 2 - שני, ..., 7 - שבת)
7. הצג את יום השבוע.
-----------------
דיאגרמת זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InputDay["הזנת מספר היום בשנה: <code><b>dayOfYear</b></code>"]
    InputDay --> InputYear["הזנת שנה: <code><b>year</b></code>"]
    InputYear --> IsLeapYear{"בדיקה: <code><b>השנה מעוברת?</b></code>"}
    IsLeapYear -- כן --> CalculateDayOfWeekLeap["חישוב יום השבוע לשנה מעוברת"]
    IsLeapYear -- לא --> CalculateDayOfWeekNonLeap["חישוב יום השבוע לשנה רגילה"]
    CalculateDayOfWeekLeap --> OutputDayOfWeek["הצגת יום השבוע"]
    CalculateDayOfWeekNonLeap --> OutputDayOfWeek
    OutputDayOfWeek --> End["סיום"]

```
**מקרא**:
    Start - התחלת התוכנית.
    InputDay - בקשת מספר היום בשנה מהמשתמש ושמירה במשתנה dayOfYear.
    InputYear - בקשת השנה מהמשתמש ושמירה במשתנה year.
    IsLeapYear - בדיקה האם השנה שהוזנה היא שנה מעוברת.
    CalculateDayOfWeekLeap - חישוב יום השבוע לשנה מעוברת.
    CalculateDayOfWeekNonLeap - חישוב יום השבוע לשנה רגילה.
    OutputDayOfWeek - הצגת יום השבוע על המסך.
    End - סיום התוכנית.
"""


# בקשת מספר היום בשנה מהמשתמש
try:
    dayOfYear = int(input("הזן מספר יום בשנה (1-366): "))
    if not 1 <= dayOfYear <= 366:
         print("אנא הזן מספר בין 1 ל-366.")
         exit()
except ValueError:
    print("אנא הזן מספר שלם.")
    exit()


# בקשת השנה מהמשתמש
try:
    year = int(input("הזן שנה: "))
except ValueError:
    print("אנא הזן מספר שלם.")
    exit()


# קביעה האם השנה היא שנה מעוברת
isLeapYear = (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)

# חישוב יום השבוע
if isLeapYear:
    dayOfWeek = (dayOfYear + 2) % 7 # 1 בינואר בשנה מעוברת הוא יום שני (היום השני בשבוע), ולכן מוסיפים 2
else:
    dayOfWeek = (dayOfYear + 1) % 7 # 1 בינואר בשנה רגילה הוא יום ראשון (היום הראשון בשבוע), ולכן מוסיפים 1

# התאמת מספר יום השבוע לייצוג טקסטואלי
days = ["ראשון", "שני", "שלישי", "רביעי", "חמישי", "שישי", "שבת"]
dayOfWeekStr = days[dayOfWeek] if dayOfWeek > 0 else days[6]  # אם התוצאה 0, זה יום שבת


# הצגת התוצאה
print(f"זה יום {dayOfWeekStr}.")

"""
הסבר לקוד:
1.  **קלט נתונים**:
    -   `try...except ValueError`: בלוקים של `try...except` משמשים לטיפול בשגיאות אפשריות בעת קליטת נתונים מהמשתמש. אם המשתמש מזין משהו שלא ניתן להמרה למספר שלם, התוכנית תציג הודעת שגיאה ותסתיים.
    -   `dayOfYear = int(input("הזן מספר יום בשנה (1-366): "))`: מבקש מהמשתמש להזין את מספר היום בשנה וממיר את הקלט למספר שלם.
    -   `if not 1 <= dayOfYear <= 366`: בודק האם המספר שהוזן הוא יום תקין בשנה (1-366). אם לא, מוצגת הודעת שגיאה והתוכנית מסתיימת.
    -   `year = int(input("הזן שנה: "))`: מבקש מהמשתמש להזין את השנה וממיר את הקלט למספר שלם.
2.  **קביעת שנה מעוברת**:
    -   `isLeapYear = (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)`: קובע האם השנה שהוזנה היא שנה מעוברת. שנה נחשבת מעוברת אם היא מתחלקת ב-4, אך לא ב-100, או אם היא מתחלקת ב-400.
3.  **חישוב יום השבוע**:
    -   `if isLeapYear:`: בודק האם השנה היא מעוברת.
        - `dayOfWeek = (dayOfYear + 2) % 7`: אם השנה מעוברת, מוסיפים 2 למספר היום, מכיוון ש-1 בינואר בשנה מעוברת הוא יום שני. מחשבים את שארית החלוקה ב-7, מה שנותן את יום השבוע (0 - ראשון, 1 - שני, ..., 6 - שבת).
    -   `else:`: אם השנה אינה מעוברת.
        -   `dayOfWeek = (dayOfYear + 1) % 7`: אם השנה אינה מעוברת, מוסיפים 1 למספר היום, מכיוון ש-1 בינואר בשנה רגילה הוא יום ראשון. גם כאן מחשבים את שארית החלוקה ב-7.
4.  **המרת מספר יום השבוע למחרוזת**:
     -   `days = ["ראשון", "שני", "שלישי", "רביעי", "חמישי", "שישי", "שבת"]`: רשימה של שמות ימי השבוע.
     - `dayOfWeekStr = days[dayOfWeek] if dayOfWeek > 0 else days[6]`: ממירים את מספר יום השבוע למחרוזת באמצעות הרשימה `days`. אם `dayOfWeek` שווה ל-0, נקבע יום השבוע כ"שבת".
5.  **הצגת התוצאה**:
    -   `print(f"זה יום {dayOfWeekStr}.")`: מציג את ההודעה עם יום השבוע על המסך.
"""
```
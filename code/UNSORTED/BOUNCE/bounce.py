BOUNCE:
=================
מורכבות: 5
-----------------
המשחק "כדור קופץ" הוא אנימציה פשוטה המדמה תנועת כדור המקפץ מהגבולות העליון והתחתון של המסך. המשתמש יכול ללחוץ על כל מקש כדי לעצור את האנימציה. גובה הכדור משתנה בצורה סינוסואידית.

כללי המשחק:
1. הכדור מתחיל לנוע ממיקום אנכי התחלתי מסוים.
2. הכדור נע למעלה ולמטה, מדמה קפיצה מהגבולות העליון והתחתון של המסך.
3. גובה הכדור משתנה לפי פונקציית סינוס.
4. האנימציה נמשכת עד שהמשתמש לוחץ על מקש.
-----------------
אלגוריתם:
1. אתחול משתנים:
    - `Y` - מיקום אנכי התחלתי של הכדור (אמצע המסך).
    - `T` - ערך התחלתי עבור פונקציית הסינוס (0).
    - `D` - צעד השינוי עבור פונקציית הסינוס (0.2).
2. התחלת לולאה אינסופית:
    2.1. חישוב מיקום אנכי חדש `Y` לפי הנוסחה: `20 + 19 * SIN(T)`.
    2.2. הצגת `Y` על המסך, באמצעות ערך שלם.
    2.3. הגדלת `T` ב-`D`.
    2.4. בדיקה האם נלחץ מקש כלשהו.
    2.5. אם נלחץ מקש, יש לצאת מהלולאה.
3. סיום המשחק.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InitializeVariables["<p align='left'>אתחול משתנים:
    <code><b>
    Y = 20
    T = 0
    D = 0.2
    </b></code></p>"]
    InitializeVariables --> LoopStart{"התחלת לולאה אינסופית"}
    LoopStart --> CalculateY["<code><b>Y = 20 + 19 * SIN(T)</b></code>"]
    CalculateY --> OutputY["הצג <code><b>INT(Y)</b></code> על המסך"]
    OutputY --> IncreaseT["<code><b>T = T + D</b></code>"]
    IncreaseT --> CheckKeyPress{"בדיקה: האם נלחץ מקש?"}
    CheckKeyPress -- לא --> LoopStart
    CheckKeyPress -- כן --> End["סיום"]

```
מקרא:
    Start - התחלת התוכנית.
    InitializeVariables - אתחול משתנים: Y (מיקום אנכי התחלתי), T (ערך התחלתי עבור פונקציית הסינוס), D (צעד השינוי עבור פונקציית הסינוס).
    LoopStart - התחלת לולאה אינסופית.
    CalculateY - חישוב מיקום אנכי חדש Y לפי הנוסחה 20 + 19 * SIN(T).
    OutputY - הצגת ערך שלם של Y על המסך.
    IncreaseT - הגדלת ערך T בגודל D.
    CheckKeyPress - בדיקה האם נלחץ מקש כלשהו.
    End - סיום התוכנית.
"""
import math
import time
import os
import sys

def clear_screen():
    """מנקה את מסך הטרמינל."""
    if os.name == 'nt':  # עבור Windows
        os.system('cls')
    else:  # עבור Linux ו-macOS
        os.system('clear')

def get_keypress():
    """בודק לחיצת מקש, ללא חסימת ריצת התוכנית."""
    if os.name == 'nt':
        import msvcrt
        if msvcrt.kbhit():
            msvcrt.getch()
            return True
    else:
        import select
        if sys.stdin in select.select([sys.stdin], [], [], 0)[0]:
            sys.stdin.read(1)
            return True
    return False

# אתחול משתנים
Y = 20  # מיקום אנכי התחלתי של הכדור
T = 0   # ערך התחלתי עבור פונקציית הסינוס
D = 0.2 # צעד השינוי עבור פונקציית הסינוס

# לולאה אינסופית של אנימציה
while True:
    # מחשבים את המיקום האנכי החדש של הכדור
    Y = 20 + 19 * math.sin(T)
    # מנקים את המסך לפני הצגת המיקום החדש
    clear_screen()
    # מציגים את הכדור על המסך
    print(" " * int(Y) + "O")
    # מגדילים את הערך של T עבור הפריימים הבאים
    T += D
    # השהיה ליצירת אפקט של אנימציה
    time.sleep(0.1)
    # בודקים לחיצת מקש
    if get_keypress():
        break  # סיום הלולאה אם נלחץ מקש
"""
הסבר קוד:
1. **ייבוא מודולים:**
   - `import math`: מייבא את מודול `math` לשימוש בפונקציה המתמטית `sin`.
   - `import time`: מייבא את מודול `time` לשליטה בהשהיה באנימציה.
   - `import os`: מייבא את מודול `os` לעבודה עם מערכת ההפעלה.
   - `import sys`: מייבא את מודול `sys` לעבודה עם פרמטרים מערכתיים.
2. **פונקציה `clear_screen()`:**
   - הפונקציה `clear_screen()` משמשת לניקוי מסך הטרמינל.
   - בהתאם למערכת ההפעלה (Windows או מערכות דמויות Unix), נקראת פקודת הניקוי המתאימה.
3. **פונקציה `get_keypress()`:**
    - הפונקציה `get_keypress()` בודקת האם נלחץ מקש כלשהו.
    - עבור Windows משתמשים במודול `msvcrt`, `kbhit()` - בודק לחיצה, `getch()` - קורא את התו.
    - עבור מערכות דמויות Unix משתמשים ב-`select.select` לקריאת תו מ-stdin ללא חסימה.
    - מחזירה `True` אם נלחץ מקש, `False` אחרת.
4. **אתחול משתנים:**
   - `Y = 20`: מיקום אנכי התחלתי של הכדור.
   - `T = 0`: ערך התחלתי עבור פונקציית הסינוס.
   - `D = 0.2`: צעד השינוי עבור פונקציית הסינוס.
5. **לולאה אינסופית `while True:`:**
   - הלולאה נמשכת עד שיילחץ מקש.
   - `Y = 20 + 19 * math.sin(T)`: מחשבת את המיקום האנכי החדש של הכדור, באמצעות פונקציית הסינוס.
   - `clear_screen()`: מנקה את המסך לפני ציור המיקום החדש.
   - `print(" " * int(Y) + "O")`: מציגה את הכדור (`O`) על המסך.
   - `T += D`: מגדילה את הערך של `T` עבור צעד האנימציה הבא.
   - `time.sleep(0.1)`: יוצרת השהיה של 0.1 שניות כדי להאט את האנימציה.
   - `if get_keypress():`: בודקת האם נלחץ מקש.
      - `break`: מסיימת את הלולאה אם נלחץ מקש.
"""
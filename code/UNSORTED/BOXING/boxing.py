<BOXING>:
=================
רמת מורכבות: 4
-----------------
המשחק "איגרוף" מהווה סימולציה טקסטואלית של דו-קרב איגרוף בין שני שחקנים, כאשר כל שחקן בתורו מנחית מכות, בבחירת עוצמתן (חלשה, בינונית, חזקה), והמחשב קובע באקראי האם המכה פגעה. המשחק נמשך עד אשר אחד המתאגרפים יוכרע בנוקאאוט (מונה הבריאות שלו יגיע ל-0).
כללי המשחק:
1.  משחקים שני שחקנים בתורות.
2.  לכל שחקן יש מונה בריאות, ששווה בתחילה ל-10.
3.  השחקן יכול לבחור אחד מתוך שלושה סוגי מכה: חלשה (1), בינונית (2) או חזקה (3).
4.  עוצמת המכה משפיעה על סיכוי הפגיעה: למכה חלשה ההסתברות הגבוהה ביותר לפגיעה, ולחזקה - הנמוכה ביותר.
5.  אם המכה פוגעת, בריאות היריב פוחתת ב-1.
6.  המשחק מסתיים כאשר בריאותו של אחד מהשחקנים מגיעה ל-0.
7.  השחקן שבריאותו נותרה מעל 0 מוכרז כמנצח.
-----------------
אלגוריתם:
1. אתחל את בריאות שני השחקנים (שחקן 1 ושחקן 2) לערך 10.
2. התחל לולאת משחק הנמשכת כל עוד בריאות שני השחקנים גדולה מ-0:
    2.1 הצג את הבריאות הנוכחית של שני השחקנים.
    2.2 בקש קלט מהשחקן הנוכחי (שחקן 1 או 2).
        - אם השחקן הנוכחי הוא שחקן 1, בקש קלט של עוצמת מכה (1-חלשה, 2-בינונית, 3-חזקה).
        - אם השחקן הנוכחי הוא שחקן 2, בקש קלט של עוצמת מכה (1-חלשה, 2-בינונית, 3-חזקה).
    2.3 צור מספר אקראי מ-1 עד 100.
    2.4 אם:
        - השחקן בחר במכה חלשה (1) והמספר האקראי קטן או שווה ל-80, אז המכה פגעה במטרה, הפחת את בריאות היריב ב-1.
        - השחקן בחר במכה בינונית (2) והמספר האקראי קטן או שווה ל-60, אז המכה פגעה במטרה, הפחת את בריאות היריב ב-1.
        - השחקן בחר במכה חזקה (3) והמספר האקראי קטן או שווה ל-40, אז המכה פגעה במטרה, הפחת את בריאות היריב ב-1.
        - אחרת, המכה לא פגעה במטרה.
    2.5 הצג הודעה האם המכה פגעה במטרה.
    2.6 החלף את התור לשחקן הבא.
3. לאחר סיום הלולאה, קבע את המנצח (שבריאותו מעל 0).
4. הצג הודעה על המנצח.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["Начало"] --> InitializeHealth["<p align='left'>Инициализация здоровья игроков:
    <code><b>
    player1Health = 10
    player2Health = 10
    currentPlayer = 1
    </b></code></p>"]
    InitializeHealth --> LoopStart{"Начало цикла: пока здоровье обоих игроков > 0"}
    LoopStart -- Да --> DisplayHealth["Вывод текущего здоровья игроков"]
    DisplayHealth --> InputAttack["Ввод силы удара игроком <code><b>currentPlayer</b></code>"]
    InputAttack --> GenerateRandomNumber["Генерация случайного числа <code><b>randomNum</b></code> от 1 до 100"]
    GenerateRandomNumber --> CheckHit["<p align='left'>Проверка на попадание в зависимости от силы удара:
    <code><b>
    if (attackType == 1 and randomNum <= 80) or
        (attackType == 2 and randomNum <= 60) or
        (attackType == 3 and randomNum <= 40)
    </b></code></p>"]
    CheckHit -- Да --> ReduceOpponentHealth["Уменьшение здоровья противника на 1"]
    ReduceOpponentHealth --> OutputHit["Вывод сообщения о попадании"]
    OutputHit --> SwitchPlayer["Переключение на следующего игрока"]
    SwitchPlayer --> LoopStart
    CheckHit -- Нет --> OutputMiss["Вывод сообщения о промахе"]
    OutputMiss --> SwitchPlayer
    LoopStart -- Нет --> DetermineWinner["Определение победителя"]
    DetermineWinner --> OutputWinner["Вывод сообщения о победителе"]
    OutputWinner --> End["Конец"]
```

מקרא:
    Start - תחילת התוכנית.
    InitializeHealth - אתחול משתנים: player1Health ו- player2Health (בריאות השחקנים) מוגדרים ל-10, currentPlayer (השחקן הנוכחי) מוגדר ל-1.
    LoopStart - תחילת לולאה, הנמשכת כל עוד בריאות שני השחקנים גדולה מ-0.
    DisplayHealth - הצגת הבריאות הנוכחית של שני השחקנים.
    InputAttack - בקשת קלט של עוצמת מכה (1, 2 או 3) מהשחקן הנוכחי ושמירתה במשתנה attackType.
    GenerateRandomNumber - יצירת מספר אקראי בטווח שבין 1 ל-100 ושמירתו במשתנה randomNum.
    CheckHit - בדיקה האם המכה פגעה, על בסיס המספר האקראי ועוצמת המכה שנבחרה.
    ReduceOpponentHealth - הפחתת בריאות היריב ב-1, אם המכה פגעה.
    OutputHit - הצגת הודעה על פגיעת המכה.
    SwitchPlayer - העברת התור לשחקן הבא.
    OutputMiss - הצגת הודעה על החטאה.
    DetermineWinner - קביעת המנצח לאחר סיום הלולאה.
    OutputWinner - הצגת הודעה על המנצח.
    End - סוף התוכנית.
```
```python
import random

# Инициализация здоровья игроков
player1Health = 10
player2Health = 10
# Начинаем с первого игрока
currentPlayer = 1


# Основной игровой цикл
while player1Health > 0 and player2Health > 0:
    # Выводим текущее здоровье игроков
    print(f"Здоровье игрока 1: {player1Health}, Здоровье игрока 2: {player2Health}")
    
    # Запрашиваем ввод от текущего игрока
    while True:
        try:
            attackType = int(input(f"Игрок {currentPlayer}, выберите силу удара (1-слабый, 2-средний, 3-сильный): "))
            if attackType in [1, 2, 3]:
                break
            else:
                print("Некорректный ввод. Пожалуйста, выберите 1, 2 или 3.")
        except ValueError:
            print("Некорректный ввод. Пожалуйста, введите число.")
    
    # Генерируем случайное число
    randomNum = random.randint(1, 100)
    
    # Определяем, попал ли удар и уменьшаем здоровье противника, если удар попал
    hit = False
    if attackType == 1 and randomNum <= 80:
        hit = True
    elif attackType == 2 and randomNum <= 60:
        hit = True
    elif attackType == 3 and randomNum <= 40:
        hit = True
    
    if hit:
        print("Удар достиг цели!")
        if currentPlayer == 1:
            player2Health -= 1
        else:
            player1Health -= 1
    else:
        print("Удар не достиг цели.")

    # Переключаем ход на следующего игрока
    currentPlayer = 3 - currentPlayer  # Переключает с 1 на 2 и с 2 на 1

# Определяем победителя
if player1Health <= 0:
    print("Игрок 2 победил!")
else:
    print("Игрок 1 победил!")
```
```
הסבר הקוד:
1.  **ייבוא מודול `random`**:
   -  `import random`: מייבא את מודול `random`, המשמש ליצירת מספרים אקראיים.
2.  **אתחול משתנים**:
    -   `player1Health = 10`: מאתחל את בריאות שחקן 1 עם ערך 10.
    -   `player2Health = 10`: מאתחל את בריאות שחקן 2 עם ערך 10.
    -   `currentPlayer = 1`: מגדיר את השחקן הנוכחי לשחקן 1.
3. **לולאת המשחק הראשית `while player1Health > 0 and player2Health > 0:`**:
   -  הלולאה נמשכת כל עוד בריאות שני השחקנים גדולה מ-0.
   -  **הצגת הבריאות הנוכחית**:
        - `print(f"Здоровье игрока 1: {player1Health}, Здоровье игрока 2: {player2Health}")`: מציג את הבריאות הנוכחית של שני השחקנים.
   - **בקשת קלט מהשחקן הנוכחי**:
      -  `while True:`:  לולאה אינסופית לבקשת קלט, עד שתוזן אפשרות תקינה.
      -  `try...except ValueError:`: מטפלת בשגיאות קלט אפשריות (אם המשתמש יזין ערך שאינו מספר).
      -  `attackType = int(input(f"Игрок {currentPlayer}, выберите силу удара (1-слабый, 2-средний, 3-сильный): "))`: מבקש מהשחקן הנוכחי לבחור את עוצמת המכה.
      -  `if attackType in [1, 2, 3]:`: בודק האם הקלט תקין (1, 2 או 3).
      - `break`: יוצא מלולאת בקשת הקלט כאשר הקלט תקין.
      - `else:`: מציג הודעה על קלט לא תקין, אם הוזן מספר שאינו 1, 2 או 3.
   - **יצירת מספר אקראי**:
      -  `randomNum = random.randint(1, 100)`: יוצר מספר אקראי מ-1 עד 100.
   - **בדיקת פגיעה והפחתת בריאות**:
      - `hit = False`: מאתחל את המשתנה hit (פגיעה) עם ערך False.
      - `if attackType == 1 and randomNum <= 80:`: בודק, אם נבחרה מכה חלשה והמספר האקראי קטן או שווה ל-80, המכה פוגעת במטרה.
      - `elif attackType == 2 and randomNum <= 60:`: בודק, אם נבחרה מכה בינונית והמספר האקראי קטן או שווה ל-60, המכה פוגעת במטרה.
      - `elif attackType == 3 and randomNum <= 40:`: בודק, אם נבחרה מכה חזקה והמספר האקראי קטן או שווה ל-40, המכה פוגעת במטרה.
      - `if hit:`: בודק האם המכה הייתה מוצלחת.
      - `print("Удар достиг цели!")`: מציג הודעה על הצלחה.
      -  מפחית את בריאות היריב בהתאם לשחקן הנוכחי.
      -  `else:`: אם המכה לא פגעה במטרה.
      -  `print("Удар не достиг цели.")`: מציג הודעה על כישלון.
   - **החלפת התור**:
      -   `currentPlayer = 3 - currentPlayer`: מחליף את השחקן הנוכחי (אם היה 1, הופך ל-2, ולהיפך).
4.  **קביעת המנצח**:
    -  `if player1Health <= 0:`: בודק, אם בריאות שחקן 1 הפכה ל-0 או פחות.
    - `print("Игрок 2 победил!")`: מציג הודעה על ניצחון שחקן 2.
    -   `else:`: אם בריאות שחקן 1 גדולה מ-0.
    -  `print("Игрок 1 победил!")`: מציג הודעה על ניצחון שחקן 1.
```
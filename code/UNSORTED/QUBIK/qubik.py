QUBIK:
=================
דרגת מורכבות: 7
-----------------
המשחק "קוביק" מדמה סיבוב של קובייה תלת-ממדית המורכבת מקוביות קטנות. השחקן מזין קואורדינטות (X, Y, Z) של אחת הקוביות הפנימיות, והתוכנית מציגה את מיקומה ביחס לאוריינטציה הנוכחית של הקובייה הגדולה. השחקן יכול גם לסובב את הקובייה סביב צירי ה-X, Y או Z.
מטרת המשחק היא להבין כיצד סיבוב הקובייה משפיע על הקואורדינטות היחסיות של הקובייה הפנימית שנבחרה.
-----------------
כללי המשחק:
1. בתחילת המשחק נוצרת קובייה בגודל 3x3x3.
2. השחקן מזין קואורדינטות (X, Y, Z) של קובייה פנימית, כאשר הקואורדינטות הן בטווח שבין 1 ל-3.
3. התוכנית מציגה את המיקום הנוכחי של הקובייה שהוזנה ביחס לאוריינטציה הנוכחית של הקובייה.
4. השחקן יכול לסובב את הקובייה ב-90 מעלות סביב ציר X, Y או Z.
5. לאחר כל סיבוב של הקובייה, התוכנית מציגה את הקואורדינטות החדשות של הקובייה.
6. המשחק נמשך עד שהשחקן מזין "END" (או 0) לסיום.
-----------------
אלגוריתם:
1. אתחול קובייה בגודל 3x3x3 (מיוצגת כמערך) והגדרת אוריינטציה התחלתית.
2. כל עוד לא הוזנה הפקודה "END":
    2.1 בקשת קואורדינטות X, Y, Z מהמשתמש.
    2.2 אם הוזנו קואורדינטות:
        2.2.1 הצגת המיקום הנוכחי של הקובייה.
    2.3 אם הוזנו הפקודות "X", "Y", "Z":
        2.3.1 ביצוע סיבוב של הקובייה בכיוון הציר המצוין.
    2.4. אם הוזנה הפקודה 0 או END, סיום המשחק.
3. סוף המשחק.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InitializeCube["<p align='left'>אתחול קובייה 3x3x3 ואוריינטציה התחלתית</p>"]
    InitializeCube --> MainLoop{"תחילת לולאה ראשית: כל עוד לא הוזן 'END'"}
    MainLoop -- Yes --> InputCommand["בקשה מהמשתמש: X, Y, Z או פקודה ('X', 'Y', 'Z') או 'END'"]
    InputCommand --> CheckEnd{"בדיקה: האם הוזן 'END'?"}
    CheckEnd -- Yes --> End["סוף"]
    CheckEnd -- No --> CheckCoordinates{"בדיקה: האם הוזנו קואורדינטות X, Y, Z?"}
    CheckCoordinates -- Yes --> OutputPosition["הצגת מיקום הקובייה הנוכחי"]
    OutputPosition --> MainLoop
    CheckCoordinates -- No --> CheckRotation{"בדיקה: האם הוזנה פקודת סיבוב 'X', 'Y' או 'Z'?"}
     CheckRotation -- Yes --> RotateCube["סיבוב הקובייה סביב הציר המצוין"]
    RotateCube --> MainLoop
    CheckRotation -- No --> MainLoop
    MainLoop -- No --> End
```

מקרא:
    Start - תחילת התוכנית.
    InitializeCube - אתחול מערך תלת-ממדי המייצג את הקובייה 3x3x3, והגדרת האוריינטציה ההתחלתית.
    MainLoop - תחילת הלולאה הראשית, הנמשכת כל עוד המשתמש לא מזין "END".
    InputCommand - בקשת קלט מהמשתמש של קואורדינטות X, Y, Z, או פקודת סיבוב X, Y, Z, או 'END'.
    CheckEnd - בדיקה האם הוזן 'END'.
    End - סוף התוכנית.
    CheckCoordinates - בדיקה האם הוזנו קואורדינטות X, Y, Z.
    OutputPosition - הצגת המיקום הנוכחי של הקובייה המצוינת לאחר הטרנספורמציות.
    CheckRotation - בדיקה האם הוזנה פקודת סיבוב 'X', 'Y' או 'Z'.
    RotateCube - ביצוע סיבוב הקובייה סביב הציר המצוין.
"""
import numpy as np

# Функция для поворота куба вокруг оси X
def rotate_x(cube):
    """
    מסובב את הקובייה ב-90 מעלות סביב ציר X.

    Args:
        cube (numpy.ndarray): מערך תלת-ממדי המייצג את הקובייה.

    Returns:
        numpy.ndarray: הקובייה לאחר הסיבוב.
    """
    return np.rot90(cube, k=-1, axes=(1, 2))

# Функция для поворота куба вокруг оси Y
def rotate_y(cube):
    """
        מסובב את הקובייה ב-90 מעלות סביב ציר Y.

        Args:
            cube (numpy.ndarray): מערך תלת-ממדי המייצג את הקובייה.

        Returns:
            numpy.ndarray: הקובייה לאחר הסיבוב.
        """
    return np.rot90(cube, k=-1, axes=(0, 2))

# Функция для поворота куба вокруг оси Z
def rotate_z(cube):
    """
            מסובב את הקובייה ב-90 מעלות סביב ציר Z.

            Args:
                cube (numpy.ndarray): מערך תלת-ממדי המייצג את הקובייה.

            Returns:
                numpy.ndarray: הקובייה לאחר הסיבוב.
            """
    return np.rot90(cube, k=-1, axes=(0, 1))

# Функция для получения положения кубика
def get_cube_position(cube, x, y, z):
    """
        מקבל את המיקום הנוכחי של קובייה בתוך הקובייה.

        Args:
            cube (numpy.ndarray): מערך תלת-ממדי המייצג את הקובייה.
            x (int): קואורדינטה של הקובייה על ציר X (מ-1 עד 3).
            y (int): קואורדינטה של הקובייה על ציר Y (מ-1 עד 3).
            z (int): קואורדינטה של הקובייה על ציר Z (מ-1 עד 3).

        Returns:
            tuple: קואורדינטות הקובייה באוריינטציה הנוכחית של הקובייה.
        """
    return cube[z-1, y-1, x-1]

# Инициализация куба 3x3x3
cube = np.arange(1, 28).reshape(3, 3, 3) # Создаем куб 3x3x3

# Игровой цикл
while True:
    command = input("Введите координаты (X Y Z) или команду ('X', 'Y', 'Z') или 'END' для выхода: ").upper()
    
    if command == "END" or command == "0":
        print("Игра завершена.")
        break
    
    parts = command.split()
    
    if len(parts) == 3:
        try:
            x, y, z = map(int, parts)
            if 1 <= x <= 3 and 1 <= y <= 3 and 1 <= z <= 3:
                pos = get_cube_position(cube, x, y, z)
                print(f"Положение кубика ({x}, {y}, {z}): {pos}")
            else:
                  print("Координаты должны быть целыми числами от 1 до 3.")
        except ValueError:
                print("Неверный формат координат.")
    elif len(parts) == 1:
        if command == "X":
            cube = rotate_x(cube)
            print("Куб повернут вокруг оси X.")
        elif command == "Y":
            cube = rotate_y(cube)
            print("Куб повернут вокруг оси Y.")
        elif command == "Z":
            cube = rotate_z(cube)
            print("Куб повернут вокруг оси Z.")
        else:
              print("Неизвестная команда.")
    else:
         print("Неверный формат ввода.")
         
"""
הסבר קוד:

1.  **ייבוא מודול `numpy`**:
    -  `import numpy as np`: מייבא את מודול `numpy`, המשמש לעבודה עם מערכים.
2.  **פונקציות סיבוב קובייה:**
    -   `rotate_x(cube)`:
        -   מקבלת מערך המייצג את הקובייה.
        -   משתמשת ב-`np.rot90()` לסיבוב הקובייה ב-90 מעלות נגד כיוון השעון סביב ציר X.
        -   מחזירה את הקובייה לאחר הסיבוב.
    -   `rotate_y(cube)`:
        -   בדומה ל-`rotate_x`, אך מסובבת סביב ציר Y.
    -   `rotate_z(cube)`:
        -   בדומה ל-`rotate_x`, אך מסובבת סביב ציר Z.
3.  **פונקציה `get_cube_position(cube, x, y, z)`**:
    -  מקבלת את הקובייה וקואורדינטות הקובייה הקטנה (x, y, z).
    -  מחזירה את הערך של הקובייה הקטנה בקואורדינטות המצוינות, תוך התחשבות באינדקס מערך (אינדקסים של מערך מתחילים ב-0, וקואורדינטות קובייה קטנה - ב-1).
4.  **אתחול הקובייה**:
    -  `cube = np.arange(1, 28).reshape(3, 3, 3)`: יוצר מערך תלת-ממדי (קובייה) בגודל 3x3x3, המאוכלס במספרים מ-1 עד 27.
5.  **לולאת המשחק הראשית `while True:`**:
    -  `command = input("Введите координаты (X Y Z) или команду ('X', 'Y', 'Z') или 'END' для выхода: ").upper()`: מבקש קלט מהמשתמש וממיר אותו לאותיות גדולות.
    -  **טיפול בפקודה "END"**:
        -   `if command == "END" or command == "0":`: בודק האם הוזנה הפקודה "END" או "0" לסיום המשחק.
        -  `break`: אם כן, הלולאה מסתיימת.
    -  **ניתוח הקלט**:
       -  `parts = command.split()`: מפצל את המחרוזת שהוזנה לחלקים לפי רווחים.
    -  **טיפול בקלט קואורדינטות (X Y Z)**:
        -  `if len(parts) == 3:`: בודק שהוזנו 3 אלמנטים, בציפייה לקואורדינטות X, Y, Z.
        -  `try...except ValueError`: מטפל בשגיאת קלט לא תקין.
        -   `x, y, z = map(int, parts)`: ממיר את החלקים שהוזנו למספרים שלמים.
        -  `if 1 <= x <= 3 and 1 <= y <= 3 and 1 <= z <= 3`: בודק שהקואורדינטות שהוזנו נמצאות בטווח שבין 1 ל-3.
        -   `pos = get_cube_position(cube, x, y, z)`: מקבל את מיקום הקובייה לפי הקואורדינטות.
        -  `print(f"Положение кубика ({x}, {y}, {z}): {pos}")`: מציג את מיקום הקובייה.
        -   `else`: אם הקואורדינטות מחוץ לטווח.
        -   `print("Координаты должны быть целыми числами от 1 до 3.")`: הצגת הודעת שגיאה.
    -  **טיפול בפקודות סיבוב ("X", "Y", "Z")**:
        -  `elif len(parts) == 1:`: בודק שהוזנה רק פקודה אחת.
        -  `if command == "X":`: בודק האם הוזנה הפקודה "X".
            -  `cube = rotate_x(cube)`: מסובב את הקובייה סביב ציר X.
            -  `print("Куб повернут вокруг оси X.")`: מציג הודעה על הסיבוב.
        -  `elif command == "Y":`: בדומה עבור ציר Y.
        -   `elif command == "Z":`: בדומה עבור ציר Z.
         -  `else`: אם הוזנה פקודה לא ידועה.
            - `print("Неизвестная команда.")`: הודעת שגיאה.
    -  `else:`: אם פורמט הקלט לא תקין.
        - `print("Неверный формат ввода.")`: הודעת שגיאה.
"""
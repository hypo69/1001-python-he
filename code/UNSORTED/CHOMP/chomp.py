"""
CHOMP:
=================
מורכבות: 5
-----------------
המשחק "CHOMP" הוא משחק לשני שחקנים, המשתמש בלוח מלבני המייצג טבלת שוקולד.
אחת הפינות (בדרך כלל השמאלית התחתונה) מייצגת "ריבוע" מורעל. השחקנים בתורם שוברים חתיכות מהטבלה, מבצעים מהלכים. מטרת המשחק היא להכריח את היריב לאכול את הריבוע המורעל.
השחקן שנאלץ לאכול את הריבוע המורעל - מפסיד.
כללי המשחק:
1.  שדה המשחק הוא טבלת שוקולד מלבנית.
2.  אחת הפינות (השמאלית התחתונה) נחשבת מורעלת.
3.  השחקנים בתורם נוגסים חלק מטבלת השוקולד.
4.  שחקן בוחר שורה ועמודה (נוגס חתיכת שוקולד).
5.  כל הריבועים מימין ומעל המיקום הנבחר מוסרים.
6.  המטרה - להכריח את היריב לאכול את הריבוע המורעל.
7.  השחקן שאוכל את הריבוע המורעל - מפסיד.
-----------------
אלגוריתם:
1.  תחילת המשחק.
2.  בקש מהמשתמש את גודל טבלת השוקולד (מספר השורות והעמודות).
3.  אתחל את לוח המשחק, המייצג את טבלת השוקולד.
4.  התחל את לולאת המשחק, עד שהמשחק יסתיים:
    4.1. הדפס את המצב הנוכחי של הלוח למסך.
    4.2. בקש מהשחקן הנוכחי את קואורדינטות החלק הננגס.
    4.3. עדכן את מצב הלוח, לאחר שנגסת את החלק הנבחר.
    4.4. בדוק אם השחקן הנוכחי לא אכל את הריבוע המורעל.
    4.5. אם אכל, הכרז על ניצחון השחקן היריב וסיים את המשחק.
    4.6. העבר את התור לשחקן הבא.
5. סוף המשחק.
-----------------

"""


def initialize_board(rows, cols):
    """
    מאתחל את לוח המשחק (טבלת השוקולד).

    ארגומנטים:
        rows (int): מספר השורות בלוח.
        cols (int): מספר העמודות בלוח.

    מחזיר:
        list of lists: רשימה של רשימות, המייצגת את לוח המשחק, כאשר 'X' - שוקולד, ' ' - מקום ריק.
    """
    board = [['X' for _ in range(cols)] for _ in range(rows)]
    return board

def display_board(board):
    """
    מדפיס את המצב הנוכחי של הלוח למסך.

    ארגומנטים:
        board (list of lists): לוח המשחק.
    """
    for row in board:
        print(' '.join(row))

def make_move(board, row_move, col_move):
    """
    מעדכן את מצב הלוח לאחר מהלך השחקן.
    כל הריבועים מימין ומעל המיקום הנבחר מוסרים.

    ארגומנטים:
        board (list of lists): לוח המשחק.
        row_move (int): השורה אליה השחקן ביצע את המהלך.
        col_move (int): העמודה אליה השחקן ביצע את המהלך.

    מחזיר:
        list of lists: לוח המשחק המעודכן.
    """
    rows = len(board)
    cols = len(board[0])
    for row in range(row_move, rows):
        for col in range(col_move, cols):
            board[row][col] = ' '
    return board


def is_game_over(board):
  """
    בודק האם המשחק הסתיים.
    המשחק מסתיים אם הריבוע המורעל (הפינה השמאלית העליונה) נאכל.

    ארגומנטים:
        board (list of lists): לוח המשחק.

    מחזיר:
        bool: True אם המשחק הסתיים, אחרת False.
    """
  return board[0][0] == ' '


def play_chomp():
    """
    הפונקציה הראשית של המשחק "CHOMP".
    מיישמת את תהליך המשחק, כולל קבלת גודל הלוח, הצגת הלוח,
    וטיפול במהלכי השחקנים.
    """
    while True:
        try:
            rows = int(input("Введите количество строк на доске: ")) # בקש מהמשתמש להזין מספר שורות
            cols = int(input("Введите количество столбцов на доске: ")) # בקש מהמשתמש להזין מספר עמודות
            if rows <= 0 or cols <= 0:
                 print("Размер доски должен быть положительным числом.") # גודל הלוח חייב להיות מספר חיובי
                 continue
            break
        except ValueError:
            print("Пожалуйста, введите целое число.") # אנא הזן מספר שלם

    board = initialize_board(rows, cols)
    current_player = 1  # Начинает первый игрок - השחקן הראשון מתחיל

    while True:
        print(f"\nХод игрока {current_player}:") # הדפס תור השחקן הנוכחי
        display_board(board)
        
        while True:
            try:
                row_move = int(input("Введите строку для откусывания (начиная с 0): ")) # בקש שורת מהלך
                col_move = int(input("Введите столбец для откусывания (начиная с 0): ")) # בקש עמודת מהלך
                if row_move < 0 or col_move < 0 or row_move >= rows or col_move >= cols:
                    print("Неверный ход. Укажите строку и столбец в пределах доски") # מהלך לא חוקי. ציין שורה ועמודה בתוך גבולות הלוח
                    continue
                if board[row_move][col_move] == ' ':
                     print("Вы не можете откусить пустое место. Попробуйте еще раз") # אינך יכול לנגוס מקום ריק. נסה שוב
                     continue
                break
            except ValueError:
                print("Пожалуйста, введите целые числа.") # אנא הזן מספרים שלמים
            except IndexError:
                print("Неверный ход. Укажите строку и столбец в пределах доски") # מהלך לא חוקי. ציין שורה ועמודה בתוך גבולות הלוח
            

        board = make_move(board, row_move, col_move)

        if is_game_over(board):
          print(f"Победил игрок {3-current_player}!") # הדפס מי ניצח (היריב של השחקן הנוכחי)
          display_board(board)
          break
        
        current_player = 3 - current_player # Переключаем игрока - החלף שחקן

if __name__ == "__main__":
    play_chomp()

"""
הסבר קוד:
1.  **אתחול**:
   -  `initialize_board(rows, cols)`: הפונקציה יוצרת לוח משחק בגודל `rows` x `cols`. בתחילה כל התאים מלאים בתו 'X', המייצג שוקולד.
2.  **הצגת הלוח**:
   -  `display_board(board)`: הפונקציה מדפיסה את המצב הנוכחי של הלוח למסך, מציגה כל שורה בנפרד.
3.  **מהלך השחקן**:
   -  `make_move(board, row_move, col_move)`: הפונקציה מעדכנת את הלוח, מסירה את כל ה-'X' מימין ומטה מהקואורדינטות שצוינו `row_move` ו-`col_move`, ומחליפה אותן ב-' '.
4.  **בדיקת סיום המשחק**:
    -  `is_game_over(board)`: בודקת האם הריבוע המורעל (הפינה השמאלית העליונה) נאכל. אם התא `board[0][0]` מכיל ' ', המשחק הסתיים.
5.  **הלוגיקה הראשית של המשחק `play_chomp()`**:
    -   מבקשת מהמשתמש את גודל הלוח.
    -   מאתחלת את הלוח.
    -   מארגנת לולאת משחק שבה השחקנים מבצעים מהלכים בתורם.
    -   מדפיסה את מצב הלוח הנוכחי.
    -   מבקשת מהשחקן להזין קואורדינטות למהלך.
    -   מעדכנת את הלוח לאחר המהלך.
    -   בודקת האם המשחק הסתיים.
    -   מדפיסה הודעה על ניצחון אחד מהשחקנים.
    -   מחליפה את השחקן הנוכחי.
6.  **טיפול בחריגים**:
   -   הקוד מכיל בלוקים `try-except` לטיפול בשגיאות קלט (ערכים לא מספריים).
7.  **הפעלת המשחק**:
   -  `if __name__ == "__main__":`: בלוק זה מבטיח שהפונקציה `play_chomp()` תופעל רק כאשר הסקריפט מופעל ישירות.
"""
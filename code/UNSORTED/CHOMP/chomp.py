CHOMP:
=================
רמת מורכבות: 5
-----------------
המשחק "CHOMP" הוא משחק לשני שחקנים, המשתמש בלוח מלבני המייצג לוח שוקולד.
אחת הפינות (בדרך כלל התחתונה-שמאלית) מייצגת "קובייה" מורעלת. שחקנים בתורם שוברים חתיכות מהלוח, מבצעים מהלכים. מטרת המשחק היא לאלץ את היריב לאכול את הקובייה המורעלת.
השחקן שנאלץ לאכול את הקובייה המורעלת מפסיד.
כללי המשחק:
1. שדה המשחק הוא לוח שוקולד מלבני.
2. אחת הפינות (התחתונה-שמאלית) נחשבת מורעלת.
3. שחקנים בתורם נוגסים חלק מלוח השוקולד.
4. שחקן בוחר שורה ועמודה (נוגס חתיכת שוקולד).
5. כל התאים מימין ומעל המיקום הנבחר מוסרים.
6. המטרה - לאלץ את היריב לאכול את הקובייה המורעלת.
7. השחקן שאוכל את הקובייה המורעלת מפסיד.
-----------------
אלגוריתם:
1. התחלת המשחק.
2. בקשת גודל לוח השוקולד (מספר שורות ועמודות) מהמשתמש.
3. אתחול לוח המשחק, המייצג את לוח השוקולד.
4. התחלת לולאת המשחק, עד שהמשחק יסתיים:
    4.1. הצגת מצב הלוח הנוכחי על המסך.
    4.2. בקשת קואורדינטות הנגיסה מהשחקן הנוכחי.
    4.3. עדכון מצב הלוח לאחר הנגיסה.
    4.4. בדיקה האם השחקן הנוכחי אכל את הקובייה המורעלת.
    4.5. אם כן, הכרזה על ניצחון השחקן היריב וסיום המשחק.
    4.6. העברת התור לשחקן הבא.
5. סיום המשחק.
-----------------

"""


def initialize_board(rows, cols):
    """
    מאתחל את שדה המשחק (לוח השוקולד).

    ארגומנטים:
        rows (int): מספר השורות בלוח.
        cols (int): מספר העמודות בלוח.

    מחזיר:
        list of lists: רשימת רשימות המייצגת את לוח המשחק, כאשר 'X' מייצג שוקולד, ו-' ' מייצג מקום ריק.
    """
    board = [['X' for _ in range(cols)] for _ in range(rows)]
    return board

def display_board(board):
    """
    מדפיס את מצב הלוח הנוכחי למסך.

    ארגומנטים:
        board (list of lists): לוח המשחק.
    """
    for row in board:
        print(' '.join(row))

def make_move(board, row_move, col_move):
    """
    מעדכן את מצב הלוח לאחר מהלך שחקן.
    כל התאים מימין ומעל המיקום הנבחר מוסרים.

    ארגומנטים:
        board (list of lists): לוח המשחק.
        row_move (int): השורה אליה השחקן ביצע את המהלך.
        col_move (int): העמודה אליה השחקן ביצע את המהלך.

    מחזיר:
        list of lists: לוח המשחק המעודכן.
    """
    rows = len(board)
    cols = len(board[0])
    for row in range(row_move, rows):
        for col in range(col_move, cols):
            board[row][col] = ' '
    return board


def is_game_over(board):
  """
    בודק האם המשחק הסתיים.
    המשחק מסתיים אם הקובייה המורעלת (הפינה השמאלית העליונה) נאכלה.

    ארגומנטים:
        board (list of lists): לוח המשחק.

    מחזיר:
        bool: True אם המשחק הסתיים, אחרת False.
    """
  return board[0][0] == ' '


def play_chomp():
    """
    הפונקציה הראשית של המשחק "CHOMP".
    מממשת את תהליך המשחק, כולל קלט גודל הלוח, הצגת הלוח,
    וטיפול במהלכי שחקנים.
    """
    while True:
        try:
            rows = int(input("Введите количество строк на доске: ")) # בקשת קלט - מספר שורות
            cols = int(input("Введите количество столбцов на доске: ")) # בקשת קלט - מספר עמודות
            if rows <= 0 or cols <= 0:
                 print("Размер доски должен быть положительным числом.") # הודעת שגיאה - גודל חייב להיות חיובי
                 continue
            break
        except ValueError:
            print("Пожалуйста, введите целое число.") # הודעת שגיאה - קלט אינו מספר שלם

    board = initialize_board(rows, cols)
    current_player = 1  # השחקן הראשון מתחיל

    while True:
        print(f"\nХод игрока {current_player}:") # הצגת תור השחקן הנוכחי
        display_board(board)

        while True:
            try:
                row_move = int(input("Введите строку для откусывания (начиная с 0): ")) # בקשת קלט - שורת מהלך
                col_move = int(input("Введите столбец для откусывания (начиная с 0): ")) # בקשת קלט - עמודת מהלך
                if row_move < 0 or col_move < 0 or row_move >= rows or col_move >= cols:
                    print("Неверный ход. Укажите строку и столбец в пределах доски") # הודעת שגיאה - קואורדינטות מחוץ ללוח
                    continue
                if board[row_move][col_move] == ' ':
                     print("Вы не можете откусить пустое место. Попробуйте еще раз") # הודעת שגיאה - ניסיון לנשוך מקום ריק
                     continue
                break
            except ValueError:
                print("Пожалуйста, введите целые числа.") # הודעת שגיאה - קלט אינו מספר שלם
            except IndexError: # This except is actually redundant because of the check above, but keeping it as in original structure
                print("Неверный ход. Укажите строку и столбец в пределах доски") # הודעת שגיאה - קואורדינטות מחוץ ללוח (כפילות)


        board = make_move(board, row_move, col_move)

        if is_game_over(board):
          print(f"Победил игрок {3-current_player}!") # הצגת הודעת ניצחון (השחקן האחר ניצח)
          display_board(board) # הצגת הלוח הסופי
          break

        current_player = 3 - current_player # מחליפים שחקן

if __name__ == "__main__":
    play_chomp()

"""
הסבר קוד:
1. **אתחול**:
   - `initialize_board(rows, cols)`: פונקציה היוצרת לוח משחק בגודל `rows` x `cols`. בתחילה, כל התאים ממולאים בתו 'X', המייצג שוקולד.
2. **הצגת לוח**:
   - `display_board(board)`: פונקציה המדפיסה את מצב הלוח הנוכחי למסך, מציגה כל שורה בנפרד.
3. **מהלך שחקן**:
   - `make_move(board, row_move, col_move)`: פונקציה המעדכנת את הלוח, מסירה את כל ה-'X' מימין ומתחת לקואורדינטות שצוינו (`row_move` ו-`col_move`), ומחליפה אותם ב-' '.
4. **בדיקת סיום משחק**:
    - `is_game_over(board)`: בודקת האם הקובייה המורעלת (הפינה השמאלית העליונה) נאכלה. אם התא `board[0][0]` מכיל ' ', אזי המשחק הסתיים.
5. **לוגיקת המשחק הראשית `play_chomp()`**:
    - מבקשת מהמשתמש את מימדי הלוח.
    - מאתחלת את הלוח.
    - מארגנת את לולאת המשחק, בה השחקנים מבצעים מהלכים בתורם.
    - מציגה את מצב הלוח הנוכחי.
    - מבקשת מהשחקן קלט של קואורדינטות המהלך.
    - מעדכנת את הלוח לאחר המהלך.
    - בודקת האם המשחק הסתיים.
    - מציגה הודעה על ניצחון אחד השחקנים.
    - מחליפה את השחקן הנוכחי.
6. **טיפול בחריגות**:
   - הקוד מכיל בלוקים של `try-except` לטיפול בשגיאות קלט (ערכים שאינם מספרים).
7. **הפעלת המשחק**:
   - `if __name__ == "__main__":`: בלוק זה מבטיח שהפונקציה `play_chomp()` תופעל רק בעת הרצת הסקריפט ישירות.
"""
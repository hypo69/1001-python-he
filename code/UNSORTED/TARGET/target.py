HORSES:
=================
קושי: 6
-----------------
המשחק "מרוץ סוסים" הוא סימולציה של מרוץ המערב מספר סוסים. השחקן מבצע הימור על אחד מהסוסים, ולאחר מכן מתחיל המרוץ. הסוסים נעים באקראי בכל צעד, והסוס שמגיע ראשון לקו הגמר מנצח. השחקן מנצח אם הסוס שלו הגיע ראשון.

כללי המשחק:
1. לשחקן מוצע לבצע הימור על אחד מהסוסים (מ-1 עד 7).
2. לאחר בחירת הסוס, המירוץ מתחיל.
3. בכל צעד של המירוץ, כל סוס מתקדם קדימה במספר אקראי של עמדות (מ-1 עד 3).
4. המירוץ נמשך עד אשר אחד מהסוסים מגיע לקו הגמר (עמדה 75).
5. אם הסוס שעליו הימר השחקן מנצח, השחקן מנצח.

-----------------
אלגוריתם:
1. לאתחל מערך של עמדות הסוסים, כל הסוסים בנקודת הזינוק (עמדה 0).
2. לבקש מהשחקן את מספר הסוס שעליו הוא מבצע את ההימור (מ-1 עד 7).
3. להתחיל לולאה "כל עוד אף אחד מהסוסים לא הגיע לקו הגמר (עמדה 75)":
   3.1 עבור כל סוס:
      3.1.1 לייצר מספר אקראי מ-1 עד 3 (בכמה עמדות הסוס מתקדם).
      3.1.2 להגדיל את עמדת הסוס בערך שנוצר.
   3.2 להציג על המסך את העמדה הנוכחית של כל סוס.
4. לקבוע את מספר הסוס המנצח (שהגיע ראשון לקו הגמר).
5. להציג על המסך את מספר הסוס המנצח.
6. אם מספר הסוס המנצח זהה להימור של השחקן, אז להציג הודעה "ניצחת!". אחרת "הפסדת!".
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InitializeHorses["אתחול עמדות סוסים: <code><b>horsePositions = [0, 0, 0, 0, 0, 0, 0]</b></code>"]
    InitializeHorses --> InputBet["קלט הימור השחקן: <code><b>userBet</b></code>"]
    InputBet --> RaceStart{"תחילת המרוץ: כל עוד אף סוס לא הגיע לקו הגמר (75)"}
    RaceStart -- כן --> LoopHorses{"עבור כל סוס"}
    LoopHorses --> GenerateMove["לייצר היסט אקראי (1-3)"]
    GenerateMove --> UpdatePosition["לעדכן עמדת סוס"]
    UpdatePosition --> CheckFinish{"<code><b>position >= 75?</b></code>"}
    CheckFinish -- כן --> FinishRace["לקבוע מנצח"]
    CheckFinish -- לא --> OutputPositions["להציג עמדות סוסים נוכחיות"]
    OutputPositions --> RaceStart
    LoopHorses --> CheckFinish
    FinishRace --> OutputWinner["להציג מספר מנצח"]
    OutputWinner --> CheckWin["בדיקה: <code><b>userBet == winnerHorse?</b></code>"]
    CheckWin -- כן --> OutputWin["הצגת הודעה: <b>ניצחת!</b>"]
    CheckWin -- לא --> OutputLose["הצגת הודעה: <b>הפסדת!</b>"]
    OutputWin --> End["סוף"]
     OutputLose --> End
     RaceStart -- לא --> End
```

מקרא:
    Start - התחלת המשחק.
    InitializeHorses - אתחול מערך horsePositions (עמדות סוסים) באפסים, המייצגים את נקודת הזינוק.
    InputBet - בקשת מספר הסוס שעליו השחקן מהמר.
    RaceStart - תחילת לולאת המרוץ, הנמשכת כל עוד אף אחד מהסוסים לא מגיע לקו הגמר.
    LoopHorses - תחילת לולאה העוברת על כל הסוסים.
    GenerateMove - יצירת מספר אקראי (1-3) לקביעת היסט הסוס.
    UpdatePosition - עדכון העמדה הנוכחית של הסוס בהתאם להיסט שנוצר.
    CheckFinish - בדיקה האם הסוס הגיע לקו הגמר (עמדה 75).
    OutputPositions - הצגת העמדות הנוכחיות של כל הסוסים.
    FinishRace - קביעת מנצח המרוץ (הסוס הראשון שהגיע לקו הגמר).
    OutputWinner - הצגת מספר הסוס המנצח.
    CheckWin - בדיקה האם הימור השחקן זהה למספר הסוס המנצח.
    OutputWin - הצגת ההודעה "ניצחת!", אם ההימור זהה למספר המנצח.
    OutputLose - הצגת ההודעה "הפסדת!", אם ההימור אינו זהה למספר המנצח.
    End - סוף המשחק.
"""

import random

# 1. אתחול מערך עמדות סוסים (כל הסוסים בנקודת הזינוק - עמדה 0)
horsePositions = [0, 0, 0, 0, 0, 0, 0]

# 2. בקשת מספר הסוס שעליו השחקן מבצע את ההימור (מ-1 עד 7)
while True:
    try:
        userBet = int(input("בצע הימור על סוס (1-7): "))
        if 1 <= userBet <= 7:
            break  # יציאה מהלולאה אם הקלט תקין
        else:
            print("אנא הזן מספר בין 1 ל-7.")
    except ValueError:
        print("אנא הזן מספר שלם.")


# 3. לולאת המרוץ הראשית, כל עוד אף אחד מהסוסים לא הגיע לקו הגמר (עמדה 75)
while max(horsePositions) < 75:
    # 3.1 עבור כל סוס
    for horse in range(len(horsePositions)):
        # 3.1.1 לייצר מספר אקראי מ-1 עד 3 (בכמה עמדות הסוס מתקדם)
        move = random.randint(1, 3)
        # 3.1.2 להגדיל את עמדת הסוס בערך שנוצר
        horsePositions[horse] += move

    # 3.2 להציג על המסך את העמדה הנוכחית של כל סוס (להמחשה)
    for i, position in enumerate(horsePositions):
        print(f"סוס {i + 1}: {'=' * position}{i + 1}")
    print("-" * 30)
# 4. קביעת מספר הסוס המנצח (הראשון שהגיע לקו הגמר)
winnerHorse = horsePositions.index(max(horsePositions)) + 1
# 5. הצגת מספר הסוס המנצח על המסך
print(f"סוס מס' {winnerHorse} ניצח!")

# 6. אם מספר הסוס המנצח זהה להימור של השחקן, אז להציג הודעה "ניצחת!". אחרת "הפסדת!".
if winnerHorse == userBet:
    print("ניצחת!")
else:
    print("הפסדת!")
"""
הסבר הקוד:
1.  **ייבוא המודול `random`**:
   -  `import random`: מייבא את המודול `random`, המשמש ליצירת מספרים אקראיים המגדירים את תנועת הסוסים.

2.  **אתחול משתנים**:
    - `horsePositions = [0, 0, 0, 0, 0, 0, 0]`: יוצר רשימה (מערך) `horsePositions`, המייצגת את עמדות שבעת הסוסים בנקודת הזינוק של המרוץ. העמדה ההתחלתית של כל הסוסים היא 0.

3.  **קלט הימור השחקן**:
    -   `while True:`: לולאה אינסופית הנמשכת עד שהשחקן מזין הימור תקין.
    -   `try...except ValueError:`: בלוק try-except מטפל בשגיאות קלט אפשריות, אם המשתמש מזין ערך שאינו מספר שלם.
    -   `userBet = int(input("בצע הימור על סוס (1-7): "))`: מבקש מהמשתמש את מספר הסוס (מ-1 עד 7) שעליו הוא מהמר, וממיר את הקלט למספר שלם.
    -   `if 1 <= userBet <= 7:`: בודק האם המספר שהוזן הוא מספר סוס תקין.
    -   `break`: יציאה מהלולאה אם הקלט תקין.
    -   `print("אנא הזן מספר בין 1 ל-7.")`: מציג הודעת שגיאה אם המספר שהוזן אינו תקין.

4.  **לולאת המשחק הראשית**:
    -   `while max(horsePositions) < 75:`: הלולאה נמשכת כל עוד העמדה המקסימלית בין כל הסוסים לא הגיעה ל-75 (קו הגמר).
    -   `for horse in range(len(horsePositions)):`: לולאה העוברת על כל סוס ברשימה `horsePositions`.
    -   `move = random.randint(1, 3)`: מייצר מספר שלם אקראי מ-1 עד 3, המייצג את ההיסט האקראי של הסוס הנוכחי.
    -   `horsePositions[horse] += move`: מגדיל את עמדת הסוס הנוכחי בהיסט שנוצר.
    -   `for i, position in enumerate(horsePositions):`: לולאה העוברת על עמדות כל הסוסים כדי להציג את העמדה הנוכחית של כל סוס.
    -   `print(f"סוס {i + 1}: {'=' * position}{i + 1}")`: מציג את העמדה הנוכחית של כל סוס בצורת מחרוזת, כאשר סימני `=` מייצגים את המרחק שעבר.
    -   `print("-" * 30)`: מציג קו מפריד בין שלבי המרוץ.

5.  **קביעת המנצח**:
    -   `winnerHorse = horsePositions.index(max(horsePositions)) + 1`: מוצא את האינדקס (מספר) של הסוס עם העמדה המקסימלית (המנצח) ומוסיף 1, מכיוון שמספור הסוסים מתחיל מ-1.
    -   `print(f"סוס מס' {winnerHorse} ניצח!")`: מציג את מספר הסוס המנצח.

6.  **קביעת תוצאת המשחק**:
    -   `if winnerHorse == userBet:`: בודק האם מספר הסוס המנצח זהה להימור השחקן.
    -   `print("ניצחת!")`: מציג הודעה על ניצחון אם ההימור זהה למספר המנצח.
    -   `else: print("הפסדת!")`: מציג הודעה על הפסד אם ההימור אינו זהה למספר המנצח.
"""
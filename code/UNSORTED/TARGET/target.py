"""
סוּסִים:
=================
רמת קושי: 6
-----------------
המשחק "מרוץ סוסים" הוא סימולציה של מרוץ הכולל מספר סוסים. השחקן מבצע הימור על אחד הסוסים, ולאחר מכן מתחיל המרוץ. הסוסים מתקדמים באופן אקראי בכל צעד, והסוס שמגיע ראשון לקו הסיום הוא המנצח. השחקן מנצח אם הסוס שעליו הימר הגיע ראשון.

חוקי המשחק:
1. לשחקן מוצע לבצע הימור על אחד הסוסים (מ-1 עד 7).
2. לאחר בחירת הסוס מתחיל המרוץ.
3. בכל צעד של המרוץ, כל סוס מתקדם קדימה במספר אקראי של מיקומים (מ-1 עד 3).
4. המרוץ נמשך עד שאחד הסוסים מגיע לקו הסיום (מיקום 75).
5. אם הסוס שעליו הימר השחקן מנצח, השחקן מנצח.

-----------------
אלגוריתם:
1. אתחול מערך מיקומי הסוסים, כל הסוסים בנקודת ההתחלה (מיקום 0).
2. בקשת מספר הסוס מהשחקן שעליו הוא מהמר (מ-1 עד 7).
3. התחלת לולאה "כל עוד אף אחד מהסוסים לא הגיע לקו הסיום (מיקום 75)":
   3.1 עבור כל סוס:
      3.1.1 יצירת מספר אקראי מ-1 עד 3 (בכמה מיקומים מתקדם הסוס).
      3.1.2 הגדלת מיקום הסוס בערך שנוצר אקראית.
   3.2 הדפסת המיקום הנוכחי של כל סוס למסך.
4. קביעת מספר הסוס המנצח (שהגיע ראשון לקו הסיום).
5. הדפסת מספר הסוס המנצח למסך.
6. אם מספר הסוס המנצח תואם את הימור השחקן, הדפסת הודעה "ניצחת!". אחרת "הפסדת!".
-----------------
דיאגרמת זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InitializeHorses["אתחול מיקומי הסוסים: <code><b>horsePositions = [0, 0, 0, 0, 0, 0, 0]</b></code>"]
    InitializeHorses --> InputBet["קליטת הימור השחקן: <code><b>userBet</b></code>"]
    InputBet --> RaceStart{"התחלת המרוץ: כל עוד אף סוס לא הגיע לקו הסיום (75)"}
    RaceStart -- כן --> LoopHorses{"עבור כל סוס"}
    LoopHorses --> GenerateMove["יצירת היסט אקראי (1-3)"]
    GenerateMove --> UpdatePosition["עדכון מיקום הסוס"]
    UpdatePosition --> CheckFinish{"<code><b>position >= 75?</b></code>"}
    CheckFinish -- כן --> FinishRace["קביעת המנצח"]
    CheckFinish -- לא --> OutputPositions["הדפסת מיקומי הסוסים הנוכחיים"]
    OutputPositions --> RaceStart
    LoopHorses --> CheckFinish
    FinishRace --> OutputWinner["הדפסת מספר המנצח"]
    OutputWinner --> CheckWin["בדיקה: <code><b>userBet == winnerHorse?</b></code>"]
    CheckWin -- כן --> OutputWin["הדפסת הודעה: <b>ניצחת!</b>"]
    CheckWin -- לא --> OutputLose["הדפסת הודעה: <b>הפסדת!</b>"]
    OutputWin --> End["סוף"]
     OutputLose --> End
     RaceStart -- לא --> End
```

מקרא:
    Start - התחלת המשחק.
    InitializeHorses - אתחול מערך horsePositions (מיקומי סוסים) באפסים, המייצג את נקודת ההתחלה.
    InputBet - בקשה מהשחקן למספר הסוס שעליו הוא מהמר.
    RaceStart - תחילת לולאת המרוץ, הנמשכת כל עוד אף סוס לא הגיע לקו הסיום.
    LoopHorses - תחילת לולאה הסוקרת את כל הסוסים.
    GenerateMove - יצירת מספר אקראי (1-3) לקביעת היסט הסוס.
    UpdatePosition - עדכון המיקום הנוכחי של הסוס בהתאם להיסט שנוצר.
    CheckFinish - בדיקה האם הסוס הגיע לקו הסיום (מיקום 75).
    OutputPositions - הדפסת המיקומים הנוכחיים של כל הסוסים.
    FinishRace - קביעת מנצח המרוץ (הסוס הראשון שהגיע לקו הסיום).
    OutputWinner - הדפסת מספר הסוס המנצח.
    CheckWin - בדיקה האם הימור השחקן תואם את מספר הסוס המנצח.
    OutputWin - הדפסת הודעה "ניצחת!", אם ההימור תואם את מספר המנצח.
    OutputLose - הדפסת הודעה "הפסדת!", אם ההימור אינו תואם את מספר המנצח.
    End - סיום המשחק.
"""

import random

# 1. אתחול מערך מיקומי הסוסים (כל הסוסים בנקודת ההתחלה - מיקום 0)
horsePositions = [0, 0, 0, 0, 0, 0, 0]

# 2. בקשה מהשחקן למספר הסוס שעליו הוא מהמר (מ-1 עד 7)
while True:
    try:
        userBet = int(input("בצע הימור על סוס (1-7): "))
        if 1 <= userBet <= 7:
            break  # יציאה מהלולאה אם הקלט תקין
        else:
            print("אנא הזן מספר בין 1 ל-7.")
    except ValueError:
        print("אנא הזן מספר שלם.")


# 3. לולאת המשחק הראשית, כל עוד אף אחד מהסוסים לא הגיע לקו הסיום (מיקום 75)
while max(horsePositions) < 75:
    # 3.1 עבור כל סוס
    for horse in range(len(horsePositions)):
        # 3.1.1 יצירת מספר אקראי מ-1 עד 3 (בכמה מיקומים מתקדם הסוס)
        move = random.randint(1, 3)
        # 3.1.2 הגדלת מיקום הסוס בערך שנוצר אקראית
        horsePositions[horse] += move

    # 3.2 הדפסת המיקום הנוכחי של כל סוס למסך (לשם הדגמה ויזואלית)
    for i, position in enumerate(horsePositions):
        print(f"סוס {i + 1}: {'=' * position}{i + 1}")
    print("-" * 30)
# 4. קביעת מספר הסוס המנצח (שהגיע ראשון לקו הסיום)
winnerHorse = horsePositions.index(max(horsePositions)) + 1
# 5. הדפסת מספר הסוס המנצח למסך
print(f"הסוס המנצח הוא מס' {winnerHorse}!")

# 6. אם מספר הסוס המנצח תואם את הימור השחקן, הדפסת הודעה "ניצחת!". אחרת "הפסדת!".
if winnerHorse == userBet:
    print("ניצחת!")
else:
    print("הפסדת!")
"""
הסבר הקוד:
1.  **ייבוא המודול `random`**:
   -  `import random`: מייבא את המודול `random`, המשמש ליצירת מספרים אקראיים הקובעים את תנועת הסוסים.

2.  **אתחול משתנים**:
    - `horsePositions = [0, 0, 0, 0, 0, 0, 0]`: יוצר רשימה (מערך) `horsePositions`, המייצגת את מיקומי שבעת הסוסים בנקודת ההתחלה של המרוץ. המיקום ההתחלתי של כל הסוסים הוא 0.

3.  **קליטת הימור השחקן**:
    -   `while True:`: לולאה אינסופית הנמשכת כל עוד השחקן לא הזין הימור תקין.
    -   `try...except ValueError:`: בלוק try-except מטפל בשגיאות קלט אפשריות, אם המשתמש יזין ערך שאינו מספר שלם.
    -   `userBet = int(input("בצע הימור על סוס (1-7): "))`: מבקש מהמשתמש את מספר הסוס (1 עד 7) שעליו הוא מהמר, וממיר את הקלט למספר שלם.
    -   `if 1 <= userBet <= 7:`: בודק האם המספר שהוזן הוא מספר סוס תקין.
    -   `break`: יציאה מהלולאה אם הקלט תקין.
    -   `print("אנא הזן מספר בין 1 ל-7.")`: מדפיס הודעת שגיאה אם המספר שהוזן אינו תקין.
    -   `print("אנא הזן מספר שלם.")`: מדפיס הודעת שגיאה אם הוזן ערך שאינו מספר שלם.

4.  **לולאת המשחק הראשית**:
    -   `while max(horsePositions) < 75:`: הלולאה נמשכת כל עוד המיקום המקסימלי מבין כל הסוסים לא מגיע ל-75 (קו הסיום).
    -   `for horse in range(len(horsePositions)):`: הלולאה עוברת על כל סוס ברשימה `horsePositions`.
    -   `move = random.randint(1, 3)`: יוצר מספר שלם אקראי בין 1 ל-3, המייצג את ההיסט האקראי של הסוס הנוכחי.
    -   `horsePositions[horse] += move`: מגדיל את מיקום הסוס הנוכחי בערך ההיסט שנוצר.
    -   `for i, position in enumerate(horsePositions):`: הלולאה עוברת על מיקומי כל הסוסים על מנת להדפיס את המיקום הנוכחי של כל סוס.
    -   `print(f"סוס {i + 1}: {'=' * position}{i + 1}")`: מדפיס את המיקום הנוכחי של כל סוס כמחרוזת, כאשר הסימן `=` מייצג את המרחק שעבר.
    -   `print("-" * 30)`: מדפיס קו מפריד בין צעדי המרוץ.

5.  **קביעת המנצח**:
    -   `winnerHorse = horsePositions.index(max(horsePositions)) + 1`: מוצא את האינדקס (מספר) הסוס עם המיקום המקסימלי (המנצח) ומוסיף 1, מכיוון שמספור הסוסים מתחיל מ-1.
    -   `print(f"הסוס המנצח הוא מס' {winnerHorse}!")`: מדפיס את מספר הסוס המנצח.

6.  **קביעת תוצאת המשחק**:
    -   `if winnerHorse == userBet:`: בודק האם מספר הסוס המנצח תואם את הימור השחקן.
    -   `print("ניצחת!")`: מדפיס הודעה על ניצחון אם ההימור תואם את מספר המנצח.
    -   `else: print("הפסדת!")`: מדפיס הודעה על הפסד אם ההימור אינו תואם את מספר המנצח.
"""
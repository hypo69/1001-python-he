<3D PLOT>:
=================
מורכבות: 7
-----------------
המשחק "3D PLOT" הוא תוכנה המשרטטת גרפים תלת-ממדיים של הפונקציה Z = F(X, Y). המשתמש מזין ערכים עבור X ו-Y, והתוכנה מחשבת את ערך Z המתאים, ולאחר מכן מדמיינת את הנקודה במרחב תלת-ממדי. המשתמש יכול להגדיר גבולות עבור X ו-Y, וכן את הצעד (מרווח הגידול) שבו ישתנו ערכי X ו-Y בלולאה.
חוקי המשחק:
1. המשתמש מזין את הערכים המינימלי והמקסימלי עבור X, וכן את מרווח הגידול (הצעד) עבור X.
2. המשתמש מזין את הערכים המינימלי והמקסימלי עבור Y, וכן את מרווח הגידול (הצעד) עבור Y.
3. התוכנה מחשבת את ערך Z לפי הנוסחה Z = X * X - Y * Y ומציגה את הנקודה (X, Y, Z) במרחב תלת-ממדי.
4. התוכנה עוברת על כל ערכי X ו-Y בטווחים המוגדרים עם הצעדים המוגדרים, מחשבת ומציגה את ערכי Z המתאימים.
5. התוכנה מסתיימת לאחר שכל הנקודות שורטטו.
-----------------
אלגוריתם:
1. קלט: בקש מהמשתמש את ערך X המינימלי (Xmin), ערך X המקסימלי (Xmax) ומרווח הגידול של X (Xincrement).
2. קלט: בקש מהמשתמש את ערך Y המינימלי (Ymin), ערך Y המקסימלי (Ymax) ומרווח הגידול של Y (Yincrement).
3. לולאה על X: התחל לולאה שבה X משתנה מ-Xmin עד Xmax בצעדים של Xincrement.
   3.1. לולאה על Y: התחל לולאה מקוננת שבה Y משתנה מ-Ymin עד Ymax בצעדים של Yincrement.
        3.1.1 חישוב Z: חשב את Z כ-Z = X*X - Y*Y.
        3.1.2 פלט: הצג את הערכים X, Y ו-Z.
   3.2. סוף לולאה על Y.
4. סוף לולאה על X.
5. סוף התוכנה.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["Начало"] --> InputXMin["Ввод: <code><b>Xmin</b></code>"]
    InputXMin --> InputXMax["Ввод: <code><b>Xmax</b></code>"]
    InputXMax --> InputXIncrement["Ввод: <code><b>Xincrement</b></code>"]
    InputXIncrement --> InputYMin["Ввод: <code><b>Ymin</b></code>"]
    InputYMin --> InputYMax["Ввод: <code><b>Ymax</b></code>"]
    InputYMax --> InputYIncrement["Ввод: <code><b>Yincrement</b></code>"]
    InputYIncrement --> LoopXStart{"Начало цикла по X: <code><b>X = Xmin, пока X <= Xmax</b></code>"}
    LoopXStart -- Да --> LoopYStart{"Начало цикла по Y: <code><b>Y = Ymin, пока Y <= Ymax</b></code>"}
    LoopYStart -- Да --> CalculateZ["<code><b>Z = X*X - Y*Y</b></code>"]
    CalculateZ --> OutputXYZ["Вывод: <code><b>X, Y, Z</b></code>"]
    OutputXYZ --> LoopYEnd{"Конец цикла по Y"}
    LoopYEnd -- Да --> LoopYStart
    LoopYEnd -- Нет --> LoopXEnd{"Конец цикла по X"}
    LoopXEnd -- Да --> LoopXStart
    LoopXEnd -- Нет --> End["Конец"]
```
מקרא:
    Start - תחילת התוכנה.
    InputXMin - בקשת המשתמש והזנת הערך המינימלי עבור X (Xmin).
    InputXMax - בקשת המשתמש והזנת הערך המקסימלי עבור X (Xmax).
    InputXIncrement - בקשת המשתמש והזנת מרווח הגידול עבור X (Xincrement).
    InputYMin - בקשת המשתמש והזנת הערך המינימלי עבור Y (Ymin).
    InputYMax - בקשת המשתמש והזנת הערך המקסימלי עבור Y (Ymax).
    InputYIncrement - בקשת המשתמש והזנת מרווח הגידול עבור Y (Yincrement).
    LoopXStart - תחילת הלולאה על X, הנמשכת כל עוד X <= Xmax.
    LoopYStart - תחילת הלולאה המקוננת על Y, הנמשכת כל עוד Y <= Ymax.
    CalculateZ - חישוב ערך Z לפי הנוסחה Z = X * X - Y * Y.
    OutputXYZ - הצגת ערכי X, Y ו-Z.
    LoopYEnd - סוף הלולאה על Y. אם הלולאה על Y לא הסתיימה, חוזרים לתחילת הלולאה LoopYStart.
    LoopXEnd - סוף הלולאה על X. אם הלולאה על X לא הסתיימה, חוזרים לתחילת הלולאה LoopXStart.
    End - סוף התוכנה.
```
```python


# Ввод данных от пользователя
try:
    Xmin = float(input("Введите минимальное значение X: "))
    Xmax = float(input("Введите максимальное значение X: "))
    Xincrement = float(input("Введите приращение для X: "))
    
    Ymin = float(input("Введите минимальное значение Y: "))
    Ymax = float(input("Введите максимальное значение Y: "))
    Yincrement = float(input("Введите приращение для Y: "))
except ValueError:
    print("Ошибка: Пожалуйста, введите числовые значения.")
    exit()

# Внешний цикл по X
X = Xmin
while X <= Xmax:
    # Внутренний цикл по Y
    Y = Ymin
    while Y <= Ymax:
        # Вычисление Z по формуле Z = X*X - Y*Y
        Z = X * X - Y * Y
        # Вывод координат X, Y и значения Z
        print(f"X: {X}, Y: {Y}, Z: {Z}")
        # Увеличение Y на заданное приращение
        Y += Yincrement
    # Увеличение X на заданное приращение
    X += Xincrement
    
"""
הסבר קוד:
1.  **בלוק קלט נתונים:**
   -  `try...except ValueError`: בלוק זה מטפל בחריגות שעשויות להתרחש אם המשתמש יזין נתונים לא תקינים (לדוגמה, לא מספר).
   -  `Xmin = float(input("Введите минимальное значение X: "))`: מבקש את הערך המינימלי עבור ציר X וממיר אותו למספר עם נקודה עשרונית.
   -  שורות דומות מבקשות וממירות ערכים עבור `Xmax`, `Xincrement`, `Ymin`, `Ymax` ו-`Yincrement`.
   -  `exit()`: אם מתרחשת שגיאת קלט, התוכנה מסתיימת.

2.  **לולאה חיצונית `while X <= Xmax:`**:
    -   מגדירה את הערך ההתחלתי של `X` שווה ל-`Xmin`.
    -   הלולאה מתבצעת כל עוד הערך של `X` אינו עולה על `Xmax`.
    -   `X` גדל ב-`Xincrement` בסוף כל איטרציה של הלולאה.

3.  **לולאה פנימית `while Y <= Ymax:`**:
    -   מגדירה את הערך ההתחלתי של `Y` שווה ל-`Ymin` בתחילת כל איטרציה של הלולאה החיצונית.
    -   הלולאה מתבצעת כל עוד הערך של `Y` אינו עולה על `Ymax`.
    -   `Y` גדל ב-`Yincrement` בסוף כל איטרציה של הלולאה הפנימית.

4.  **חישוב `Z`**:
   -   `Z = X * X - Y * Y`: מחשב את הערך של `Z` לפי הנוסחה הנתונה.

5.  **הצגת קואורדינטות**:
   -   `print(f"X: {X}, Y: {Y}, Z: {Z}")`: מציג את הערכים של `X`, `Y` ואת `Z` שחושב עבור כל נקודה.

6.  **הגדלת `X` ו-`Y`**:
    -  `Y += Yincrement`: מגדיל את הערך של `Y` במרווח הגידול הנתון `Yincrement` בסוף כל איטרציה של הלולאה הפנימית.
    - `X += Xincrement`: מגדיל את הערך של `X` במרווח הגידול הנתון `Xincrement` בסוף כל איטרציה של הלולאה החיצונית.

בכך, התוכנה בונה גרף תלת-ממדי של הפונקציה Z = X^2 - Y^2, על ידי מעבר על ערכי X ו-Y בטווחים הנתונים עם מרווחי גידול נתונים והצגת הקואורדינטות של הנקודות (X, Y, Z).
"""
```
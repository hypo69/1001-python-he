<תרשים תלת-ממדי>:
=================
מורכבות: 7
-----------------
תוכנית "תרשים תלת-ממדי" היא תוכנה המשרטטת גרפים תלת-ממדיים של הפונקציה Z = F(X, Y). המשתמש מזין ערכים עבור X ו-Y, והתוכנה מחשבת את ערך Z המתאים, ולאחר מכן ממחישה את הנקודה במרחב תלת-ממדי. המשתמש יכול להגדיר גבולות עבור X ו-Y, וכן מקטע (תוספת) שבו ערכי X ו-Y ישתנו בלולאה.
כללים:
1.  המשתמש מזין את הערך המינימלי והמקסימלי עבור X, וכן את המקטע (תוספת) עבור X.
2.  המשתמש מזין את הערך המינימלי והמקסימלי עבור Y, וכן את המקטע (תוספת) עבור Y.
3.  התוכנה מחשבת את ערך Z על פי הנוסחה Z = X * X - Y * Y ומציגה את הנקודה (X, Y, Z) במרחב תלת-ממדי.
4.  התוכנה עוברת על כל ערכי X ו-Y בטווחי הערכים המוגדרים עם המקטעים המוגדרים, מחשבת ומציגה את ערכי Z המתאימים.
5.  התוכנה מסתיימת לאחר שכל הנקודות שורטטו.
-----------------
אלגוריתם:
1.  קלט: בקש מהמשתמש את ערך X המינימלי (Xmin), ערך X המקסימלי (Xmax) ומקטע X (Xincrement).
2.  קלט: בקש מהמשתמש את ערך Y המינימלי (Ymin), ערך Y המקסימלי (Ymax) ומקטע Y (Yincrement).
3.  לולאה עבור X: התחל לולאה שבה X משתנה מ-Xmin ל-Xmax במקטע Xincrement.
    3.1. לולאה עבור Y: התחל לולאה מקוננת שבה Y משתנה מ-Ymin ל-Ymax במקטע Yincrement.
        3.1.1 חישוב Z: חשב את Z כ-Z = X*X - Y*Y.
        3.1.2 פלט: הצג את ערכי X, Y ו-Z.
    3.2. סוף הלולאה עבור Y.
4.  סוף הלולאה עבור X.
5.  סוף התוכנית.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InputXMin["קלט: <code><b>Xmin</b></code>"]
    InputXMin --> InputXMax["קלט: <code><b>Xmax</b></code>"]
    InputXMax --> InputXIncrement["קלט: <code><b>Xincrement</b></code>"]
    InputXIncrement --> InputYMin["קלט: <code><b>Ymin</b></code>"]
    InputYMin --> InputYMax["קלט: <code><b>Ymax</b></code>"]
    InputYMax --> InputYIncrement["קלט: <code><b>Yincrement</b></code>"]
    InputYIncrement --> LoopXStart{"תחילת לולאה עבור X: <code><b>X = Xmin, כל עוד X <= Xmax</b></code>"}
    LoopXStart -- כן --> LoopYStart{"תחילת לולאה עבור Y: <code><b>Y = Ymin, כל עוד Y <= Ymax</b></code>"}
    LoopYStart -- כן --> CalculateZ["<code><b>Z = X*X - Y*Y</b></code>"]
    CalculateZ --> OutputXYZ["פלט: <code><b>X, Y, Z</b></code>"]
    OutputXYZ --> LoopYEnd{"סוף לולאה עבור Y"}
    LoopYEnd -- כן --> LoopYStart
    LoopYEnd -- לא --> LoopXEnd{"סוף לולאה עבור X"}
    LoopXEnd -- כן --> LoopXStart
    LoopXEnd -- לא --> End["סוף"]
```
מקרא:
    Start - התחלת התוכנית.
    InputXMin - בקשת המשתמש וקלט ערך מינימלי עבור X (Xmin).
    InputXMax - בקשת המשתמש וקלט ערך מקסימלי עבור X (Xmax).
    InputXIncrement - בקשת המשתמש וקלט מקטע (תוספת) עבור X (Xincrement).
    InputYMin - בקשת המשתמש וקלט ערך מינימלי עבור Y (Ymin).
    InputYMax - בקשת המשתמש וקלט ערך מקסימלי עבור Y (Ymax).
    InputYIncrement - בקשת המשתמש וקלט מקטע (תוספת) עבור Y (Yincrement).
    LoopXStart - התחלת לולאה עבור X, הנמשכת כל עוד X <= Xmax.
    LoopYStart - התחלת לולאה מקוננת עבור Y, הנמשכת כל עוד Y <= Ymax.
    CalculateZ - חישוב ערך Z על פי הנוסחה Z = X * X - Y * Y.
    OutputXYZ - פלט ערכי X, Y ו-Z.
    LoopYEnd - סוף הלולאה עבור Y. אם לולאת Y לא הסתיימה, חוזרים להתחלת הלולאה LoopYStart.
    LoopXEnd - סוף הלולאה עבור X. אם לולאת X לא הסתיימה, חוזרים להתחלת הלולאה LoopXStart.
    End - סוף התוכנית.
```
```python


# Ввод данных от пользователя
try:
    Xmin = float(input("Введите минимальное значение X: "))
    Xmax = float(input("Введите максимальное значение X: "))
    Xincrement = float(input("Введите приращение для X: "))
    
    Ymin = float(input("Введите минимальное значение Y: "))
    Ymax = float(input("Введите максимальное значение Y: "))
    Yincrement = float(input("Введите приращение для Y: "))
except ValueError:
    print("Ошибка: Пожалуйста, введите числовые значения.")
    exit()

# Внешний цикл по X
X = Xmin
while X <= Xmax:
    # Внутренний цикл по Y
    Y = Ymin
    while Y <= Ymax:
        # Вычисление Z по формуле Z = X*X - Y*Y
        Z = X * X - Y * Y
        # Вывод координат X, Y и значения Z
        print(f"X: {X}, Y: {Y}, Z: {Z}")
        # Увеличение Y на заданное приращение
        Y += Yincrement
    # Увеличение X на заданное приращение
    X += Xincrement
    
"""
הסבר הקוד:
1.  **בלוק קלט הנתונים:**
    -   `try...except ValueError`: בלוק זה מטפל בחריגות שעשויות להתרחש אם המשתמש יזין נתונים שאינם תקינים (לדוגמה, לא מספר).
    -   `Xmin = float(input("Введите минимальное значение X: "))`: מבקש את הערך המינימלי עבור ציר X וממיר אותו למספר בעל נקודה צפה.
    -   שורות דומות מבקשות וממירות ערכים עבור `Xmax`, `Xincrement`, `Ymin`, `Ymax` ו-`Yincrement`.
    -   `exit()`: אם מתרחשת שגיאת קלט, התוכנית מסתיימת.

2.  **הלולאה החיצונית `while X <= Xmax:`**:
    -   מגדיר את הערך ההתחלתי של `X` כשווה ל-`Xmin`.
    -   הלולאה מבוצעת כל עוד ערך `X` אינו עולה על `Xmax`.
    -   `X` גדל ב-`Xincrement` בסיום כל איטרציה של הלולאה.

3.  **הלולאה הפנימית `while Y <= Ymax:`**:
    -   מגדיר את הערך ההתחלתי של `Y` כשווה ל-`Ymin` בתחילת כל איטרציה של הלולאה החיצונית.
    -   הלולאה מבוצעת כל עוד ערך `Y` אינו עולה על `Ymax`.
    -   `Y` גדל ב-`Yincrement` בסיום כל איטרציה של הלולאה הפנימית.

4.  **חישוב `Z`**:
    -   `Z = X * X - Y * Y`: מחשב את ערך `Z` על פי הנוסחה המוגדרת.

5.  **פלט הקואורדינטות**:
    -   `print(f"X: {X}, Y: {Y}, Z: {Z}")`: מציג את ערכי `X`, `Y` ואת `Z` המחושב עבור כל נקודה.

6.  **הגדלת `X` ו-`Y`**:
    -   `Y += Yincrement`: מגדיל את ערך `Y` במקטע המוגדר `Yincrement` בסיום כל איטרציה של הלולאה הפנימית.
    -   `X += Xincrement`: מגדיל את ערך `X` במקטע המוגדר `Xincrement` בסיום כל איטרציה של הלולאה החיצונית.

באופן זה, התוכנית משרטטת גרף תלת-ממדי של הפונקציה Z = X^2 - Y^2, על ידי מעבר על ערכי X ו-Y בטווחים המוגדרים עם המקטעים המוגדרים, ומציגה את קואורדינטות הנקודות (X, Y, Z).
"""
```
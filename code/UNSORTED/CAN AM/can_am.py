CAN AM:
=================
רמת קושי: 5
-----------------
המשחק "CAN AM" מהווה סימולציה של מרוץ על מסלול. השחקן שולט במכונית, וקובע כמה פעמים עליו ללחוץ על הגז כדי להגיע לקו הסיום. על השחקן לחשב נכונה את ההאצה כדי לא לסטות מהמסלול או לעצור מוקדם מדי.

כללי המשחק:
1. השחקן מתחיל את המרוץ ממיקום אפס.
2. אורך המסלול קבוע ושווה ל-200.
3. בכל תור, השחקן מזין את מספר הלחיצות על הגז (בין 0 ל-9).
4. מהירות הרכב גדלה בגודל הערך שהוזן.
5. המיקום החדש של הרכב מחושב על ידי הוספת ערך המהירות הנוכחית למיקום הנוכחי.
6. אם מיקום הרכב הופך להיות פחות מאפס או יותר מ-200, השחקן מפסיד במרוץ.
7. אם מיקום הרכב מגיע ל-200 או יותר, השחקן מנצח במרוץ.
8. המשחק נמשך עד שהשחקן מגיע לקו הסיום או סוטה מהמסלול.
-----------------
אלגוריתם:
1. קביעת מיקום התחלתי של הרכב ל-0.
2. קביעת מהירות התחלתית של הרכב ל-0.
3. קביעת אורך המסלול ל-200.
4. התחלת לולאה "כל עוד המכונית לא הגיעה לקו הסיום ולא סטתה מהמסלול":
    4.1 בקשת כמות לחיצות גז מהשחקן.
    4.2 הגדלת המהירות בערך שהוזן.
    4.3 שינוי המיקום, על ידי הוספת המהירות הנוכחית למיקום הנוכחי.
    4.4 אם המיקום פחות מאפס או יותר מאורך המסלול, הצגת ההודעה "CRASH" וסיום המשחק.
    4.5 אם המיקום גדול או שווה לאורך המסלול, הצגת ההודעה "WINNER" וסיום המשחק.
5. סיום המשחק.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InitializeVariables["<p align='left'>אתחול משתנים:<br><code><b>position = 0</b><br><b>speed = 0</b><br><b>trackLength = 200</b></code></p>"]
    InitializeVariables --> LoopStart{"תחילת לולאה: עד לסיום או התרסקות"}
    LoopStart -- כן --> InputAcceleration["<p align='left'>קלט האצה:
    <code><b>acceleration = int(input())</b></code></p>"]
    InputAcceleration --> UpdateSpeed["<code><b>speed = speed + acceleration</b></code>"]
    UpdateSpeed --> UpdatePosition["<code><b>position = position + speed</b></code>"]
    UpdatePosition --> CheckCrash{"בדיקה: <code><b>position < 0 or position > trackLength?</b></code>"}
    CheckCrash -- כן --> OutputCrash["פלט הודעה: <b>CRASH</b>"]
    OutputCrash --> End["סיום"]
    CheckCrash -- לא --> CheckWin{"בדיקה: <code><b>position >= trackLength?</b></code>"}
    CheckWin -- כן --> OutputWin["פלט הודעה: <b>WINNER</b>"]
    OutputWin --> End
    CheckWin -- לא --> LoopStart
    LoopStart -- לא --> End
```
מקרא:
    Start - התחלת התוכנית.
    InitializeVariables - אתחול משתנים: position (מיקום נוכחי) מאותחל ל-0, speed (מהירות נוכחית) מאותחל ל-0, trackLength (אורך המסלול) מאותחל ל-200.
    LoopStart - תחילת הלולאה, הנמשכת עד שהמכונית מגיעה לקו הסיום או מתרחשת תאונה.
    InputAcceleration - בקשה מהמשתמש להזין האצה ושמירתה במשתנה acceleration.
    UpdateSpeed - עדכון מהירות: האצה שהוזנה מתווספת למהירות הנוכחית.
    UpdatePosition - עדכון מיקום: המהירות הנוכחית מתווספת למיקום הנוכחי.
    CheckCrash - בדיקה האם המיקום יצא מגבולות המסלול (פחות מ-0 או יותר מ-trackLength).
    OutputCrash - פלט הודעה "CRASH" אם התרחשה תאונה.
    End - סיום התוכנית.
    CheckWin - בדיקה האם המיקום הגיע לסוף המסלול (גדול או שווה ל-trackLength).
    OutputWin - פלט הודעה "WINNER" אם הושג קו הסיום.

```
import time

# אתחול משתנים
position = 0 # מיקום התחלתי של המכונית
speed = 0    # מהירות התחלתית של המכונית
trackLength = 200 # אורך המסלול

print("המשחק CAN AM החל!")
time.sleep(1)  # יוצר השהיה של 1 שניה
# לולאת המשחק הראשית
while True:
    print(f"מיקום נוכחי: {position}, מהירות נוכחית: {speed}")
    # מבקשים מהמשתמש להזין האצה
    try:
        acceleration = int(input("הזן האצה (0-9): "))
    except ValueError:
        print("אנא הזן מספר שלם בין 0 ל-9.")
        continue
    
    if acceleration < 0 or acceleration > 9:
      print("האצה חייבת להיות בטווח שבין 0 ל-9.")
      continue

    # מעדכנים את המהירות
    speed += acceleration
    # מעדכנים את המיקום
    position += speed

    # בודקים האם המכונית יצאה מגבולות המסלול
    if position < 0 or position > trackLength:
        print("CRASH! יצאת מהמסלול!")
        break # מסיימים את המשחק
    # בודקים האם הושג קו הסיום
    if position >= trackLength:
        print("WINNER! הגעת לקו הסיום!")
        break # מסיימים את המשחק
    time.sleep(1) # יוצר השהיה של 1 שניה

```
הסבר על הקוד:
1.  **ייבוא מודול `time`**:
    -   `import time`: מייבא את מודול `time`, המשמש להשהיית ביצוע התוכנית.
2.  **אתחול משתנים**:
    -   `position = 0`: מאתחל את המשתנה `position` (מיקום התחלתי של המכונית) בערך 0.
    -   `speed = 0`: מאתחל את המשתנה `speed` (מהירות התחלתית של המכונית) בערך 0.
    -   `trackLength = 200`: מאתחל את המשתנה `trackLength` (אורך המסלול) בערך 200.
3. **ברכה**:
    - מוצגת ההודעה "המשחק CAN AM החל!".
    - time.sleep(1) - יוצר השהיה של 1 שניה.
4.  **הלולאה הראשית `while True:`**:
    -   לולאה אינסופית, הנמשכת עד שהשחקן מגיע לקו הסיום או סוטה מהמסלול (תבוצע הפקודה `break`).
    -   `print(f"מיקום נוכחי: {position}, מהירות נוכחית: {speed}")`: מציג את המיקום והמהירות הנוכחיים של הרכב.
    -   **קלט נתונים**:
        -   `try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין ערך שאינו מספר שלם, תוצג הודעת שגיאה.
        -   `acceleration = int(input("הזן האצה (0-9): "))`: מבקש מהמשתמש להזין האצה וממיר אותה למספר שלם.
    -  **בדיקת טווח ההאצה**:
        - `if acceleration < 0 or acceleration > 9:`: בודק אם ההאצה חורגת מהטווח המותר.
        - `print("האצה חייבת להיות בטווח שבין 0 ל-9.")`: אם ההאצה שגויה, מציג הודעת שגיאה וממשיך את הלולאה לאיטרציה הבאה.
    -   **עדכון מהירות ומיקום**:
        -   `speed += acceleration`: מגדיל את מהירות הרכב הנוכחית בערך ההאצה שהוזן.
        -   `position += speed`: מעדכן את מיקום הרכב הנוכחי, על ידי הוספת המהירות הנוכחית אליו.
    -   **בדיקה לגבי התרסקות**:
        -   `if position < 0 or position > trackLength:`: בודק אם המכונית סטתה מגבולות המסלול.
        -   `print("CRASH! יצאת מהמסלול!")`: מציג הודעה על הפסד.
        -   `break`: מסיים את הלולאה אם התרחשה התרסקות.
    -   **בדיקה לגבי ניצחון**:
        -   `if position >= trackLength:`: בודק אם המכונית הגיעה לקו הסיום.
        -   `print("WINNER! הגעת לקו הסיום!")`: מציג הודעה על ניצחון.
        -   `break`: מסיים את הלולאה אם השחקן ניצח.
    -   `time.sleep(1)`: עוצר את התוכנית למשך 1 שניה, כדי לאפשר לשחקן לראות את התוצאות.
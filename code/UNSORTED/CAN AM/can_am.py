CAN AM:
=================
מורכבות: 5
-----------------
המשחק "CAN AM" הוא סימולציה של מרוץ על מסלול. השחקן מנהל מכונית, וקובע כמה פעמים יש ללחוץ על הגז כדי להגיע לקו הסיום. על השחקן לחשב נכונה את התאוצה כדי לא לצאת מהמסלול או לא לעצור מוקדם מדי.

כללי המשחק:
1. השחקן מתחיל את המרוץ מנקודת האפס.
2. אורך המסלול נתון ושווה ל-200.
3. בכל מהלך, השחקן מזין את מספר הלחיצות על הגז (מ-0 עד 9).
4. מהירות המכונית גדלה בגודל הערך שהוזן.
5. מיקום המכונית החדש מחושב על ידי הוספת גודל המהירות הנוכחית למיקום הנוכחי.
6. אם מיקום המכונית הופך קטן מאפס או גדול מ-200, השחקן מפסיד את המרוץ.
7. אם מיקום המכונית מגיע ל-200 או יותר, השחקן מנצח את המרוץ.
8. המשחק נמשך עד שהשחקן מגיע לקו הסיום או יוצא מהמסלול.
-----------------
אלגוריתם:
1. הגדר את המיקום ההתחלתי של המכונית ל-0.
2. הגדר את המהירות ההתחלתית של המכונית ל-0.
3. הגדר את אורך המסלול ל-200.
4. התחל לולאה "כל עוד המכונית אינה בקו הסיום או יצאה מהמסלול":
    4.1 בקש מהשחקן את מספר הלחיצות על הגז.
    4.2 הגדל את המהירות בערך שהוזן.
    4.3 שנה את המיקום על ידי הוספת המהירות הנוכחית למיקום הנוכחי.
    4.4 אם המיקום קטן מאפס או גדול מאורך המסלול, הצג הודעה "CRASH" וסיים את המשחק.
    4.5 אם המיקום גדול או שווה לאורך המסלול, הצג הודעה "WINNER" וסיים את המשחק.
5. סוף המשחק.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InitializeVariables["<p align='left'>אתחול משתנים:<br><code><b>position = 0</b><br><b>speed = 0</b><br><b>trackLength = 200</b></code></p>"]
    InitializeVariables --> LoopStart{"התחלת לולאה: כל עוד לא פיניש ולא קראש"}
    LoopStart -- כן --> InputAcceleration["<p align='left'>הזנת תאוצה:
    <code><b>acceleration = int(input())</b></code></p>"]
    InputAcceleration --> UpdateSpeed["<code><b>speed = speed + acceleration</b></code>"]
    UpdateSpeed --> UpdatePosition["<code><b>position = position + speed</b></code>"]
    UpdatePosition --> CheckCrash{"בדיקה: <code><b>position < 0 or position > trackLength?</b></code>"}
    CheckCrash -- כן --> OutputCrash["הצגת הודעה: <b>CRASH</b>"]
    OutputCrash --> End["סוף"]
    CheckCrash -- לא --> CheckWin{"בדיקה: <code><b>position >= trackLength?</b></code>"}
    CheckWin -- כן --> OutputWin["הצגת הודעה: <b>WINNER</b>"]
    OutputWin --> End
    CheckWin -- לא --> LoopStart
    LoopStart -- לא --> End
```
מקרא:
    Start - התחלת התוכנית.
    InitializeVariables - אתחול משתנים: position (מיקום נוכחי) מוגדר ל-0, speed (מהירות נוכחית) מוגדרת ל-0, trackLength (אורך מסלול) מוגדר ל-200.
    LoopStart - התחלת הלולאה, הנמשכת כל עוד המכונית אינה בקו הסיום ולא התרחשה תאונה.
    InputAcceleration - בקשת הזנת תאוצה מהמשתמש ושמירתה במשתנה acceleration.
    UpdateSpeed - עדכון מהירות: למהירות הנוכחית מתווספת התאוצה שהוזנה.
    UpdatePosition - עדכון מיקום: למיקום הנוכחי מתווספת המהירות הנוכחית.
    CheckCrash - בדיקה האם המיקום חרג מגבולות המסלול (קטן מ-0 או גדול מ-trackLength).
    OutputCrash - הצגת הודעה "CRASH" אם התרחשה תאונה.
    End - סוף התוכנית.
    CheckWin - בדיקה האם המיקום הגיע לסוף המסלול (גדול או שווה ל-trackLength).
    OutputWin - הצגת הודעה "WINNER" אם הושג קו הסיום.

```
import time

# אתחול משתנים
position = 0 # מיקום התחלתי של המכונית
speed = 0    # מהירות התחלתית של המכונית
trackLength = 200 # אורך המסלול

print("המשחק CAN AM החל!")
time.sleep(1)  # השהיה של שנייה אחת
# לולאת המשחק הראשית
while True:
    print(f"מיקום נוכחי: {position}, מהירות נוכחית: {speed}")
    # מבקשים מהמשתמש הזנת תאוצה
    try:
        acceleration = int(input("הזן תאוצה (0-9): "))
    except ValueError:
        print("אנא הזן מספר שלם בין 0 ל-9.")
        continue
    
    if acceleration < 0 or acceleration > 9:
      print("התאוצה חייבת להיות בטווח שבין 0 ל-9.")
      continue

    # מעדכנים את המהירות
    speed += acceleration
    # מעדכנים את המיקום
    position += speed

    # בודקים אם המכונית יצאה מהמסלול
    if position < 0 or position > trackLength:
        print("CRASH! יצאת מהמסלול!")
        break # מסיימים את המשחק
    # בודקים אם הגענו לקו הסיום
    if position >= trackLength:
        print("WINNER! הגעת לקו הסיום!")
        break # מסיימים את המשחק
    time.sleep(1) # השהיה של שנייה אחת

```
הסבר קוד:
1.  **ייבוא מודול `time`**:
    -   `import time`: מייבא את מודול `time`, המשמש להשהיית ביצוע התוכנית.
2.  **אתחול משתנים**:
    -   `position = 0`: מאתחל את המשתנה `position` (מיקום התחלתי של המכונית) בערך 0.
    -   `speed = 0`: מאתחל את המשתנה `speed` (מהירות התחלתית של המכונית) בערך 0.
    -   `trackLength = 200`: מאתחל את המשתנה `trackLength` (אורך המסלול) בערך 200.
3. **ברכה**:
    - מוצגת ההודעה "המשחק CAN AM החל!".
    - `time.sleep(1)` - מבצע השהיה של שנייה אחת.
4.  **לולאת המשחק הראשית `while True:`**:
    -   לולאה אינסופית, הנמשכת כל עוד השחקן אינו מגיע לקו הסיום או אינו יוצא מהמסלול (תתבצע הפקודה `break`).
    -   `print(f"מיקום נוכחי: {position}, מהירות נוכחית: {speed}")`: מציג את המיקום והמהירות הנוכחיים של המכונית.
    -   **קלט נתונים**:
        -   `try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין ערך שאינו מספר שלם, תוצג הודעת שגיאה.
        -   `acceleration = int(input("הזן תאוצה (0-9): "))`: מבקש מהמשתמש את התאוצה וממיר אותה למספר שלם.
    -  **בדיקת טווח התאוצה**:
        - `if acceleration < 0 or acceleration > 9:`: בודק אם התאוצה חורגת מהטווח המותר.
        - `print("התאוצה חייבת להיות בטווח שבין 0 ל-9.")`: אם התאוצה אינה תקינה, מציג הודעת שגיאה וממשיך ללולאה הבאה.
    -   **עדכון מהירות ומיקום**:
        -   `speed += acceleration`: מגדיל את המהירות הנוכחית של המכונית בערך התאוצה שהוזנה.
        -   `position += speed`: מעדכן את המיקום הנוכחי של המכונית, על ידי הוספת המהירות הנוכחית אליו.
    -   **בדיקת קראש**:
        -   `if position < 0 or position > trackLength:`: בודק האם המכונית יצאה מגבולות המסלול.
        -   `print("CRASH! יצאת מהמסלול!")`: מציג הודעה על הפסד.
        -   `break`: מסיים את הלולאה, אם התרחש קראש.
    -   **בדיקת ניצחון**:
        -   `if position >= trackLength:`: בודק האם המכונית הגיעה לקו הסיום.
        -   `print("WINNER! הגעת לקו הסיום!")`: מציג הודעה על ניצחון.
        -   `break`: מסיים את הלולאה, אם השחקן ניצח.
    -   `time.sleep(1)`: מבצע השהיה של שנייה אחת, כדי שהשחקן יוכל לראות את התוצאות.
<גבוה נמוך>:
=================
מורכבות: 2
-----------------
המשחק "גבוה-נמוך" הוא משחק פשוט, שבו המחשב בוחר מספר בין 1 ל-100, והשחקן מנסה לנחש אותו. לאחר כל ניסיון, המחשב מודיע האם המספר שהוזן היה גבוה יותר או נמוך יותר מהמספר הנעלם.
המשחק נמשך עד שהשחקן מנחש את המספר.

חוקי המשחק:
1. המחשב בוחר מספר שלם אקראי בין 1 ל-100.
2. השחקן מזין את ניחושיו לגבי המספר הנעלם.
3. לאחר כל ניסיון, המחשב מודיע האם המספר שהוזן היה נמוך מדי או גבוה מדי.
4. המשחק נמשך עד שהשחקן מנחש את המספר הנעלם.
-----------------
אלגוריתם:
1. להגדיר את מספר הניסיונות ל-0.
2. ליצור מספר אקראי בטווח בין 1 ל-100.
3. להתחיל בלולאת "כל עוד המספר לא נוחש":
    3.1 להגדיל את מספר הניסיונות ב-1.
    3.2 לבקש מהשחקן להזין מספר.
    3.3 אם המספר שהוזן שווה למספר הנעלם, לעבור לשלב 4.
    3.4 אם המספר שהוזן קטן מהמספר הנעלם, להציג את ההודעה "TOO LOW".
    3.5 אם המספר שהוזן גדול מהמספר הנעלם, להציג את ההודעה "TOO HIGH".
4. להציג את ההודעה "YOU GOT IT IN {מספר ניסיונות} GUESSES!"
5. סיום המשחק.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InitializeVariables["<p align='left'>אתחול משתנים:
    <code><b>
    numberOfGuesses = 0
    targetNumber = random(1, 100)
    </b></code></p>"]
    InitializeVariables --> LoopStart{"תחילת לולאה: כל עוד לא נוחש"}
    LoopStart -- כן --> IncreaseGuesses["<code><b>numberOfGuesses = numberOfGuesses + 1</b></code>"]
    IncreaseGuesses --> InputGuess["הזנת מספר על ידי המשתמש: <code><b>userGuess</b></code>"]
    InputGuess --> CheckGuess{"בדיקה: <code><b>userGuess == targetNumber?</b></code>"}
    CheckGuess -- כן --> OutputWin["הצגת הודעה: <b>YOU GOT IT IN <code>{numberOfGuesses}</code> GUESSES!</b>"]
    OutputWin --> End["סיום"]
    CheckGuess -- לא --> CheckLow{"בדיקה: <code><b>userGuess &lt; targetNumber</b></code>?"}
    CheckLow -- כן --> OutputLow["הצגת הודעה: <b>TOO LOW</b>"]
    OutputLow --> LoopStart
    CheckLow -- לא --> OutputHigh["הצגת הודעה: <b>TOO HIGH</b>"]
    OutputHigh --> LoopStart
    LoopStart -- לא --> End

```
מקרא:
    Start - תחילת התוכנית.
    InitializeVariables - אתחול משתנים: numberOfGuesses (מספר ניסיונות) מוגדר ל-0, ו-targetNumber (המספר הנעלם) נוצר באופן אקראי בין 1 ל-100.
    LoopStart - תחילת הלולאה שנמשכת כל עוד המספר לא נוחש.
    IncreaseGuesses - הגדלת מונה מספר הניסיונות ב-1.
    InputGuess - בקשת הזנת מספר מהמשתמש ושמירתו במשתנה userGuess.
    CheckGuess - בדיקה האם המספר שהוזן userGuess שווה למספר הנעלם targetNumber.
    OutputWin - הצגת הודעה על הניצחון, אם המספרים שווים, עם ציון מספר הניסיונות.
    End - סיום התוכנית.
    CheckLow - בדיקה האם המספר שהוזן userGuess קטן מהמספר הנעלם targetNumber.
    OutputLow - הצגת ההודעה "TOO LOW", אם המספר שהוזן קטן מהמספר הנעלם.
    OutputHigh - הצגת ההודעה "TOO HIGH", אם המספר שהוזן גדול מהמספר הנעלם.
```
```python
import random

# מאתחל את מונה הניסיונות
numberOfGuesses = 0
# בוחר מספר אקראי בין 1 ל-100
targetNumber = random.randint(1, 100)

# מתחיל את לולאת המשחק הראשית
while True:
    # מגדיל את מספר הניסיונות ב-1
    numberOfGuesses += 1
    # מבקש מהמשתמש להזין מספר
    try:
       userGuess = int(input("Введите ваше предположение: "))
    except ValueError:
        print("Пожалуйста, введите целое число.")
        continue
    # בודק האם המשתמש ניחש את המספר
    if userGuess == targetNumber:
        print(f"Вы угадали число за {numberOfGuesses} попыток!")
        break  # מסיים את הלולאה אם המספר נוחש
    elif userGuess < targetNumber:
        print("Слишком низко")  # מודיע שהמספר הנעלם גדול יותר
    else:
        print("Слишком высоко")  # מודיע שהמספר הנעלם קטן יותר
```
הסבר על הקוד:
1. **ייבוא מודול `random`**:
   - `import random`: מייבא את מודול `random`, המשמש ליצירת מספר אקראי.
2. **אתחול משתנים:**
   -  `numberOfGuesses = 0`: מאתחל את המשתנה `numberOfGuesses` לספירת ניסיונות השחקן ומגדיר את ערכו ההתחלתי ל-0.
    - `targetNumber = random.randint(1, 100)`: יוצר מספר שלם אקראי בטווח של 1 עד 100 (כולל) ושומר אותו במשתנה `targetNumber`, שישמש כמספר הנעלם.
3. **לולאת המשחק הראשית `while True:`**:
    -  לולאה אינסופית הנמשכת עד שהשחקן מנחש את המספר (עד שיתבצע `break`).
    - `numberOfGuesses += 1`: בכל איטרציה של הלולאה, מגדיל את מונה הניסיונות ב-1.
    - **טיפול בקלט המשתמש**:
        - `try...except ValueError`: בלוק `try-except` מטפל בשגיאות קלט אפשריות. אם המשתמש יזין מספר לא שלם, התוכנית לא תקרוס, אלא תציג הודעת שגיאה.
        - `userGuess = int(input("Введите ваше предположение: "))`: מציג בקשה למשתמש להזין מספר ומנסה להמיר את הטקסט שהוזן למספר שלם. התוצאה נשמרת ב-`userGuess`.
    - **בדיקת ניחוש המספר**:
        - `if userGuess == targetNumber:`: בודק האם המספר שהוזן על ידי המשתמש (`userGuess`) שווה למספר הנעלם (`targetNumber`).
           - `print(f"Вы угадали число за {numberOfGuesses} попыток!")`: אם המספרים שווים, מציג הודעת ברכה ומספר הניסיונות.
           -  `break`: יוצא מלולאת `while True`, ומסיים את המשחק.
        - `elif userGuess < targetNumber:`: אם המספר שהוזן קטן מהמספר הנעלם, מציג את ההודעה "Слишком низко".
        - `else:`: אם המספר שהוזן אינו שווה ואינו קטן מהמספר הנעלם, משמע שהוא גדול יותר, ולכן מוצגת ההודעה "Слишком высоко".
4. **סיום המשחק**:
  -  לולאת `while True` נמשכת כל עוד המשתמש לא ניחש את המספר. כאשר המשתמש מנחש את המספר, מתבצעת הפקודה `break`, והתוכנית מסיימת את ביצוע הלולאה, ולפיכך גם את המשחק.
```
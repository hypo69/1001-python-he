CHIEF:
=================
רמת מורכבות: 4
-----------------
המשחק "צ'יף" (CHIEF) הוא משחק שבו השחקן מגלם מנהל האחראי על תכנון הייצור במפעל. השחקן קובע את כמות המוצרים המיוצרים מכל סוג, והמחשב קובע האם ערכים אלו תואמים לדרישות הנדרשות. במידה ולא, השחקן מקבל הודעה המציינת אילו ערכים בדיוק היו שגויים. מטרת המשחק היא להגיע לייצור אופטימלי על ידי ניחוש נכון של כמות המוצרים.

כללי המשחק:
1. המחשב בוחר שלושה ערכים אקראיים בטווח שבין 1 ל-10: `targetA`, `targetB` ו-`targetC`.
2. השחקן מזין את ניחושיו עבור הערכים `userA`, `userB` ו-`userC`.
3. המחשב בודק האם הערכים שהוזנו תואמים לערכים שנבחרו על ידו.
4. אם כל שלושת הערכים נוחשו נכונה, המשחק מסתיים בניצחון.
5. אם לפחות ערך אחד אינו תואם, המחשב מציג אילו ערכים בדיוק היו שגויים.
6. המשחק נמשך עד שהשחקן מנחש את כל שלושת הערכים נכונה.
-----------------
אלגוריתם:
1. ליצור מספרים שלמים אקראיים `targetA`, `targetB` ו-`targetC` בטווח שבין 1 ל-10.
2. להתחיל לולאה "כל עוד לא נוחשו כל המספרים":
    2.1 לבקש מהשחקן להזין שלושה מספרים שלמים: `userA`, `userB` ו-`userC`.
    2.2 לאתחל את המחרוזת `message` כמחרוזת ריקה.
    2.3 אם `userA` אינו שווה ל-`targetA`, להוסיף "A" ל-`message`.
    2.4 אם `userB` אינו שווה ל-`targetB`, להוסיף "B" ל-`message`.
    2.5 אם `userC` אינו שווה ל-`targetC`, להוסיף "C" ל-`message`.
    2.6 אם `message` אינה ריקה, להציג את ההודעה "WRONG ON " ואת תוכן `message`.
    2.7 אחרת, להציג את ההודעה "YOU GOT IT!".
3. סיום המשחק.
-----------------

"""
import random

# יוצר מספרים אקראיים מ-1 עד 10 עבור targetA, targetB ו-targetC
targetA = random.randint(1, 10)
targetB = random.randint(1, 10)
targetC = random.randint(1, 10)


# לולאה אינסופית, כל עוד השחקן לא מנחש את כל המספרים
while True:
    # מבקש קלט של שלושה מספרים מהמשתמש
    try:
        userA = int(input("Введите число для A (от 1 до 10): "))
        userB = int(input("Введите число для B (от 1 до 10): "))
        userC = int(input("Введите число для C (от 1 до 10): "))
    except ValueError:
        print("Пожалуйста, введите целые числа.")
        continue

    # מאתחל את המחרוזת עבור הודעות שגיאה
    message = ""

    # בודק האם המספר A נכון
    if userA != targetA:
        message += "A"  # אם שגוי, מוסיף "A" להודעה

    # בודק האם המספר B נכון
    if userB != targetB:
        message += "B" # אם שגוי, מוסיף "B" להודעה

    # בודק האם המספר C נכון
    if userC != targetC:
        message += "C" # אם שגוי, מוסיף "C" להודעה

    # אם יש שגיאות, מציג הודעה על אילו מספרים שגויים
    if message != "":
        print(f"WRONG ON {message}")
    # אם כל המספרים נכונים, מברך את השחקן ומסיים את המשחק
    else:
        print("YOU GOT IT!")
        break
"""
הסבר על הקוד:
1.  **ייבוא המודול `random`**:
    -   `import random`: מייבא את המודול `random`, הנחוץ ליצירת מספרים אקראיים.

2.  **יצירת מספרים אקראיים**:
    -   `targetA = random.randint(1, 10)`: יוצר מספר שלם אקראי מ-1 עד 10 ומקצה אותו למשתנה `targetA`.
    -   `targetB = random.randint(1, 10)`: יוצר מספר שלם אקראי מ-1 עד 10 ומקצה אותו למשתנה `targetB`.
    -   `targetC = random.randint(1, 10)`: יוצר מספר שלם אקראי מ-1 עד 10 ומקצה אותו למשתנה `targetC`.
   משתנים אלו מייצגים את המספרים שנבחרו באופן אקראי, אותם השחקן צריך לנחש.
3.  **לולאת המשחק הראשית**:
    -   `while True:`: מפעיל לולאה אינסופית, שתמשיך לפעול עד שהשחקן ינחש את כל שלושת המספרים.
    -   **קלט מספרים מהמשתמש**:
    	-	`try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין ערך שאינו מספר שלם, תוצג הודעת שגיאה.
    	-  `userA = int(input("Введите число для A (от 1 до 10): "))`: מבקש מהמשתמש להזין מספר עבור `A`, ממיר אותו למספר שלם ושומר אותו במשתנה `userA`.
    	-  באופן דומה, מבקש להזין מספרים עבור `B` ו-`C` ושומר אותם ב-`userB` ו-`userC` בהתאמה.
    -   `message = ""`: מאתחל מחרוזת ריקה `message`, שתשמש לאחסון מידע על המספרים שהוזנו באופן שגוי.
4.  **בדיקת המספרים שהוזנו**:
    -  `if userA != targetA:`: בודק האם המספר שהוזן על ידי המשתמש `userA` אינו שווה למספר שנבחר על ידי המחשב `targetA`.
    -  `message += "A"`: אם `userA` אינו שווה ל-`targetA`, מוסיף את התו "A" למחרוזת `message`, כדי לציין שהמספר `A` הוזן באופן שגוי.
    - בדיקות דומות מתבצעות עבור המספרים `B` ו-`C`.
5.  **הצגת התוצאות**:
    -   `if message != "":`: בודק האם המחרוזת `message` אינה ריקה.
    -   `print(f"WRONG ON {message}")`: אם המחרוזת `message` אינה ריקה, מציג הודעה המציינת אילו מספרים הוזנו באופן שגוי (לדוגמה, "WRONG ON AB" - משמעות הדבר היא שהמספרים `A` ו-`B` הוזנו באופן שגוי).
    -   `else:`: מתבצע אם המחרוזת `message` ריקה (כלומר, כל המספרים נוחשו נכונה).
    -   `print("YOU GOT IT!")`: מציג הודעת ניצחון.
    -   `break`: מסיים את הלולאה ואת המשחק, אם כל המספרים נוחשו נכונה.
"""
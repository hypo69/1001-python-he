CHIEF:
=================
רמת קושי: 4
-----------------
המשחק "צ'יף" הוא משחק שבו השחקן מגלם דמות של מנהל המפעל, המתכנן את הייצור. השחקן קובע את כמות הפריטים המיוצרים מכל סוג, והמחשב בודק האם ערכים אלו עומדים בדרישות הנדרשות. אם לא, השחקן מקבל הודעה המציינת אילו ערכים היו שגויים. מטרת המשחק היא להגיע לייצור אופטימלי על ידי ניחוש נכון של כמות הפריטים.

חוקי המשחק:
1. המחשב בוחר שלושה ערכים אקראיים בטווח שבין 1 ל-10: `targetA`, `targetB` ו-`targetC`.
2. השחקן מזין את הניחושים שלו עבור הערכים `userA`, `userB` ו-`userC`.
3. המחשב בודק האם הערכים שהוזנו תואמים לערכים שנבחרו.
4. אם כל שלושת הערכים נוחשו נכונה, המשחק מסתיים בניצחון.
5. אם לפחות ערך אחד אינו תואם, המחשב מציג אילו ערכים בדיוק היו שגויים.
6. המשחק ממשיך עד שהשחקן מנחש נכונה את כל שלושת הערכים.
-----------------
אלגוריתם:
1. ליצור מספרים שלמים אקראיים `targetA`, `targetB` ו-`targetC` בטווח שבין 1 ל-10.
2. להתחיל לולאה "כל עוד לא נוחשו כל המספרים":
    2.1 לבקש מהשחקן להזין שלושה מספרים שלמים: `userA`, `userB` ו-`userC`.
    2.2 לאתחל את המחרוזת `message` כמחרוזת ריקה.
    2.3 אם `userA` אינו שווה ל-`targetA`, להוסיף את התו "A" ל-`message`.
    2.4 אם `userB` אינו שווה ל-`targetB`, להוסיף את התו "B" ל-`message`.
    2.5 אם `userC` אינו שווה ל-`targetC`, להוסיף את התו "C" ל-`message`.
    2.6 אם `message` אינה ריקה, להציג את ההודעה "שגוי ב- " ואת תוכן `message`.
    2.7 אחרת, להציג את ההודעה "ניחשת נכונה!".
3. סיום המשחק.
-----------------

"""
import random

# יצירת מספרים אקראיים בטווח 1 עד 10 עבור targetA, targetB ו-targetC
targetA = random.randint(1, 10)
targetB = random.randint(1, 10)
targetC = random.randint(1, 10)


# לולאה אינסופית, עד שהשחקן ינחש נכונה את כל המספרים
while True:
    # בקשת קלט של שלושה מספרים מהמשתמש
    try:
        userA = int(input("הזן מספר עבור A (בין 1 ל-10): "))
        userB = int(input("הזן מספר עבור B (בין 1 ל-10): "))
        userC = int(input("הזן מספר עבור C (בין 1 ל-10): "))
    except ValueError:
        print("אנא הזן מספרים שלמים.")
        continue

    # אתחול מחרוזת להודעות שגיאה
    message = ""

    # בדיקה האם המספר A נכון
    if userA != targetA:
        message += "A"  # אם שגוי, הוספת "A" להודעה

    # בדיקה האם המספר B נכון
    if userB != targetB:
        message += "B" # אם שגוי, הוספת "B" להודעה

    # בדיקה האם המספר C נכון
    if userC != targetC:
        message += "C" # אם שגוי, הוספת "C" להודעה

    # אם יש שגיאות, הצגת הודעה המציינת אילו מספרים שגויים
    if message != "":
        print(f"שגוי ב- {message}")
    # אם כל המספרים נכונים, ברכת השחקן וסיום המשחק
    else:
        print("ניחשת נכונה!")
        break
"""
הסבר הקוד:
1.  **ייבוא מודול `random`**:
    -   `import random`: מייבא את מודול `random`, הנדרש ליצירת מספרים אקראיים.

2.  **יצירת מספרים אקראיים**:
    -   `targetA = random.randint(1, 10)`: יוצר מספר שלם אקראי בטווח 1 עד 10 ומקצה אותו למשתנה `targetA`.
    -   `targetB = random.randint(1, 10)`: יוצר מספר שלם אקראי בטווח 1 עד 10 ומקצה אותו למשתנה `targetB`.
    -   `targetC = random.randint(1, 10)`: יוצר מספר שלם אקראי בטווח 1 עד 10 ומקצה אותו למשתנה `targetC`.
   משתנים אלו מייצגים את המספרים שנבחרו על ידי המחשב, שהשחקן צריך לנחש.
3.  **לולאת המשחק הראשית**:
    -   `while True:`: מפעיל לולאה אינסופית, שתמשיך לפעול עד שהשחקן ינחש נכונה את כל שלושת המספרים.
    -   **קלט מספרים מהמשתמש**:
    	-	`try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין ערך שאינו מספר שלם, תוצג הודעת שגיאה.
    	-  `userA = int(input("הזן מספר עבור A (בין 1 ל-10): "))`: מבקש מהמשתמש להזין מספר עבור `A`, ממיר אותו למספר שלם ושומר במשתנה `userA`.
    	-  באופן דומה, מבקש הזנת מספרים עבור `B` ו-`C` ושומר אותם במשתנים `userB` ו-`userC` בהתאמה.
    -   `message = ""`: מאתחל מחרוזת ריקה בשם `message`, שתשמש לאחסון מידע על המספרים שהוזנו באופן שגוי.
4.  **בדיקת המספרים שהוזנו**:
    -  `if userA != targetA:`: בודק האם המספר `userA` שהוזן על ידי המשתמש אינו שווה למספר `targetA` שנבחר.
    -  `message += "A"`: אם `userA` אינו שווה ל-`targetA`, מוסיף את התו "A" למחרוזת `message`, דבר המציין שהמספר `A` הוזן באופן שגוי.
    - בדיקות דומות מבוצעות עבור המספרים `B` ו-`C`.
5.  **הצגת תוצאות**:
    -   `if message != "":`: בודק האם המחרוזת `message` אינה ריקה.
    -   `print(f"שגוי ב- {message}")`: אם המחרוזת `message` אינה ריקה, מציג הודעה על המספרים שהוזנו באופן שגוי (לדוגמה, "שגוי ב- AB" - משמעות הדבר שהמספרים `A` ו-`B` הוזנו באופן שגוי).
    -   `else:`: מבוצע אם המחרוזת `message` ריקה (כלומר, כל המספרים נוחשו נכונה).
    -   `print("ניחשת נכונה!")`: מציג הודעת ניצחון.
    -   `break`: מסיים את הלולאה ואת המשחק, אם כל המספרים נוחשו נכונה.
"""
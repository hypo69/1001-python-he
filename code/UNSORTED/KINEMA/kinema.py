**KINEMA:**
=================
רמת מורכבות: 5
-----------------
המשחק "קינמה" מהווה סימולציה טקסטואלית של תנועת נקודה על מישור. השחקן מזין פקודות להזזת הנקודה שמאלה, ימינה, למעלה ולמטה, והתוכנה מציגה את קואורדינטות הנקודה הנוכחיות ואת המסלול שעברה. מטרת המשחק היא לחקור את המרחב ולהזיז את הנקודה, באמצעות הפקודות הנתונות.

**כללי המשחק:**
1.  בתחילה, הנקודה נמצאת בקואורדינטות (0, 0).
2.  השחקן מזין פקודות להזזת הנקודה. הפקודות הן כדלקמן:
    -   "L" או "l": הזזה שמאלה בעמדה אחת (הקטנת קואורדינטה X ב-1).
    -   "R" או "r": הזזה ימינה בעמדה אחת (הגדלת קואורדינטה X ב-1).
    -   "U" או "u": הזזה למעלה בעמדה אחת (הגדלת קואורדינטה Y ב-1).
    -   "D" או "d": הזזה למטה בעמדה אחת (הקטנת קואורדינטה Y ב-1).
3.  לאחר כל מהלך, התוכנה מציגה את קואורדינטות הנקודה הנוכחיות (X, Y).
4.  המשחק נמשך עד שהשחקן מזין את הפקודה "E" או "e" ליציאה.
-----------------
**אלגוריתם:**
1.  קביעת קואורדינטות התחלתיות X ו-Y ל-0.
2.  הצגת קואורדינטות הנקודה ההתחלתיות (0, 0).
3.  התחלת לולאה "כל עוד לא הוזנה פקודת יציאה (E או e)":
    3.1 בקשת קלט מהשחקן עבור פקודת הזזה (L, R, U, D).
    3.2 אם הפקודה שווה ל-"L" או "l", יש להקטין את קואורדינטה X ב-1.
    3.3 אם הפקודה שווה ל-"R" או "r", יש להגדיל את קואורדינטה X ב-1.
    3.4 אם הפקודה שווה ל-"U" או "u", יש להגדיל את קואורדינטה Y ב-1.
    3.5 אם הפקודה שווה ל-"D" או "d", יש להקטין את קואורדינטה Y ב-1.
    3.6 אם הפקודה שווה ל-"E" או "e", יש לסיים את המשחק.
    3.7 הצגת קואורדינטות הנקודה הנוכחיות (X, Y).
4.  סיום המשחק.
-----------------
**בלוק-דיאגרמה:**
```mermaid
flowchart TD
    Start["התחלה"] --> InitializeVariables["<p align='left'>אתחול משתנים:
    <code><b>
    x = 0
    y = 0
    </b></code></p>"]
    InitializeVariables --> OutputInitialCoordinates["הצגת קואורדינטות התחלתיות: (0, 0)"]
    OutputInitialCoordinates --> LoopStart{"תחילת לולאה: כל עוד לא הוזנה פקודה 'E'"}
    LoopStart -- Да --> InputCommand["קלט פקודה מהמשתמש: <code><b>command</b></code>"]
    InputCommand --> CheckCommandE{"בדיקה: <code><b>command == 'E' or command == 'e'?</b></code>"}
    CheckCommandE -- Да --> End["סיום"]
    CheckCommandE -- Нет --> CheckCommandL{"בדיקה: <code><b>command == 'L' or command == 'l'?</b></code>"}
    CheckCommandL -- Да --> MoveLeft["<code><b>x = x - 1</b></code>"]
    MoveLeft --> OutputCoordinates["הצגת קואורדינטות נוכחיות: <code><b>(x, y)</b></code>"]
    OutputCoordinates --> LoopStart
    CheckCommandL -- Нет --> CheckCommandR{"בדיקה: <code><b>command == 'R' or command == 'r'?</b></code>"}
    CheckCommandR -- Да --> MoveRight["<code><b>x = x + 1</b></code>"]
    MoveRight --> OutputCoordinates
    CheckCommandR -- Нет --> CheckCommandU{"בדיקה: <code><b>command == 'U' or command == 'u'?</b></code>"}
    CheckCommandU -- Да --> MoveUp["<code><b>y = y + 1</b></code>"]
    MoveUp --> OutputCoordinates
    CheckCommandU -- Нет --> CheckCommandD{"בדיקה: <code><b>command == 'D' or command == 'd'?</b></code>"}
     CheckCommandD -- Да --> MoveDown["<code><b>y = y - 1</b></code>"]
    MoveDown --> OutputCoordinates
    CheckCommandD -- Нет --> OutputInvalidCommand["הצגת הודעה: <b>פקודה לא חוקית</b>"]
    OutputInvalidCommand --> LoopStart
    LoopStart -- Нет --> End
```
**מקרא:**
   Start - תחילת התוכנית.
    InitializeVariables - אתחול משתנים: x (קואורדינטה X) ו-y (קואורדינטה Y) נקבעים ל-0.
    OutputInitialCoordinates - הצגת קואורדינטות הנקודה ההתחלתיות (0, 0).
    LoopStart - תחילת לולאה, שנמשכת עד שת הוזנה פקודה "E" או "e".
    InputCommand - בקשת קלט מהמשתמש עבור פקודת הזזה.
    CheckCommandE - בדיקה האם הפקודה שהוזנה שווה ל-"E" או "e" (פקודת יציאה).
    End - סיום התוכנית.
    CheckCommandL - בדיקה האם הפקודה שהוזנה שווה ל-"L" או "l" (הזזה שמאלה).
    MoveLeft - הקטנת קואורדינטה x ב-1.
    OutputCoordinates - הצגת קואורדינטות הנקודה הנוכחיות (x, y).
    CheckCommandR - בדיקה האם הפקודה שהוזנה שווה ל-"R" או "r" (הזזה ימינה).
    MoveRight - הגדלת קואורדינטה x ב-1.
    CheckCommandU - בדיקה האם הפקודה שהוזנה שווה ל-"U" או "u" (הזזה למעלה).
    MoveUp - הגדלת קואורדינטה y ב-1.
    CheckCommandD - בדיקה האם הפקודה שהוזנה שווה ל-"D" או "d" (הזזה למטה).
    MoveDown - הקטנת קואורדינטה y ב-1.
    OutputInvalidCommand - הצגת הודעה "פקודה לא חוקית", אם הפקודה שהוזנה לא זוהתה.
```


import time
# אתחול קואורדינטות
x = 0 # קואורדינטה X
y = 0 # קואורדינטה Y

# הצגת קואורדינטות התחלתיות
print("Начальные координаты: (0, 0)")
time.sleep(0.5) # השהיה קצרה

# לולאת המשחק הראשית
while True:
    # בקשת קלט פקודה מהמשתמש
    command = input("Введите команду (L/R/U/D или E для выхода): ").upper() # ממירים לאותיות רישיות כדי לפשט את ההשוואה

    # בדיקת פקודת יציאה מהמשחק
    if command == "E":
        print("Игра окончена.")
        break # יציאה מהלולאה, סיום המשחק

    # הזזת הנקודה בהתאם לפקודה
    if command == "L":
        x -= 1 # הקטנת קואורדינטה X ב-1
    elif command == "R":
        x += 1 # הגדלת קואורדינטה X ב-1
    elif command == "U":
        y += 1 # הגדלת קואורדינטה Y ב-1
    elif command == "D":
        y -= 1 # הקטנת קואורדינטה Y ב-1
    else:
        print("Неверная команда. Пожалуйста, используйте L, R, U, D или E.")
        continue # אם הפקודה שגויה, ממשיכים לתחילת הלולאה.

    # הצגת קואורדינטות נוכחיות
    print(f"Текущие координаты: ({x}, {y})")
    time.sleep(0.5) # השהיה קצרה
"""
הסבר הקוד:
1. **אתחול קואורדינטות**:
   - `x = 0`: המשתנה `x` מייצג את הקואורדינטה האופקית (X) ומאותחל לאפס.
   - `y = 0`: המשתנה `y` מייצג את הקואורדינטה האנכית (Y) ומאותחל לאפס.
2. **הצגת קואורדינטות התחלתיות**:
   - `print("Начальные координаты: (0, 0)")`: מציג הודעה עם קואורדינטות הנקודה ההתחלתיות.
   - `time.sleep(0.5)`: עוצר את ביצוע התוכנית למשך 0.5 שניות, לנוחות המשתמש.
3. **לולאת המשחק הראשית**:
   - `while True:`: לולאה אינסופית, המתבצעת כל עוד המשתמש לא הזין פקודת יציאה.
4. **קלט פקודה**:
    - `command = input("Введите команду (L/R/U/D или E для выхода): ").upper()`: מבקש מהמשתמש להזין פקודה, באמצעות `input()`. הפקודה `.upper()` ממירה את המחרוזת שהוזנה לאותיות רישיות, כדי שהשוואת הפקודות לא תהיה תלוית רישיות/אותיות קטנות.
5. **בדיקת פקודת יציאה**:
   - `if command == "E":`: בודק האם המשתמש הזין את פקודת היציאה ("E").
   - `print("Игра окончена.")`: מציג הודעה על סיום המשחק.
   - `break`: יוצא מהלולאה `while`, ומסיים את התוכנית.
6. **עיבוד פקודות הזזה**:
   - `if command == "L":`: בודק האם הוזנה הפקודה "L" (שמאלה).
     - `x -= 1`: מקטין את קואורדינטה `x` ב-1.
   - `elif command == "R":`: בודק האם הוזנה הפקודה "R" (ימינה).
     - `x += 1`: מגדיל את קואורדינטה `x` ב-1.
   - `elif command == "U":`: בודק האם הוזנה הפקודה "U" (למעלה).
     - `y += 1`: מגדיל את קואורדינטה `y` ב-1.
   - `elif command == "D":`: בודק האם הוזנה הפקודה "D" (למטה).
     - `y -= 1`: מקטין את קואורדינטה `y` ב-1.
   - `else:`: אם הוזנה פקודה שאינה תואמת לאף אחת מהפקודות החוקיות, מוצגת הודעת שגיאה וממשיכים לאיטרציה הבאה של הלולאה.
7. **הצגת קואורדינטות נוכחיות**:
   - `print(f"Текущие координаты: ({x}, {y})")`: מציג את קואורדינטות הנקודה הנוכחיות.
   - `time.sleep(0.5)`: עוצר את ביצוע התוכנית למשך 0.5 שניות, לנוחות המשתמש.
"""
```
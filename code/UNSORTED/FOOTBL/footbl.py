FOOTBL:
=================
רמת מורכבות: 5
-----------------
המשחק "כדורגל" הוא משחק טקסט בו שני שחקנים מזינים לסירוגין מספרים, המייצגים ניסיונות בעיטה לשער. המשחק נמשך עד לרגע בו נכבש גול. מטרת המשחק היא להבקיח שער נגד היריב.

כללי המשחק:
1. שני שחקנים מזינים לסירוגין מספרים בין 1 ל-10, המייצגים ניסיונות בעיטה לשער.
2. המספר "1" פירושו שהבעיטה החטיאה את המטרה, כל מספר אחר פירושו שהשחקן הבקיע גול.
3. המשחק מסתיים כאשר אחד השחקנים מבקיע גול.
-----------------
אלגוריתם:
1. אתחל את מספר השחקן, התחל עם השחקן הראשון (1).
2. התחל את לולאת המשחק, עד שלא יכבש גול.
3. הצג הודעה "PLAYER" + מספר השחקן + "---YOUR SHOT".
4. בקש מהשחקן להזין מספר (ניסיון בעיטה).
5. אם המספר שהוזן שווה ל-1, עבור לשלב 7.
6. הצג הודעה "GOAL !!! PLAYER" + מספר השחקן + "WINS !!!"
7. שנה את מספר השחקן ליריב, כלומר אם היה שחקן 1, עכשיו שחקן 2, ולהיפך.
8. חזור לשלב 3.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InitializePlayer["אתחול: <code>currentPlayer = 1</code>"]
    InitializePlayer --> GameLoopStart{"תחילת לולאה: עד שנכבש גול"}
    GameLoopStart --> OutputPlayerTurn["הצגת הודעה: <code>'PLAYER ' + currentPlayer + ' - YOUR SHOT'</code>"]
    OutputPlayerTurn --> InputShot["קלט מספר על ידי המשתמש: <code>shot</code>"]
    InputShot --> CheckGoal{"בדיקה: <code>shot == 1?</code>"}
    CheckGoal -- לא --> OutputWin["הצגת הודעה: <code>'GOAL!!! PLAYER ' + currentPlayer + ' WINS!!!'</code>"]
    OutputWin --> End["סיום"]
    CheckGoal -- כן --> SwitchPlayer["החלפת שחקן: <code>currentPlayer = 3 - currentPlayer</code>"]
    SwitchPlayer --> GameLoopStart
    GameLoopStart -- לא --> End
```
מקרא:
    Start - תחילת המשחק.
    InitializePlayer - אתחול המשתנה currentPlayer (השחקן הנוכחי) בערך 1.
    GameLoopStart - תחילת לולאת המשחק, הנמשכת עד שנכבש גול.
    OutputPlayerTurn - הצגת הודעה המציינת כי תורו של השחקן הנוכחי.
    InputShot - בקשה מהשחקן הנוכחי להזין מספר (ניסיון הבעיטה שלו).
    CheckGoal - בדיקה האם המספר שהוזן (shot) שווה ל-1. אם שווה ל-1, הבעיטה החטיאה את המטרה; אם לא, נכבש גול.
    OutputWin - הצגת הודעה המציינת כי השחקן הנוכחי הבקיע גול וניצח.
    End - סיום המשחק.
    SwitchPlayer - מעבר לתור השחקן הבא. אם היה שחקן 1, הבא הוא 2, ואם 2, הבא הוא 1.
```

# אתחול השחקן הנוכחי (שחקן 1 מתחיל)
currentPlayer = 1

# לולאת המשחק הראשית
while True:
    # הצגת הודעה על תור השחקן הנוכחי
    print(f"PLAYER {currentPlayer} --- YOUR SHOT")
    
    # בקשת קלט מספר מהשחקן
    try:
        shot = int(input("Введите число от 1 до 10: "))
    except ValueError:
        print("Пожалуйста, введите целое число.")
        continue
        
    # בדיקה האם נכבש גול (אם shot != 1)
    if shot != 1:
        print(f"GOAL!!! PLAYER {currentPlayer} WINS!!!")
        break # סיום המשחק
    
    # החלפת שחקן: אם היה שחקן 1, אז עכשיו 2, ולהיפך
    currentPlayer = 3 - currentPlayer
    
"""
הסבר הקוד:
1.  **אתחול השחקן הנוכחי**:
    -   `currentPlayer = 1`: קובע את הערך ההתחלתי של המשתנה `currentPlayer` ל-1, מה שאומר שהשחקן הראשון מתחיל את המשחק.

2.  **לולאת המשחק הראשית `while True:`**:
    -   לולאה אינסופית שנמשכת עד שאחד השחקנים מבקיע גול (פעולת `break` מתבצעת).

3.  **הצגת הודעה על השחקן הנוכחי**:
    -   `print(f"PLAYER {currentPlayer} --- YOUR SHOT")`: מציגה הודעה המציינת תורו של מי כעת.

4.  **בקשת קלט מספר מהשחקן**:
   -  `try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין ערך שאינו מספר שלם, תוצג הודעת שגיאה.
    -   `shot = int(input("Введите число от 1 до 10: "))`: מבקש קלט של מספר מהשחקן הנוכחי וממיר את המחרוזת שהוזנה למספר שלם. מספר זה מייצג ניסיון בעיטה.

5.  **בדיקה האם נכבש גול**:
    -   `if shot != 1:`: בודק האם המספר שהוזן אינו שווה ל-1. אם המספר אינו 1, זה נחשב לגול.
    -   `print(f"GOAL!!! PLAYER {currentPlayer} WINS!!!")`: מציג הודעה שהשחקן הבקיע גול וניצח במשחק.
    -   `break`: מסיים את לולאת המשחק ואת המשחק כולו.

6.  **החלפת שחקן**:
    -   `currentPlayer = 3 - currentPlayer`: מחליף את השחקן הנוכחי ליריב. אם `currentPlayer` היה 1, הוא יהפוך ל-2, ולהיפך.

"""
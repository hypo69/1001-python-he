FOOTBL:
=================
דרגת קושי: 5
-----------------
המשחק "כדורגל" הוא משחק מבוסס טקסט, שבו שני שחקנים מזינים בתורם מספרים, המייצגים ניסיונות בעיטה לשער. המשחק נמשך עד אשר יובקע שער. מטרת המשחק היא להבקיע שער נגד היריב.

כללי המשחק:
1. שני שחקנים מזינים בתורם מספרים בין 1 ל-10, המייצגים ניסיונות בעיטה לשער.
2. המספר "1" משמעותו שהבעיטה החטיאה את השער, כל מספר אחר משמעותו שהשחקן הבקיע שער.
3. המשחק מסתיים כאשר אחד השחקנים מבקיע שער.
-----------------
אלגוריתם:
1. אתחול מספר השחקן, להתחיל עם השחקן הראשון (1).
2. התחלת לולאת המשחק, כל עוד לא הובקע שער.
3.   הצגת הודעה "PLAYER" + מספר השחקן + "---YOUR SHOT".
4.   בקשת קלט מהשחקן להזין מספר (ניסיון בעיטה).
5.   אם המספר שהוזן שווה ל-1, עבור לשלב 7.
6.   הצגת הודעה "GOAL !!! PLAYER" + מספר השחקן + "WINS !!!".
7.   שינוי מספר השחקן לשחקן הנגדי, כלומר, אם השחקן היה 1, כעת השחקן הוא 2, ולהיפך.
8. חזרה לשלב 3.
-----------------
דיאגרמת זרימה:
```mermaid
flowchart TD
    Start["Начало"] --> InitializePlayer["Инициализация: <code>currentPlayer = 1</code>"]
    InitializePlayer --> GameLoopStart{"Начало цикла: пока не забит гол"}
    GameLoopStart --> OutputPlayerTurn["Вывод сообщения: <code>'PLAYER ' + currentPlayer + ' - YOUR SHOT'</code>"]
    OutputPlayerTurn --> InputShot["Ввод числа пользователем: <code>shot</code>"]
    InputShot --> CheckGoal{"Проверка: <code>shot == 1?</code>"}
    CheckGoal -- Нет --> OutputWin["Вывод сообщения: <code>'GOAL!!! PLAYER ' + currentPlayer + ' WINS!!!'</code>"]
    OutputWin --> End["Конец"]
    CheckGoal -- Да --> SwitchPlayer["<code>currentPlayer = 3 - currentPlayer</code>"]
    SwitchPlayer --> GameLoopStart
    GameLoopStart -- Нет --> End
```
מקרא:
    Start - תחילת המשחק.
    InitializePlayer - אתחול המשתנה currentPlayer (השחקן הנוכחי) בערך 1.
    GameLoopStart - תחילת לולאת המשחק, הנמשכת כל עוד לא הובקע שער.
    OutputPlayerTurn - הצגת הודעה המציינת כי תור השחקן הנוכחי הגיע.
    InputShot - בקשת קלט מהשחקן הנוכחי להזין מספר (ניסיון הבעיטה שלו).
    CheckGoal - בדיקה האם המספר שהוזן (shot) שווה ל-1. אם שווה ל-1, הבעיטה החטיאה את המטרה; אם לא, הובקע שער.
    OutputWin - הצגת הודעה המציינת כי השחקן הנוכחי הבקיע שער וניצח.
    End - סוף המשחק.
    SwitchPlayer - מעבר לתור השחקן הבא. אם היה שחקן 1, הבא הוא 2; אם 2, הבא הוא 1.
"""


# אתחול השחקן הנוכחי (השחקן 1 מתחיל)
currentPlayer = 1

# לולאת המשחק הראשית
while True:
    # הצגת הודעה על תור השחקן הנוכחי
    print(f"PLAYER {currentPlayer} --- YOUR SHOT")

    # בקשת קלט מספר מהשחקן
    try:
        shot = int(input("Введите число от 1 до 10: "))
    except ValueError:
        print("Пожалуйста, введите целое число.")
        continue

    # בדיקה האם הובקע שער (אם shot != 1)
    if shot != 1:
        print(f"GOAL!!! PLAYER {currentPlayer} WINS!!!")
        break # סיום המשחק

    # החלפת שחקן: אם היה שחקן 1, עכשיו 2, ולהיפך
    currentPlayer = 3 - currentPlayer

"""
הסבר על הקוד:
1.  **אתחול השחקן הנוכחי**:
    -   `currentPlayer = 1`: קובע את הערך ההתחלתי של המשתנה `currentPlayer` ל-1, מה שאומר שהשחקן הראשון מתחיל את המשחק.

2.  **לולאת המשחק הראשית `while True:`**:
    -   לולאה אינסופית, הנמשכת עד אשר אחד השחקנים יבקיע שער (לא יבוצע האופרטור `break`).

3.  **הצגת הודעה על השחקן הנוכחי**:
    -   `print(f"PLAYER {currentPlayer} --- YOUR SHOT")`: מציגה הודעה המציינת תורו של מי כעת.

4.  **בקשת קלט מספר מהשחקן**:
   -  `try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין ערך שאינו מספר שלם, תוצג הודעת שגיאה.
    -   `shot = int(input("Введите число от 1 до 10: "))`: מבקשת קלט מספר מהשחקן הנוכחי וממירה את המחרוזת שהוזנה למספר שלם. מספר זה מייצג ניסיון בעיטה.

5.  **בדיקה האם הובקע שער**:
    -   `if shot != 1:`: בודקת האם המספר שהוזן אינו שווה ל-1. אם המספר אינו שווה ל-1, הדבר נחשב לשער.
    -   `print(f"GOAL!!! PLAYER {currentPlayer} WINS!!!")`: מציגה הודעה המציינת שהשחקן הבקיע שער וניצח במשחק.
    -   `break`: מסיים את לולאת המשחק ואת המשחק.

6.  **החלפת שחקן**:
    -   `currentPlayer = 3 - currentPlayer`: מחליף את השחקן הנוכחי לשחקן הנגדי. אם `currentPlayer` היה 1, הוא יהפוך ל-2, ולהיפך.

"""
BOMBER:
=================
רמת קושי: 5
-----------------
המשחק "בומבר" הוא משחק חידה בו השחקן מנסה להטיל פצצה על מטרה הממוקמת במרחק שנוצר באקראי. השחקן מזין את זווית ההטלה ואת מרחק ההטלה, והמחשב מחשב את מסלול הפצצה. מטרת המשחק היא לפגוע במטרה בדיוק מרבי.

חוקי המשחק:
1. המחשב יוצר מרחק אקראי למטרה בטווח של 100 עד 1000.
2. השחקן מזין את זווית הטלת הפצצה ואת מרחק הטלת הפצצה.
3. המחשב מחשב את המרחק אליו תנחת הפצצה.
4. אם מרחק נחיתת הפצצה נמצא בטווח של 10 יחידות ממרחק המטרה, השחקן מנצח.
5. אם מרחק נחיתת הפצצה אינו נופל בטווח, השחקן מפסיד.
-----------------
אלגוריתם:
1. יצירת מרחק אקראי למטרה בטווח של 100 עד 1000 והקצאתו למשתנה `targetDistance`.
2. בקשת זווית הטלת הפצצה במעלות (`angle`) ומרחק הטלה (`distance`) מהשחקן.
3. המרת הזווית ממעלות לרדיאנים: `angleInRadians = angle * 3.14159 / 180`.
4. חישוב מרחק נחיתת הפצצה לפי הנוסחה: `dropDistance = distance * cos(angleInRadians)`.
5. חישוב ההפרש בין מרחק נחיתת הפצצה למרחק המטרה: `distanceDifference = abs(dropDistance - targetDistance)`.
6. אם ההפרש בין המרחקים קטן או שווה ל-10, להציג הודעת ניצחון.
7. אחרת, להציג הודעת הפסד.
8. סוף המשחק.
-----------------

```python
import random
import math

# יצירת מרחק אקראי למטרה מ-100 עד 1000
targetDistance = random.randint(100, 1000)

# בקשת קלט עבור זווית ומרחק הטלת הפצצה
try:
    angle = float(input("הזן את זווית הטלת הפצצה במעלות: "))
    distance = float(input("הזן את מרחק הטלת הפצצה: "))
except ValueError:
    print("שגיאה: אנא הזן ערכים מספריים עבור הזווית והמרחק.")
    exit()

# המרת הזווית ממעלות לרדיאנים
angleInRadians = angle * math.pi / 180

# חישוב המרחק אליו תנחת הפצצה
dropDistance = distance * math.cos(angleInRadians)

# חישוב ההפרש בין מרחק נחיתת הפצצה למרחק המטרה
distanceDifference = abs(dropDistance - targetDistance)

# בדיקת פגיעה במטרה
if distanceDifference <= 10:
    print("ברכות! פגעת במטרה!")
else:
    print("החטאת!")
```
"""
הסבר הקוד:
1.  **ייבוא מודולים**:
    -   `import random`: מייבא את המודול `random` ליצירת מספרים אקראיים.
    -   `import math`: מייבא את המודול `math` לשימוש בפונקציות מתמטיות, כגון `cos` ו-`pi`.
2.  **יצירת מרחק אקראי למטרה**:
    -   `targetDistance = random.randint(100, 1000)`: יוצר מספר שלם אקראי בטווח של 100 עד 1000 כולל, המייצג את המרחק למטרה.
3.  **קלט נתונים מהמשתמש**:
    -   `try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין ערך שאינו מספר, תוצג הודעת שגיאה.
    -   `angle = float(input("הזן את זווית הטלת הפצצה במעלות: "))`: מבקש מהמשתמש את זווית הטלת הפצצה במעלות וממיר את הערך שהוזן למספר עם נקודה עשרונית.
    -   `distance = float(input("הזן את מרחק הטלת הפצצה: "))`: מבקש מהמשתמש את מרחק הטלת הפצצה וממיר את הערך שהוזן למספר עם נקודה עשרונית.
    -   `exit()`: אם הוזן ערך שאינו מספר, התוכנית מסתיימת.
4.  **המרת הזווית לרדיאנים**:
    -   `angleInRadians = angle * math.pi / 180`: ממיר את הזווית ממעלות לרדיאנים, כיוון שהפונקציה `cos()` במודול `math` מקבלת ארגומנט ברדיאנים.
5.  **חישוב מרחק נחיתת הפצצה**:
    -   `dropDistance = distance * math.cos(angleInRadians)`: מחשב את המרחק אליו תנחת הפצצה, תוך שימוש במרחק שהוזן ובקוסינוס הזווית ברדיאנים.
6.  **חישוב ההפרש במרחקים**:
    -   `distanceDifference = abs(dropDistance - targetDistance)`: מחשב את ההפרש המוחלט בין מרחק נחיתת הפצצה למרחק המטרה.
7.  **בדיקת פגיעה במטרה**:
    -   `if distanceDifference <= 10`: בודק האם ההפרש בין המרחקים נמצא בטווח של 10 יחידות.
    -   `print("ברכות! פגעת במטרה!")`: מציג הודעת ניצחון אם הפצצה פגעה במטרה.
    -   `else`: אם ההפרש גדול מ-10, הפצצה לא פגעה במטרה.
    -   `print("החטאת!")`: מציג הודעת הפסד.
"""
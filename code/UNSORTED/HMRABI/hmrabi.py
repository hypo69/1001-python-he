<HMRABI>:
=================
מורכבות: 4
-----------------
המשחק "חמוראפי" הוא משחק כלכלי מבוסס טקסט, בו השחקן מנהל עיר עתיקה, בניסיון לשרוד ולשגשג בתנאי משאבים מוגבלים. השחקן מקבל החלטות בנוגע לקנייה ומכירה של קרקע, חלוקת היבול, זריעה, ועוקב אחר רמות הרעב וגודל האוכלוסייה. המשחק מסתיים אם אוכלוסיית העיר גוועת ברעב, או לאחר שחלפו 10 שנים במשחק.

כללי המשחק:
1. השחקן מתחיל את המשחק עם 100 תושבים, 2800 בושלים של תבואה ו-1000 אקרים של קרקע.
2. מדי שנה, השחקן מקבל החלטות בנוגע לקנייה או מכירה של קרקע, כמות התבואה המשמשת לזריעה, והאכלת האוכלוסייה.
3. שטח הקרקע נמדד באקרים, התבואה בבושלים.
4. אירועים אקראיים, כמו פלישת עכברים, יכולים להשפיע על מלאי התבואה.
5. העיר שורדת אם יש מספיק תבואה להזנת האוכלוסייה, אחרת האוכלוסייה גוועת ברעב.
6. המשחק מסתיים לאחר 10 שנים, או כאשר העיר נחרבת לחלוטין (כל האוכלוסייה נספתה).
7. תפוקת היבול של הקרקע משתנה מדי שנה (מספר אקראי בין 1 ל-8).
8. מחיר הקרקע משתנה מדי שנה בין 15 ל-25 בושלים לאקר.

-----------------
אלגוריתם:
1. אתחול משתנים: year = 0, population = 100, grain = 2800, land = 1000.
2. התחלת לולאת משחק ל-10 שנים או עד שהאוכלוסייה שווה ל-0.
3. הצגת השנה הנוכחית, מספר התושבים, כמות התבואה וכמות הקרקע.
4. חישוב והצגת מחיר הקרקע לאקר (מספר אקראי בין 15 ל-25).
5. בקשת כמות אקרים של קרקע לקנייה או מכירה מהשחקן (מספר חיובי לקנייה, מספר שלילי למכירה).
6. בדיקה האם לשחקן יש מספיק תבואה לקניית קרקע, והאם יש מספיק קרקע למכירה.
7. עדכון כמות הקרקע והתבואה.
8. בקשת כמות בושלים של תבואה לזריעה מהשחקן.
9. בדיקה האם יש מספיק תבואה לזריעה.
10. חישוב תפוקת היבול (מספר אקראי בין 1 ל-8 בושלים לאקר).
11. חישוב והוספת יבול תבואה חדש למלאי הכולל.
12. חישוב כמות התבואה שנאכלה על ידי עכברים (מספר אקראי בין 0 ל-10%).
13. הפחתת כמות התבואה בכמות שנאכלה על ידי עכברים.
14. בקשת כמות תבואה להאכלת האוכלוסייה מהשחקן.
15. אם אין מספיק תבואה להאכלה, האוכלוסייה גוועת ברעב באופן יחסי לחוסר (מחצית האוכלוסייה גוועת אם חסרים 50% מהמלאי).
16. אם כל האוכלוסייה מתה, הצגת הודעת הפסד וסיום המשחק.
17. אם יש מספיק תבואה להאכלה, הגדלת האוכלוסייה ב-10% או עד 1000, לפי הנמוך מביניהם.
18. הגדלת השנה ב-1.
19. אם חלפו 10 שנים, הצגת הודעה על סיום המשחק.
20. סיום המשחק.

-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["Начало"] --> InitializeVariables["<p align='left'>Инициализация переменных:
    <code><b>
    year = 0<br>
    population = 100<br>
    grain = 2800<br>
    land = 1000
    </b></code></p>"]
    InitializeVariables --> LoopStart{"Начало цикла: пока <code><b>year < 10</b></code> и <code><b>population > 0</b></code>"}
    LoopStart -- Да --> DisplayStatus["<p align='left'>Вывод текущего статуса:
    <code><b>year</b>, <b>population</b>, <b>grain</b>, <b>land</b></code></p>"]
    DisplayStatus --> CalculateLandPrice["Вычисление цены земли: <code><b>landPrice = random(15, 25)</b></code>"]
    CalculateLandPrice --> DisplayLandPrice["Вывод цены земли: <code><b>landPrice</b></code>"]
    DisplayLandPrice --> InputLandTrade["Ввод: <code><b>landToBuyOrSell</b></code> (положительное - купить, отрицательное - продать)"]
    InputLandTrade --> CheckLandTrade["Проверка: хватает ли зерна для покупки или земли для продажи?"]
    CheckLandTrade -- Да --> UpdateLandAndGrain["<p align='left'>Обновление запасов:
    <code><b>land += landToBuyOrSell</b><br>
    <b>grain -= landToBuyOrSell * landPrice</b></code></p>"]
    UpdateLandAndGrain --> InputGrainToPlant["Ввод: <code><b>grainToPlant</b></code> (количество зерна для посева)"]
    InputGrainToPlant --> CheckGrainToPlant["Проверка: хватает ли зерна для посева?"]
    CheckGrainToPlant -- Да --> CalculateHarvest["Вычисление урожая: <code><b>yield = random(1, 8)</b></code><br><code><b>harvest = grainToPlant * yield</b></code>"]
    CalculateHarvest --> UpdateGrainWithHarvest["<code><b>grain += harvest</b></code>"]
    UpdateGrainWithHarvest --> CalculateRatsDamage["<p align='left'>Вычисление урона от крыс:
    <code><b>ratsDamage = random(0, 0.1) * grain</b></code></p>"]
    CalculateRatsDamage --> UpdateGrainWithRats["<code><b>grain -= ratsDamage</b></code>"]
    UpdateGrainWithRats --> InputGrainToFeed["Ввод: <code><b>grainToFeed</b></code> (количество зерна для пропитания)"]
    InputGrainToFeed --> CheckGrainToFeed["Проверка: хватает ли зерна для пропитания?"]
    CheckGrainToFeed -- Да --> UpdatePopulation["<code><b>population = min(1000, population * 1.1)</b></code>"]
    UpdatePopulation --> IncreaseYear["<code><b>year += 1</b></code>"]
    IncreaseYear --> LoopStart
    CheckGrainToFeed -- Нет --> CalculateStarvation["<p align='left'>Вычисление голода:
    <code><b>starved = population * (1 - grainToFeed/requiredGrain)</b></code></p>"]
    CalculateStarvation --> UpdatePopulationWithStarvation["<code><b>population -= starved</b></code>"]
    UpdatePopulationWithStarvation --> CheckPopulation["Проверка: <code><b>population <= 0</b></code>?"]
        CheckPopulation -- Да --> OutputGameOver["Вывод сообщения: <b>GAME OVER! You starved your population!</b>"]
        OutputGameOver --> End["Конец"]
    CheckPopulation -- Нет --> IncreaseYear
    CheckLandTrade -- Нет --> OutputErrorLand["Вывод сообщения: <b>Not enough resource for trade!</b>"]
    OutputErrorLand --> InputLandTrade
    CheckGrainToPlant -- Нет --> OutputErrorPlant["Вывод сообщения: <b>Not enough grain for planting!</b>"]
    OutputErrorPlant --> InputGrainToPlant
    LoopStart -- Нет --> OutputGameEnd["Вывод сообщения: <b>Game finished after 10 years</b>"]
    OutputGameEnd --> End
```

מקרא:
    Start - התחלת המשחק.
    InitializeVariables - אתחול ערכים התחלתיים למשתנים: השנה (year) מוגדרת ל-0, האוכלוסייה (population) ל-100, מלאי התבואה (grain) ל-2800, שטח הקרקע (land) ל-1000 אקרים.
    LoopStart - התחלת לולאת המשחק, הנמשכת כל עוד לא חלפו 10 שנים או עד שהאוכלוסייה שווה ל-0.
    DisplayStatus - הצגת הסטטוס הנוכחי של המשחק: השנה, מספר התושבים, מלאי התבואה ושטח הקרקע.
    CalculateLandPrice - חישוב מחיר אקראי לאקר קרקע בטווח של 15 עד 25 בושלים.
    DisplayLandPrice - הצגת מחיר אקר הקרקע.
    InputLandTrade - בקשת מספר אקרים של קרקע לקנייה (ערך חיובי) או מכירה (ערך שלילי) מהמשתמש.
    CheckLandTrade - בדיקה האם קיימים מספיק משאבים לביצוע עסקת קנייה/מכירה של קרקע.
    UpdateLandAndGrain - עדכון מלאי הקרקע והתבואה בהתאם לעסקה שבוצעה.
    InputGrainToPlant - בקשת כמות תבואה מהמשתמש לשימוש בזריעה.
    CheckGrainToPlant - בדיקה האם יש מספיק תבואה לזריעה.
    CalculateHarvest - חישוב היבול על בסיס כמות התבואה שנזרעה ותפוקת יבול אקראית לאקר (בין 1 ל-8 בושלים).
    UpdateGrainWithHarvest - עדכון מלאי התבואה עם היבול שנאסף.
    CalculateRatsDamage - חישוב נזק מעכברים (ערך אקראי בין 0 ל-10% ממלאי התבואה).
    UpdateGrainWithRats - הפחתת כמות התבואה בגובה הנזק מהעכברים.
    InputGrainToFeed - בקשת כמות תבואה מהמשתמש לשימוש בהאכלת האוכלוסייה.
    CheckGrainToFeed - בדיקה האם יש מספיק תבואה להזנת כל האוכלוסייה.
    UpdatePopulation - הגדלת האוכלוסייה ב-10% (אך לא יותר מ-1000 איש), אם יש מספיק תבואה.
    IncreaseYear - הגדלת השנה הנוכחית ב-1.
    CalculateStarvation - חישוב מספר התושבים שמתו מרעב, אם אין מספיק תבואה.
    UpdatePopulationWithStarvation - עדכון האוכלוסייה תוך התחשבות במתים מרעב.
    CheckPopulation - בדיקה האם כל האוכלוסייה מתה מרעב.
    OutputGameOver - הצגת הודעה על הפסד, אם כל האוכלוסייה מתה מרעב.
    OutputErrorLand - הצגת הודעת שגיאה אם אין מספיק משאבים למסחר בקרקע.
    OutputErrorPlant - הצגת הודעת שגיאה אם אין מספיק תבואה לזריעה.
    OutputGameEnd - הצגת הודעה על סיום המשחק לאחר 10 שנים.
    End - סיום המשחק.
```
```python
import random

# Инициализация переменных
year = 0          # текущий год
population = 100  # начальное население
grain = 2800      # начальное количество зерна
land = 1000       # начальная площадь земли

print("Добро пожаловать в игру Хаммурапи!")

# Основной игровой цикл, который выполняется пока не пройдет 10 лет или пока все население не вымрет.
while year < 10 and population > 0:
    print(f"\nГод {year + 1}")
    print(f"Население: {population}")
    print(f"Зерно: {grain} бушелей")
    print(f"Земля: {land} акров")

    # Вычисляем случайную цену земли от 15 до 25 бушелей за акр
    land_price = random.randint(15, 25)
    print(f"Цена земли: {land_price} бушелей за акр")

    # Спрашиваем у игрока сколько земли купить или продать.
    while True:
        try:
            land_trade = int(input("Сколько земли купить (+) или продать (-)? "))
            break
        except ValueError:
            print("Пожалуйста, введите целое число.")

    # Проверяем, достаточно ли зерна для покупки или земли для продажи. Если нет, просим ввести данные еще раз.
    while True:
        if land_trade > 0 and grain < land_trade * land_price:
            print("Недостаточно зерна для покупки земли.")
            try:
                land_trade = int(input("Сколько земли купить (+) или продать (-)? "))
            except ValueError:
                print("Пожалуйста, введите целое число.")
                continue

        elif land_trade < 0 and land < abs(land_trade):
            print("Недостаточно земли для продажи.")
            try:
                land_trade = int(input("Сколько земли купить (+) или продать (-)? "))
            except ValueError:
                print("Пожалуйста, введите целое число.")
                continue
        else:
            break

    # Обновляем количество земли и зерна
    land += land_trade
    grain -= land_trade * land_price

    # Спрашиваем сколько зерна посеять
    while True:
        try:
            grain_to_plant = int(input("Сколько зерна использовать для посева? "))
            break
        except ValueError:
            print("Пожалуйста, введите целое число.")

    # Проверяем, достаточно ли зерна для посева. Если нет, просим ввести данные еще раз.
    while True:
        if grain < grain_to_plant:
            print("Недостаточно зерна для посева.")
            try:
                grain_to_plant = int(input("Сколько зерна использовать для посева? "))
            except ValueError:
                print("Пожалуйста, введите целое число.")
                continue
        else:
            break

    # Вычисляем случайный урожай от 1 до 8 бушелей на акр
    yield_per_acre = random.randint(1, 8)
    harvest = grain_to_plant * yield_per_acre
    grain += harvest

    # Вычисляем количество зерна съеденное крысами, от 0 до 10% от всего зерна.
    rats_damage = int(random.random() * 0.1 * grain)
    grain -= rats_damage
    print(f"Крысы съели {rats_damage} бушелей зерна.")

    # Спрашиваем, сколько зерна отдать на пропитание.
    while True:
        try:
            grain_to_feed = int(input("Сколько зерна отдать на пропитание? "))
            break
        except ValueError:
           print("Пожалуйста, введите целое число.")

    # Проверяем, хватит ли зерна на пропитание.
    if grain_to_feed >= population:
        population = min(1000, int(population * 1.1))  # Увеличиваем популяцию на 10%, но не более 1000.
        grain -= grain_to_feed
        print("Все сыты, население растёт!")
    else:
        starved = int(population * (1 - grain_to_feed / population )) # Рассчитываем число умерших от голода, пропорционально недостатку зерна.
        population -= starved
        grain -= grain_to_feed
        print(f"{starved} человек умерло от голода.")
        if population <= 0:
            print("Вы проиграли, все население вымерло от голода!")

    year += 1 # увеличиваем год на 1

if year == 10:
    print("Игра окончена. Прошло 10 лет.")

"""
הסבר קוד:
1. **ייבוא מודול random**:
    - `import random`: מייבא את המודול random המשמש ליצירת מספרים אקראיים.

2. **אתחול משתנים**:
    - `year = 0`: מאתחל את המשתנה year למעקב אחר השנה הנוכחית (מתחיל מ-0).
    - `population = 100`: מאתחל את המשתנה population למעקב אחר כמות האוכלוסייה (מתחיל מ-100).
    - `grain = 2800`: מאתחל את המשתנה grain למעקב אחר כמות התבואה (מתחיל מ-2800).
    - `land = 1000`: מאתחל את המשתנה land למעקב אחר כמות הקרקע (מתחיל מ-1000).
    - `print("Добро пожаловать в игру Хаммурапи!")`: מציג הודעת ברכה לשחקן.

3. **לולאת המשחק הראשית `while year < 10 and population > 0:`**:
    - הלולאה ממשיכה כל עוד לא חלפו 10 שנים או שהאוכלוסייה גדולה מ-0.
    - `print(f"\nГод {year + 1}")`: מציג את השנה הנוכחית.
    - `print(f"Население: {population}")`: מציג את כמות האוכלוסייה הנוכחית.
    - `print(f"Зерно: {grain} бушелей")`: מציג את כמות התבואה הנוכחית.
    - `print(f"Земля: {land} акров")`: מציג את כמות הקרקע הנוכחית.

4. **קביעת מחיר הקרקע**:
    - `land_price = random.randint(15, 25)`: מייצר מחיר אקראי לאקר קרקע בטווח של 15 עד 25 בושלים.
    - `print(f"Цена земли: {land_price} бушелей за акр")`: מציג את מחיר הקרקע הנוכחי.

5. **קלט כמות קרקע למסחר**:
   - `while True: try...except ValueError`: לולאה לקלט כמות הקרקע, מטפלת בשגיאת קלט של מספר לא שלם.
   - `land_trade = int(input("Сколько земли купить (+) или продать (-)? "))`: מבקש מהמשתמש כמות קרקע לקנייה (ערך חיובי) או מכירה (ערך שלילי).

6. **בדיקת מספיקות משאבים למסחר**:
   - `while True: if land_trade > 0 and grain < land_trade * land_price:`: לולאה לבדיקה האם יש מספיק תבואה לקניית קרקע.
   - `elif land_trade < 0 and land < abs(land_trade):`: לולאה לבדיקה האם יש מספיק קרקע למכירה.
   - אם אין מספיק משאבים, מוצגת הודעה מתאימה ומתבקש קלט חדש.

7. **עדכון כמות הקרקע והתבואה**:
   - `land += land_trade`: מעדכן את כמות הקרקע.
   - `grain -= land_trade * land_price`: מעדכן את כמות התבואה.

8. **קלט כמות תבואה לזריעה**:
   - `while True: try...except ValueError`: לולאה לקלט כמות תבואה לזריעה עם טיפול בשגיאת קלט של מספר לא שלם.
   - `grain_to_plant = int(input("Сколько зерна использовать для посева? "))`: מבקש מהמשתמש כמות תבואה לזריעה.

9. **בדיקת מספיקות תבואה לזריעה**:
    -  `while True: if grain < grain_to_plant:`: לולאה לבדיקה האם יש מספיק תבואה לזריעה.
    - אם אין מספיק תבואה, מוצגת הודעה ומתבקש קלט חדש.

10. **חישוב יבול**:
    - `yield_per_acre = random.randint(1, 8)`: מייצר כמות יבול אקראית של 1 עד 8 בושלים לאקר.
    - `harvest = grain_to_plant * yield_per_acre`: מחשב את סך היבול.
    - `grain += harvest`: מוסיף את היבול למלאי התבואה הכולל.

11. **חישוב נזק מעכברים**:
    - `rats_damage = int(random.random() * 0.1 * grain)`: מחשב נזק אקראי מעכברים (בין 0 ל-10% ממלאי התבואה).
    - `grain -= rats_damage`: מפחית את כמות התבואה בגובה הנזק מהעכברים.
    - `print(f"Крысы съели {rats_damage} бушелей зерна.")`: מציג הודעה על כמות התבואה שנאכלה על ידי עכברים.

12. **קלט כמות תבואה להאכלה**:
     - `while True: try...except ValueError`: לולאה לקלט כמות תבואה להאכלה, מטפלת בשגיאת קלט של מספר לא שלם.
    - `grain_to_feed = int(input("Сколько зерна отдать на пропитание? "))`: מבקש מהמשתמש כמות תבואה להזנת האוכלוסייה.

13. **בדיקת מספיקות תבואה להאכלה**:
     - `if grain_to_feed >= population:`: בודק האם יש מספיק תבואה להזנת כל האוכלוסייה.
     - `population = min(1000, int(population * 1.1))`: אם יש מספיק תבואה, מגדיל את האוכלוסייה ב-10%, אך לא יותר מ-1000.
     - `grain -= grain_to_feed`: מפחית את מלאי התבואה בכמות שהוקצתה להאכלת האוכלוסייה.
     - `else:`: מתבצע אם אין מספיק תבואה.
     - `starved = int(population * (1 - grain_to_feed / population ))`: מחשב את כמות המתים מרעב באופן יחסי לחוסר בתבואה.
     - `population -= starved`: מפחית את כמות האוכלוסייה במספר המתים.
     - `print(f"{starved} человек умерло от голода.")`: מציג הודעה על כמות המתים מרעב.
     - `if population <= 0:`: בודק האם כל האוכלוסייה מתה.
     - `print("Вы проиграли, все население вымерло от голода!")`: מציג הודעה על הפסד במקרה של מות כל האוכלוסייה.

14. **הגדלת השנה**:
    - `year += 1`: מגדיל את ערך השנה ב-1.

15. **סיום המשחק**:
    - `if year == 10:`: בודק האם חלפו 10 שנים.
    - `print("Игра окончена. Прошло 10 лет.")`: מציג הודעה על סיום המשחק אם חלפו 10 שנים.
"""
"""
הסבר קוד:
1. **ייבוא מודול random**:
   - `import random`: מייבא את המודול `random`, המשמש ליצירת מספרים אקראיים במשחק.

2. **אתחול משתנים**:
   - `year = 0`: מאתחל את המשתנה `year` למעקב אחר השנה הנוכחית, החל מ-0.
   - `population = 100`: מגדיר את כמות האוכלוסייה ההתחלתית ל-100 איש.
   - `grain = 2800`: מגדיר את כמות התבואה ההתחלתית ל-2800 בושלים.
   - `land = 1000`: מגדיר את כמות הקרקע ההתחלתית ל-1000 אקרים.
   - `print("Добро пожаловать в игру Хаммурапи!")`: מציג הודעת ברכה לשחקן.

3. **לולאת המשחק הראשית `while year < 10 and population > 0:`**:
   - לולאת `while` מבטיחה שהמשחק יתבצע כל עוד לא חלפו 10 שנים (`year < 10`) או כל עוד האוכלוסייה גדולה מ-0 (`population > 0`).
   - `print(f"\nГод {year + 1}")`: מציג את שנת המשחק הנוכחית.
   - `print(f"Население: {population}")`: מציג את כמות האוכלוסייה הנוכחית.
   - `print(f"Зерно: {grain} бушелей")`: מציג את כמות התבואה הנוכחית.
   - `print(f"Земля: {land} акров")`: מציג את כמות הקרקע הנוכחית.

4. **חישוב מחיר קרקע**:
   - `land_price = random.randint(15, 25)`: מייצר מספר שלם אקראי בטווח של 15 עד 25 עבור מחיר הקרקע.
   - `print(f"Цена земли: {land_price} бушелей за акр")`: מציג את מחיר הקרקע.

5. **קלט עבור קנייה/מכירה של קרקע**:
   - בלוק `while True:` משמש כדי לבקש קלט מהמשתמש עד שיזין נתונים תקינים.
   - `try...except ValueError`: בלוק זה מטפל בשגיאה אם המשתמש מזין ערך שאינו מספר שלם.
   - `land_trade = int(input("Сколько земли купить (+) или продать (-)? "))`: מבקש מהמשתמש כמות קרקע לקנייה (ערך חיובי) או מכירה (ערך שלילי).

6. **בדיקת קלט עבור קנייה/מכירה של קרקע**:
   - בלוק `while True:` משמש כדי לבקש קלט מהמשתמש עד שיתקיימו התנאים לתקינות הנתונים שהוזנו.
   - `if land_trade > 0 and grain < land_trade * land_price:`: בודק האם יש מספיק תבואה לקניית קרקע.
   - `elif land_trade < 0 and land < abs(land_trade):`: בודק האם יש מספיק קרקע למכירה.

7. **עדכון מלאי קרקע ותבואה**:
   - `land += land_trade`: מעדכן את כמות הקרקע. אם הערך של `land_trade` חיובי, הקרקע נקנית וכמות הקרקע עולה. אם `land_trade` שלילי, הקרקע נמכרת וכמות הקרקע יורדת.
   - `grain -= land_trade * land_price`: מעדכן את כמות התבואה בהתאם לקנייה או מכירה של קרקע.

8. **קלט עבור זריעת תבואה**:
    - בלוק `while True:` משמש כדי לבקש קלט מהמשתמש עד שיזין נתונים תקינים.
    - `try...except ValueError`: בלוק זה מטפל בשגיאה אם המשתמש מזין ערך שאינו מספר שלם.
   - `grain_to_plant = int(input("Сколько зерна использовать для посева? "))`: מבקש מהמשתמש כמות תבואה לזריעה.

9. **בדיקת מספיקות תבואה לזריעה**:
   -  בלוק `while True:` משמש כדי לבקש קלט מהמשתמש עד שיתקיימו התנאים לתקינות הנתונים שהוזנו.
   - `if grain < grain_to_plant:`: בודק האם יש מספיק תבואה לזריעה. אם לא, מוצגת הודעה ומתבקש קלט חדש.

10. **חישוב יבול**:
    - `yield_per_acre = random.randint(1, 8)`: מייצר יבול אקראי (בין 1 ל-8 בושלים לאקר).
    - `harvest = grain_to_plant * yield_per_acre`: מחשב את סך יבול התבואה.
    - `grain += harvest`: מגדיל את כמות התבואה בגובה היבול.

11. **חישוב נזק מעכברים**:
    - `rats_damage = int(random.random() * 0.1 * grain)`: מחשב נזק אקראי מעכברים (בין 0 ל-10% מכמות התבואה הנוכחית).
    - `grain -= rats_damage`: מפחית את כמות התבואה בגובה הנזק מהעכברים.
    - `print(f"Крысы съели {rats_damage} бушелей зерна.")`: מציג הודעה על כמות התבואה שנאכלה על ידי עכברים.

12. **קלט עבור האכלת אוכלוסייה**:
    - בלוק `while True:` משמש כדי לבקש קלט מהמשתמש עד שיזין נתונים תקינים.
    - `try...except ValueError`: בלוק זה מטפל בשגיאה אם המשתמש מזין ערך שאינו מספר שלם.
    -`grain_to_feed = int(input("Сколько зерна отдать на пропитание? "))`: מבקש מהמשתמש כמות תבואה להזנת האוכלוסייה.

13. **בדיקת מספיקות תבואה להאכלה**:
    - `if grain_to_feed >= population:`: בודק האם יש מספיק תבואה להזנת כל האוכלוסייה.
    - `population = min(1000, int(population * 1.1))`: אם יש מספיק תבואה, מגדיל את האוכלוסייה ב-10%, אך לא יותר מ-1000.
    - `grain -= grain_to_feed`: מפחית את כמות התבואה בכמות שהוקצתה להאכלת האוכלוסייה.
    - `else:`: מתבצע אם אין מספיק תבואה.
    - `starved = int(population * (1 - grain_to_feed / population ))`: מחשב את כמות המתים מרעב באופן יחסי לחוסר בתבואה.
    - `population -= starved`: מפחית את כמות האוכלוסייה במספר המתים מרעב.
    - `print(f"{starved} человек умерло от голода.")`: מציג הודעה על כמות המתים מרעב.
    - `if population <= 0:`: בודק האם כל האוכלוסייה מתה.
    - `print("Вы проиграли, все население вымерло от голода!")`: מציג הודעה על הפסד במקרה של מות כל האוכלוסייה.

14. **הגדלת השנה**:
    - `year += 1`: מגדיל את ערך השנה ב-1.

15. **סיום המשחק**:
    - `if year == 10:`: בודק האם חלפו 10 שנים.
    - `print("Игра окончена. Прошло 10 лет.")`: מציג הודעה על סיום המשחק אם חלפו 10 שנים.
```
ROULET:
=================
מורכבות: 5
-----------------
המשחק "רולטה" הוא גרסה פשוטה של משחק הרולטה, שבה השחקן מבצע הימור על מספר בטווח 0 עד 36. המחשב "מסובב" את הרולטה ובוחר מספר אקראי מאותו טווח. אם המספר שבחר השחקן זהה למספר שעלה ברולטה, השחקן מנצח.
כללי המשחק:
1. השחקן מזין מספר בטווח 0 עד 36, עליו הוא מעוניין להמר.
2. המחשב בוחר מספר אקראי בטווח 0 עד 36 (תוצאת הרולטה).
3. אם המספר שבחר השחקן זהה למספר שעלה ברולטה, השחקן מנצח.
4. המחשב מציג את תוצאת המשחק, מודיע האם השחקן ניצח או הפסיד, ומראה את המספר שעלה ברולטה.
-----------------
אלגוריתם:
1. בקש מהשחקן להזין מספר בטווח 0 עד 36.
2. צור מספר אקראי בטווח 0 עד 36 (תוצאת הרולטה).
3. אם המספר של השחקן שווה לתוצאת הרולטה, הצג הודעה על זכייה.
4. אחרת, הצג הודעה על הפסד ואת תוצאת הרולטה.
5. סיום המשחק.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InputUserNumber["הזנת מספר ע"י השחקן: <code><b>userNumber</b></code> (0-36)"]
    InputUserNumber --> GenerateRouletteNumber["יצירת מספר רולטה אקראי: <code><b>rouletteNumber</b></code> (0-36)"]
    GenerateRouletteNumber --> CheckWin{"בדיקה: <code><b>userNumber == rouletteNumber?</b></code>"}
    CheckWin -- כן --> OutputWin["הצגת הודעה: <b>YOU WIN!</b>"]
    OutputWin --> End["סוף"]
    CheckWin -- לא --> OutputLose["הצגת הודעה: <b>YOU LOSE, THE NUMBER WAS <code>{rouletteNumber}</code></b>"]
    OutputLose --> End
```

מקרא:
    Start - התחלת התוכנית.
    InputUserNumber - בקשת קלט מהמשתמש עבור מספר בטווח 0 עד 36 ושמירתו במשתנה userNumber.
    GenerateRouletteNumber - יצירת מספר שלם אקראי בטווח שבין 0 ל-36 ושמירתו במשתנה rouletteNumber.
    CheckWin - בדיקה האם המספר שהוזן ע"י המשתמש userNumber שווה למספר הרולטה האקראי rouletteNumber.
    OutputWin - הצגת הודעת זכייה, אם המספרים שווים.
    OutputLose - הצגת הודעת הפסד, אם המספרים אינם שווים, עם ציון מספר הרולטה rouletteNumber.
    End - סיום התוכנית.
"""
import random

# בקש מהמשתמש להזין את המספר עליו הוא מהמר
try:
    userNumber = int(input("Введите число от 0 до 36: "))
except ValueError:
        print("Пожалуйста, введите целое число.")
        exit() # מסיים את ריצת התוכנית אם הוזן סוג נתונים שגוי.

# בדוק שהמספר שהוזן נמצא בטווח המותר
if userNumber < 0 or userNumber > 36:
    print("Число должно быть в диапазоне от 0 до 36.")
    exit() # מסיים את ריצת התוכנית אם המספר מחוץ לטווח.

# צור מספר אקראי בטווח 0 עד 36 (המספר שעלה ברולטה)
rouletteNumber = random.randint(0, 36)

# בדוק האם המספר של המשתמש זהה למספר הרולטה
if userNumber == rouletteNumber:
    print("ПОЗДРАВЛЯЮ! Вы выиграли!") # הצג הודעה על זכייה
else:
    print(f"Вы проиграли. Число было {rouletteNumber}") # הצג הודעה על הפסד ואת המספר שעלה

"""
הסבר קוד:
1.  **ייבוא מודול `random`**:
    -   `import random`: מייבא את מודול `random`, המשמש ליצירת מספר אקראי.
2.  **הזנת ההימור ע"י המשתמש**:
    -  `try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין ערך שאינו מספר שלם, תוצג הודעת שגיאה, והתוכנית תסתיים.
    -   `userNumber = int(input("Введите число от 0 до 36: "))`: מבקש מהמשתמש מספר עליו הוא מעוניין להמר, וממיר אותו למספר שלם, שומר את התוצאה ב-`userNumber`.
3.  **בדיקת תקינות ההימור**:
    -   `if userNumber < 0 or userNumber > 36:`: בודק האם המספר שהוזן נמצא בטווח המותר שבין 0 ל-36.
    -  `print("Число должно быть в диапазоне от 0 до 36.")`: מציג הודעת שגיאה אם המספר מחוץ לטווח.
    - `exit()`: מסיים את ריצת התוכנית אם המספר מחוץ לטווח.
4.  **יצירת מספר הרולטה האקראי**:
    -   `rouletteNumber = random.randint(0, 36)`: יוצר מספר שלם אקראי בטווח שבין 0 ל-36, המייצג את תוצאת "סיבוב" הרולטה, ושומר אותו ב-`rouletteNumber`.
5.  **בדיקת זכייה**:
    -   `if userNumber == rouletteNumber:`: בודק האם המספר שהוזן ע"י המשתמש זהה למספר שעלה ברולטה.
    -   `print("ПОЗДРАВЛЯЮ! Вы выиграли!")`: אם המספרים זהים, מציג הודעת זכייה.
    -   `else:`: אם המספרים אינם זהים, מבצע את קטע הקוד הבא.
    -   `print(f"Вы проиграли. Число было {rouletteNumber}")`: מציג הודעת הפסד ואת המספר שעלה ברולטה.
"""
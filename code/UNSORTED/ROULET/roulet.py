ROULET:
=================
רמת קושי: 5
-----------------
משחק "רולטה" הוא גרסה פשוטה של רולטה, שבה שחקן מבצע הימור על מספר בטווח שבין 0 ל-36. המחשב "מסובב" את הרולטה ובוחר מספר אקראי מאותו הטווח. אם המספר של השחקן תואם למספר שעלה ברולטה, השחקן מנצח.
כללי המשחק:
1. השחקן מזין מספר מ-0 עד 36, שעליו הוא רוצה להמר.
2. המחשב בוחר מספר אקראי מ-0 עד 36 (תוצאת הרולטה).
3. אם המספר של השחקן תואם למספר שעלה ברולטה, השחקן מנצח.
4. המחשב מציג את תוצאת המשחק, מדווח אם השחקן ניצח או הפסיד, ומציג את המספר שעלה ברולטה.
-----------------
אלגוריתם:
1. בקשת קלט מספר מהשחקן, בטווח 0 עד 36.
2. יצירת מספר אקראי בטווח 0 עד 36 (תוצאת הרולטה).
3. אם המספר של השחקן שווה לתוצאת הרולטה, אז הדפסת הודעת זכייה.
4. אחרת, הדפסת הודעת הפסד ואת תוצאת הרולטה.
5. סוף המשחק.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InputUserNumber["קלט מספר על ידי השחקן: <code><b>userNumber</b></code> (0-36)"]
    InputUserNumber --> GenerateRouletteNumber["יצירת מספר רולטה אקראי: <code><b>rouletteNumber</b></code> (0-36)"]
    GenerateRouletteNumber --> CheckWin{"בדיקה: <code><b>userNumber == rouletteNumber?</b></code>"}
    CheckWin -- כן --> OutputWin["הדפסת הודעה: <b>YOU WIN!</b>"]
    OutputWin --> End["סוף"]
    CheckWin -- לא --> OutputLose["הדפסת הודעה: <b>YOU LOSE, THE NUMBER WAS <code>{rouletteNumber}</code></b>"]
    OutputLose --> End
```

מקרא:
    Start - התחלת התוכנית.
    InputUserNumber - בקשת קלט מספר מהמשתמש בטווח מ-0 עד 36 ושמירתו במשתנה userNumber.
    GenerateRouletteNumber - יצירת מספר שלם אקראי בטווח מ-0 עד 36 ושמירתו במשתנה rouletteNumber, המייצג את תוצאת ה"סיבוב" של הרולטה.
    CheckWin - בדיקה האם המספר שהוזן על ידי המשתמש userNumber שווה למספר הרולטה האקראי rouletteNumber.
    OutputWin - הדפסת הודעת זכייה אם המספרים שווים.
    OutputLose - הדפסת הודעת הפסד אם המספרים אינם שווים, בצירוף מספר הרולטה rouletteNumber.
    End - סוף התוכנית.
"""
import random

# בקשת קלט מספר מהמשתמש, שעליו הוא מהמר
try:
    userNumber = int(input("הזן מספר מ-0 עד 36: "))
except ValueError:
        print("אנא הזן מספר שלם.")
        exit() # סיום התוכנית אם הוזן סוג נתונים שגוי.

# בדיקה שהמספר שהוזן נמצא בטווח המותר
if userNumber < 0 or userNumber > 36:
    print("המספר חייב להיות בטווח מ-0 עד 36.")
    exit() # סיום התוכנית אם המספר מחוץ לטווח.

# יצירת מספר אקראי מ-0 עד 36 (המספר שעלה ברולטה)
rouletteNumber = random.randint(0, 36)

# בדיקה האם המספר של המשתמש תואם למספר הרולטה
if userNumber == rouletteNumber:
    print("מזל טוב! ניצחת!") # הדפסת הודעת זכייה
else:
    print(f"הפסדת. המספר היה {rouletteNumber}") # הדפסת הודעת הפסד והמספר שעלה

"""
הסבר הקוד:
1.  **ייבוא המודול `random`**:
    -   `import random`: מייבא את המודול `random`, המשמש ליצירת מספר אקראי.
2.  **קלט ההימור של המשתמש**:
    -  `try...except ValueError`: בלוק try-except מטפל בשגיאות קלט אפשריות. אם המשתמש יזין מספר שאינו שלם, תוצג הודעת שגיאה, והתוכנית תסתיים.
    -   `userNumber = int(input("הזן מספר מ-0 עד 36: "))`: מבקש מהמשתמש מספר שעליו הוא רוצה להמר, וממיר אותו למספר שלם, ושומר את התוצאה ב-`userNumber`.
3.  **בדיקת תקינות ההימור**:
    -   `if userNumber < 0 or userNumber > 36:`: בודק האם המספר שהוזן נמצא בטווח המותר מ-0 עד 36.
    -  `print("המספר חייב להיות בטווח מ-0 עד 36.")`: מדפיס הודעת שגיאה אם המספר מחוץ לטווח.
    - `exit()`: מסיים את התוכנית אם המספר מחוץ לטווח.
4.  **יצירת מספר רולטה אקראי**:
    -   `rouletteNumber = random.randint(0, 36)`: מייצר מספר שלם אקראי בטווח מ-0 עד 36, המייצג את תוצאת ה"סיבוב" של הרולטה, ושומר אותו ב-`rouletteNumber`.
5.  **בדיקת זכייה**:
    -   `if userNumber == rouletteNumber:`: בודק האם המספר שהוזן על ידי המשתמש תואם למספר שעלה ברולטה.
    -   `print("מזל טוב! ניצחת!")`: אם המספרים תואמים, מדפיס הודעת זכייה.
    -   `else:`: אם המספרים אינם תואמים, מבצע את בלוק הקוד הבא.
    -   `print(f"הפסדת. המספר היה {rouletteNumber}")`: מדפיס הודעת הפסד ואת המספר שעלה ברולטה.
"""
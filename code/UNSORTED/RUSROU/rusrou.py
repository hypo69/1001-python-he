RUSROU:
=================
רמת קושי: 3
-----------------
המשחק "רולטה רוסית" הוא משחק פשוט שבו המחשב מדמה סיבוב של תוף אקדח עם קליע בודד. השחקן בוחר בין לחיצה על ההדק לבין עצירה. מטרת המשחק היא להימנע מהירייה. אם השחקן ממשיך לשחק וללחוץ על ההדק ברגע שבו הקליע נמצא בתוף, אזי הוא מפסיד.

כללי המשחק:
1.  המחשב מדמה תוף אקדח עם שש קָמָרוֹת, באחת מהן ממוקם קליע באופן אקראי.
2.  השחקן מחליט אם ללחוץ על ההדק ("нажать" או "да") או לעצור את המשחק ("нет").
3.  אם השחקן מחליט ללחוץ על ההדק, מתבצעת בדיקה האם הקליע היה בקָמָרָה הנוכחית.
4.  אם הקליע היה שם, השחקן מפסיד.
5.  אם הקליע לא היה שם, המשחק ממשיך, והמחשב מדמה את הסיבוב הבא של התוף.
6.  אם השחקן מחליט לעצור את המשחק, הוא נחשב למנצח.
-----------------
אלגוריתם:
1.  הזן את המשתנה `bullet` במספר אקראי בין 1 ל-6 (מספר הקָמָרָה עם הקליע).
2.  התחל לולאה "כל עוד המשחק לא הסתיים":
    2.1. שאל את השחקן האם הוא רוצה ללחוץ על ההדק ("нажать" או "да") או לעצור ("нет").
    2.2. אם השחקן בוחר "нет", אזי הצג הודעה על ניצחון וסיים את המשחק.
    2.3. אם השחקן בוחר "да", אזי יצר מספר אקראי בין 1 ל-6 (מספר הקָמָרָה הנוכחית).
    2.4. אם מספר הקָמָרָה עם הקליע (`bullet`) חופף למספר הקָמָרָה הנוכחית, הצג הודעה על הפסד וסיים את המשחק.
    2.5. אם מספר הקָמָרָה עם הקליע (`bullet`) אינו חופף למספר הקָמָרָה הנוכחית, דווח כי "נשמע צליל לחיצה" והמשך את המשחק.
-----------------
תרשים זרימה:
```mermaid
flowchart TD
    Start["התחלה"] --> InitializeBullet["<p align='left'>הזנת המשתנה <code><b>bullet</b></code>:
    <code><b>bullet = random(1, 6)</b></code></p>"]
    InitializeBullet --> LoopStart{"התחלת הלולאה: כל עוד המשחק לא הסתיים"}
    LoopStart --> InputChoice["בקשת קלט: ללחוץ או לא?"]
    InputChoice -- "Нет" --> OutputWin["פלט הודעה: <b>YOU'RE SAFE</b>"]
    OutputWin --> End["סיום"]
    InputChoice -- "Да" --> GenerateChamber["<code><b>currentChamber = random(1, 6)</b></code>"]
     GenerateChamber --> CheckChamber{"בדיקה: <code><b>currentChamber == bullet?</b></code>"}
    CheckChamber -- Да --> OutputLose["פלט הודעה: <b>BANG-YOU'RE DEAD</b>"]
    OutputLose --> End
    CheckChamber -- Нет --> OutputClick["פלט הודעה: <b>CLICK</b>"]
    OutputClick --> LoopStart
```
מקרא:
   Start - התחלת המשחק.
    InitializeBullet - הזנת המשתנה `bullet` במספר אקראי בין 1 ל-6, המייצג את הקָמָרָה עם הקליע.
    LoopStart - התחלת הלולאה שנמשכת עד שהשחקן מחליט לעצור או מפסיד.
    InputChoice - בקשת קלט מהשחקן: "нажать" או "нет".
    OutputWin - פלט הודעה "YOU'RE SAFE", אם השחקן בוחר "нет", וסיום המשחק.
    End - סיום המשחק.
    GenerateChamber - יצירת מספר אקראי `currentChamber` בין 1 ל-6, המייצג את הקָמָרָה הנוכחית.
    CheckChamber - בדיקה האם `currentChamber` חופף לקָמָרָה עם הקליע `bullet`.
    OutputLose - פלט הודעה "BANG-YOU'RE DEAD" וסיום המשחק, אם הקָמָרָה עם הקליע חפפה.
    OutputClick - פלט הודעה "CLICK", אם הקליע לא נפגע, והמשך הלולאה.
```python
import random

# Генерируем случайное число от 1 до 6, которое будет номером каморы с патроном
bullet = random.randint(1, 6)

# Начинаем основной цикл игры
while True:
    # Запрашиваем у пользователя выбор: нажать или нет
    choice = input("Нажать на курок? (да/нет): ").lower()

    # Если пользователь выбирает "нет", то он выигрывает
    if choice == "нет":
        print("Вы в безопасности!")
        break  # Завершаем игру

    # Если пользователь выбирает "да", то продолжаем игру
    if choice == "да":
        # Генерируем случайное число от 1 до 6, которое будет номером текущей каморы
        current_chamber = random.randint(1, 6)

        # Проверяем, попал ли патрон в текущую камору
        if current_chamber == bullet:
            print("БАХ-Вы мертвы!")  # Если попал, то игрок проигрывает
            break  # Завершаем игру
        else:
            print("Щелчок")  # Если не попал, то игра продолжается
    else:
         print("Некорректный ввод. Пожалуйста, введите 'да' или 'нет'.")
```
הסבר על הקוד:

1.  **ייבוא המודול `random`:**
    -   `import random`: מייבא את המודול `random`, המשמש ליצירת מספרים אקראיים.
2.  **הזנת מיקום הקליע:**
    -   `bullet = random.randint(1, 6)`: מייצר מספר שלם אקראי בין 1 ל-6, המייצג את מספר הקָמָרָה שבה נמצא הקליע. משתנה זה שומר את הקָמָרָה ה"טעונה".
3.  **לולאת המשחק הראשית:**
    -   `while True:`: לולאה אינסופית שנמשכת כל עוד המשחק לא הסתיים (השחקן לא החליט לעצור או לא הפסיד).
    -   **בקשת בחירה מהשחקן:**
        -   `choice = input("Нажать на курок? (да/нет): ").lower()`: מבקש קלט מהמשתמש (ללחוץ או לא) וממיר את הקלט לאותיות קטנות כדי לפשט את ההשוואה.
    -   **תנאי ניצחון (בחירה "нет"):**
        -   `if choice == "нет":`: אם השחקן מזין "нет", אזי הוא מנצח.
        -   `print("Вы в безопасности!")`: מוציא פלט של הודעת ניצחון.
        -   `break`: מסיים את הלולאה, כלומר המשחק נגמר.
    -   **תנאי המשך המשחק (בחירה "да"):**
        -   `if choice == "да":`: אם השחקן מזין "да", אזי המשחק ממשיך.
        -   `current_chamber = random.randint(1, 6)`: מייצר מספר שלם אקראי בין 1 ל-6, המייצג את מספר הקָמָרָה הנוכחית.
        -   **בדיקת הפסד:**
            -   `if current_chamber == bullet:`: בודק האם מספר הקָמָרָה הנוכחית חופף למספר הקָמָרָה שבה נמצא הקליע.
            -   `print("БАХ-Вы мертвы!")`: אם הקָמָרָה עם הקליע חפפה, אזי השחקן מפסיד ומוצגת הודעת הפסד.
            -   `break`: מסיים את הלולאה, כלומר המשחק נגמר.
        -   **המשך המשחק:**
            -   `else:`: אם הקליע לא היה בקָמָרָה הנוכחית.
            -   `print("Щелчок")`: מוצגת הודעה כי הקליע לא היה שם והמשחק ממשיך.
    -   **טיפול בקלט לא תקין:**
        - `else:`: אם הקלט אינו "да" ואינו "нет"
        - `print("Некорректный ввод. Пожалуйста, введите 'да' или 'нет'.")`: מוצגת הודעה על קלט לא תקין.